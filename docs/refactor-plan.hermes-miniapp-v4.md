# Refactor Plan — hermes-miniapp-v4

> Canonical plan file (live source of truth). Historical refresh snapshot lives at `docs/refactor-plan.hermes-miniapp-v4.refresh-2026-03-27.md`.

## Project snapshot
- Project: hermes-miniapp-v4
- Project slug: hermes-miniapp-v4
- Generated: 2026-03-27T18:12:07-06:00
- Branch: main
- Git status summary: dirty (tracked modifications in app/runtime/routes/server/tests plus untracked chat-title guards + local tool-demo scratch files)
- Analysis mode: refreshed backlog after live-browser audit of dev-auth + SSE streaming paths.

## Progress metrics
- Total items: 28
- Done: 26
- In progress: 0
- Blocked: 0
- Todo: 2
- % complete: 93%
- Last completed item: R25 follow-up 26 (read-sync gating extraction via chat_history_helpers)
- Last updated: 2026-03-29T18:30:18Z

## Next up
- R25: Split the remaining `static/app.js` monolith into behavior-scoped modules (bootstrap/auth, chat orchestration, latency/hydration, modal/admin UI).
- R27: Reduce production stream/debug logging noise and sensitive payload exposure.
- R28: Decompose oversized test monoliths and add browser-regression fixtures for live-risk paths.

Note: “Next up” is only a prioritized subset, not the full backlog.

## Recently completed
- R25 follow-up 26 (2026-03-29): Extended `static/chat_history_helpers.js` with read-sync gating ownership (`maybeMarkRead`) and rewired `static/app.js` `maybeMarkRead(...)` into a thin `chatHistoryController` delegate. Injected read-gating dependencies into chat-history controller creation (`getIsAuthenticated`, `isNearBottomFn`, `messagesContainer`, `unseenStreamChats`, `markReadInFlight`, `markReadFn`) so in-flight dedupe and near-bottom/unread/active-chat checks stay centralized in the helper while preserving behavior. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for auth/active/visibility/unread gating and forced/in-flight dedupe behavior; updated `tests/test_routes_meta.py` ownership/delegation assertions for the extracted helper/delegate.
- R25 follow-up 25 (2026-03-29): Extended `static/chat_history_helpers.js` with chat history/view state ownership (`addLocalMessage`, `updatePendingAssistant`, `syncActiveMessageView`, `scheduleActiveMessageView`) and rewired matching `static/app.js` wrappers into thin delegates through `chatHistoryController`. Injected `runAfterUiMutation` into the chat-history controller (with timeout fallback) so deferred active-chat rerenders remain centralized while preserving viewport-safe scheduling semantics. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for pending-assistant mutation contracts, empty-finalize guard behavior, and active-chat-only sync/scheduled render behavior; updated `tests/test_routes_meta.py` ownership/delegation assertions for the newly extracted helper functions.
- R25 follow-up 24 (combined follow-ups 22-24, 2026-03-29): Extended `static/render_trace_helpers.js` with history target lookup + visible stream patch ownership (`findLatestHistoryMessageByRole`, `findLatestAssistantHistoryMessage`, `patchVisiblePendingAssistant`, `patchVisibleToolTrace`) and rewired matching `static/app.js` functions into thin delegates with injected phase guards, key lookup, logging, and render/viewport dependencies. Added direct helper tests in `tests/render_trace_helpers.test.mjs` for latest-target derivation contracts, assistant visible pending-body patch behavior, and tool trace body/time patch behavior (including no-target success path); updated `tests/test_routes_meta.py` ownership/delegation assertions for the newly extracted helpers.

- R25 follow-up 18 (2026-03-29): Extended `static/render_trace_helpers.js` with message card render ownership (`messageVariantForRole`, `shouldSkipMessageRender`, `applyMessageMeta`, `renderMessageContent`) and rewired matching `static/app.js` wrappers to delegate through helper APIs with injected dependencies/callbacks (`formatMessageTime`, `roleLabelForMessage`, `renderBody`, `renderToolTraceBody`). Added direct helper tests for role/body skip contracts, metadata class/timestamp application, and tool-vs-body renderer dispatch; updated route-meta assertions for helper ownership and app-side delegation.
- R25 follow-up 17 (2026-03-29): Extended `static/render_trace_helpers.js` with message rendering ownership (`renderBody`, `renderToolTraceBody`, `roleLabelForMessage`) and rewired matching `static/app.js` wrappers to delegate through helper APIs with injected dependencies (`cleanDisplayText`, `escapeHtml`, `document`, `operatorDisplayName`). Added direct helper tests for fenced/non-fenced body rendering, tool-trace DOM/toggle behavior, and role-label mapping; updated route-meta assertions for helper ownership and app-side delegation.
- R25 follow-up 16 (2026-03-29): Extended `static/interaction_helpers.js` with selection-quote timer/reset helper ownership (`clearSelectionQuoteState`, `cancelSelectionQuoteTimer`, `scheduleSelectionQuoteClear`, `scheduleSelectionQuoteSync`) and rewired matching `static/app.js` wrappers to delegate through the helper module. Added direct helper tests for timer cancellation + scheduling behavior and selection-state clear/hide semantics; updated route-meta guard assertions for the newly extracted helper functions.
- R25 follow-up 15 (2026-03-29): Extended `static/interaction_helpers.js` with selection-quote/input utility ownership (`getQuoteWrapWidth`, `isCoarsePointer`, `applyQuoteIntoPrompt`, `activeSelectionQuote`, `quotePlacementKey`, `showSelectionQuoteAction`, `syncSelectionQuoteAction`) and rewired matching `static/app.js` functions to thin delegates. Added direct helper tests for quote-width bounds, coarse-pointer detection, and prompt quote insertion behavior; updated route-meta guard assertions so selection-quote positioning internals (`mobileToolbarUnsafeTop`, `composerTop`) track helper ownership.
- R25 follow-up 14 (2026-03-29): Extended `static/interaction_helpers.js` with quote text utilities (`unwrapLegacyQuoteBlock`, `normalizeQuoteSelection`, `splitGraphemes`, `wrapQuoteLine`, `formatQuoteBlock`) and rewired the corresponding `static/app.js` wrappers to delegate through the interaction helper module. Added direct Node helper coverage for legacy-frame unwrapping, whitespace/newline normalization, grapheme wrapping, and quote-block formatting; updated route-meta guard assertions to follow helper ownership of quote-format internals.
- R25 follow-up 13 (2026-03-29): Extended `static/runtime_helpers.js` with `createHapticUnreadController(...)` + `latestCompletedAssistantHapticKey(...)` and moved haptic/unread orchestration (`latestCompletedAssistantHapticKey`, `triggerIncomingMessageHaptic`, `incrementUnread`) out of `static/app.js` into runtime-controller delegates. Added direct Node runtime coverage and updated streaming hardening guards for new helper ownership.
- R25 follow-up 12 (2026-03-29): Extended `static/stream_controller.js` with `createToolTraceController(...)` and moved inline tool-trace orchestration (`resetToolStream`, `findPendingToolTraceMessage`, `ensurePendingToolTraceMessage`, `appendInlineToolTrace`, `finalizeInlineToolTrace`) out of `static/app.js` into helper-managed delegates. Added direct Node coverage in `tests/stream_controller.test.mjs` and updated streaming hardening guard assertions to follow extracted helper ownership.
- R25 follow-up 11 (2026-03-29): Extended `static/composer_state_helpers.js` with `createDraftController(...)` and moved draft storage orchestration (`loadDraftsFromStorage`, `persistDraftsToStorage`, `setDraft`, `getDraft`) out of `static/app.js` into controller-managed delegates. Added direct Node helper coverage in `tests/composer_state_helpers.test.mjs` and revalidated route/meta wiring suites.
- R25 follow-up 10 (2026-03-29): Extracted chat-list + pinned-collapse orchestration from `static/app.js` into `static/chat_tabs_helpers.js` via `createController(...)` (chat normalization/upsert/sync cleanup, pinned collapse state persistence/UI sync, auto-collapse threshold behavior, pin-button label sync), rewired `app.js` through thin delegates, and wired the new helper asset through template/version/no-store route plumbing. Added direct Node contract tests (`tests/chat_tabs_helpers.test.mjs`) plus route/template/static asset coverage updates.
- R25 follow-up 9 (2026-03-29): Extracted stream/activity chip orchestration into `runtime_helpers.createStreamActivityController(...)` and rewired `syncActivePendingStatus` plus send/resume status transitions (`active`, `error`, `network failure`, `reconnecting`, `already complete`, `reconnect failed`) to delegate from `static/app.js` through `streamActivityController`. Added direct runtime-helper tests covering pending/idle sync, active stream marker updates, reconnect/complete/failure gating, and preserved latency chip synchronization hooks.
- R25 follow-up 8 (2026-03-29): Extracted latency-chip orchestration from `static/app.js` into `runtime_helpers.createLatencyController(...)`, rewired `setChatLatency`/`syncActiveLatencyChip` in `app.js` to delegate through `latencyViewController`, and preserved viewport-safe chip updates by injecting `preserveViewportDuringUiMutation` + `setActivityChip` dependencies. Added direct runtime-helper tests for controller behavior (`setChatLatency` active update, `syncActiveLatencyChip` placeholder/restore).
- R25 follow-up 7 (2026-03-29): Extracted viewport-stability mutation helpers (`runAfterUiMutation`, `preserveViewportDuringUiMutation`) from `static/app.js` into `static/composer_viewport_helpers.js`, rewired `app.js` through controller delegates with dynamic getters (`activeChatId`, scroll bookkeeping maps, jump-visibility callback), and expanded direct Node coverage for RAF/timeout scheduling plus pinned-bottom viewport preservation behavior.
- R25 follow-up 6 (2026-03-29): Extracted render-trace state/toggle/logging helpers from `static/app.js` into `static/render_trace_helpers.js`, rewired `app.js` via a dedicated controller (`syncRenderTraceBadge`, toggle handler, trace logging), and added helper-specific Node tests plus template/version/no-store route metadata coverage for the new static asset.
- R25 follow-up 5 (2026-03-29): Extracted startup/event-binding shell glue from `static/app.js` into `static/startup_bindings_helpers.js` (core event listeners, async action button binding, shell/settings/dev-auth lifecycle wiring), rewired `app.js` through thin delegates + startup installers, and added direct Node helper contract coverage with route/template/static no-store assertions for the new helper asset.
- R26 (2026-03-28): Moved stream lifecycle helpers (`hydrateChatAfterGracefulResumeCompletion`, `consumeStreamWithReconnect`) into `static/stream_controller.js`, wired `app.js` through controller delegates, and added direct Node contract tests plus guard coverage for the consolidated send/resume/hydration flow.
- R24 (2026-03-28): Removed secret-bearing dev-auth query-string bootstrap, replaced prompt-based desktop auth with an in-app modal, and redacted sensitive query params from debug request logging; live browser verification confirmed desktop sign-in works without secrets appearing in request paths.
- R6 follow-up 3 (2026-03-28): Extended `runtime.incident_snapshot` with rolling `5m/15m/60m` rate windows (`runtime` retry/dead-letter + `terminal` done/error), terminal age quantiles (`median`, `p95`), and a lightweight `severity_hint` (`level` + `reason`) for fast triage; added route/event-broker regressions and README operator guidance.
- R6 follow-up 2 (2026-03-28): Added operator-facing `runtime.incident_snapshot` diagnostics to `/api/runtime/status` (worker health + terminal-event rollup + deduped recent error messages), added runtime/event-broker regressions, and expanded README troubleshooting docs.
- R22 (2026-03-28): Extracted interaction orchestration (composer Enter-submit shortcut + selection-quote controller/event wiring) from `static/app.js` into new `static/interaction_helpers.js`, kept behavior-preserving delegates in `app.js`, and added direct Node contract tests plus template/version/no-store route coverage.
- R6 follow-up (2026-03-28): Added operator-facing queue-recovery diagnostics to `/api/runtime/status` (`startup_recovered_running_total`, `startup_clamped_exhausted_total`, `preclaim_dead_letter_total`) and documented runtime-status troubleshooting workflow in README for future incidents.
- R19 (2026-03-28): Extracted desktop keyboard navigation/focus-control helpers from `static/app.js` into `static/keyboard_shortcuts_helpers.js`, rewired `app.js` to delegate through thin wrappers, and added dedicated Node helper tests plus asset/version/cache-route wiring coverage.
- R21 (2026-03-28): Extended frontend helper-module split contract checks in route/template tests to ensure script ordering + cache semantics remain stable as helper modules are introduced.
- R20 (2026-03-28): Stabilized no-store/version wiring for helper JS static assets by centralizing helper-version injection through `server_public_routes.py` and static asset cache policy assertions.
- R23 (2026-03-28): Added cross-layer regression sweep for frontend helper extraction path (Python route tests + Node helper suites) to prevent behavior drift during ongoing app.js decomposition.
- R14 (2026-03-28): Decomposed `store_jobs.py` lifecycle internals into focused helper modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`) while preserving `SessionStore` public method contracts and SQL behavior.
- R17 (2026-03-28): Added direct contract/regression suites for extracted modules (`tests/test_hermes_client_bootstrap.py`, `tests/test_job_runtime_events.py`, `tests/test_job_runtime_chat_job.py`, `tests/test_server_startup.py`, `tests/test_server_public_routes.py`) to prevent silent drift.
- R13 (2026-03-28): Split runtime event-bus responsibilities into a dedicated `JobEventBroker` (`job_runtime_events.py`) and wired `JobRuntime` through compatibility-preserving delegation/proxy attributes for monkeypatch/test contract stability.
- R12 (2026-03-28): Extracted Hermes client bootstrap/config parsing logic into `hermes_client_bootstrap.py` and routed `HermesClient` config-loading/routing methods through the dedicated bootstrap layer.
- R11 (2026-03-28): Extracted server startup diagnostics and public-route registration into `server_startup.py` and `server_public_routes.py`, reducing `server.py` composition blast radius while preserving entrypoints.
- R10 (2026-03-27): Expanded module-level transport/shim regressions with explicit tests for persistent worker exception wrapping, shim logger proxy behavior under close-failure cleanup, and shim subprocess module replacement contract.
- R9 (2026-03-27): Removed duplicated direct-agent tool-display formatting drift by making the parent process own `display` rendering from `tool_name`/`preview`/`args`; child runner now emits normalized tool payload only.
- R8 (2026-03-27): Added persistent-agent watchdog timeout around `event_queue.get(...)` in `_stream_via_persistent_agent` with deterministic `HermesClientError` timeout messaging including session context.
- R7 (2026-03-27): Split `hermes_client_agent.py` into focused modules (`hermes_client_agent_direct.py`, `hermes_client_agent_persistent.py`) and kept a compatibility composition shim for monkeypatch surfaces (`logger`, `subprocess`) and existing imports.

## Backlog summary
- High: 1
- Medium: 3
- Low: 0

## Backlog

- [x] R1: Harden direct-agent subprocess FD lifecycle
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client_agent.py`, `tests/test_hermes_client.py`
 - Why it matters: `_stream_via_agent` uses `Popen(..., stdin/stdout/stderr=PIPE)` and kills/waits process in `finally`, but does not explicitly close `process.stdout`/`process.stderr` handles after reader completion. Under repeated retries/failures this can contribute to transient FD pressure and amplify `[Errno 24] Too many open files` incidents.
 - Proposed change: Add deterministic handle cleanup (`stdin`/`stdout`/`stderr`) with idempotent close helpers after stream loop exits and on timeout/error paths; ensure reader-thread completion ordering does not retain open pipes.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Add/execute targeted regression that asserts pipe close methods are called in success + timeout paths.
 - Notes/dependencies: Should land before additional retry/worker hardening so FD behavior is stable.
 - Execution result: Completed 2026-03-27. Added `_safe_close_stream(...)` idempotent cleanup helper in `_stream_via_agent`, explicit teardown closes for stdin/stdout/stderr, and regression assertions for handle close behavior in success + timeout + non-zero exit tests.

- [x] R2: Repair broad-exception policy regression in `job_runtime.py`
 - Status: done
 - Severity: High
 - Scope/files: `job_runtime.py`, `tests/test_broad_exception_policy.py`
 - Why it matters: Current quality gate fails because newly introduced broad handlers in `_fd_metrics` are missing the required `broad-except-policy:` annotation.
 - Proposed change: Add policy-tagged justification comments (and logging/intentional-no-log declarations) for the two `except Exception` blocks.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py`
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py`
 - Notes/dependencies: Unblocks CI/static gate confidence for future refactors.
 - Execution result: Completed 2026-03-27. Added required `broad-except-policy:` annotations (with intentional-no-log rationale) on `_fd_metrics` broad handlers while preserving best-effort fallback behavior.

- [x] R3: Extend broad-exception policy coverage to `hermes_client_agent.py`
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_broad_exception_policy.py`, `hermes_client_agent.py`
 - Why it matters: `hermes_client_agent.py` contains multiple broad handlers (`except Exception`) that are currently outside the static policy gate. This creates a blind spot where new broad catches can regress observability standards.
 - Proposed change: Include `hermes_client_agent.py` in `TARGET_FILES`, annotate existing broad handlers with policy rationale, and ensure each handler logs or explicitly declares intentional-no-log.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py`
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
 - Notes/dependencies: Coordinate with R1 so cleanup-path handlers get final policy text once behavior is settled.
 - Execution result: Completed 2026-03-27. `TARGET_FILES` in policy test now includes `hermes_client_agent.py`; broad handlers are now policy-annotated and satisfy logging/intentional-no-log policy checks.

- [x] R4: Add runtime-level retry-exhaustion regression coverage (not only store-level)
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_routes_jobs_runtime.py`, `job_runtime.py`
 - Why it matters: Store-level tests now guard bounded attempts and dead-letter transitions, but runtime worker behavior still lacks a direct regression for user-facing exhaustion messaging and retry/dead telemetry under repeated `JobRetryableError` paths.
 - Proposed change: Add runtime test harness that forces retryable failures across attempts and asserts: (a) bounded attempt text, (b) terminal dead-letter behavior, (c) retrying stops at max.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py`
 - Notes/dependencies: Complements R1/R2 by proving end-to-end worker semantics.
 - Execution result: Completed 2026-03-27. Added `test_worker_retry_exhaustion_stops_at_max_and_surfaces_terminal_error` to assert retry exhaustion path semantics (no retries past max, dead-letter transition, bounded terminal messaging/events).

- [x] R5: Enforce `chat_jobs` invariants at schema level
 - Status: done
 - Severity: Medium
 - Scope/files: `store_schema.py`, migration path, `tests/test_store.py`
 - Why it matters: Retry logic now enforces limits in code, but DB still allows invalid states (`attempts < 0`, `max_attempts <= 0`, arbitrary status strings). Schema-level checks reduce corruption risk from manual edits/tooling mistakes and make recovery behavior predictable.
 - Proposed change: Introduce migration-safe check constraints (or equivalent guard/migration table rewrite for SQLite) for status domain and numeric bounds; backfill/coerce invalid rows during migration.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_store.py`
   - Migration regression on pre-existing DB fixture with invalid values.
 - Notes/dependencies: Design carefully for SQLite ALTER TABLE limitations.
 - Execution result: Completed 2026-03-27. Added `chat_jobs` CHECK invariants, legacy-row normalization, and migration-safe table rebuild path in `store_schema.py`; added `test_store_init_migrates_chat_jobs_invariants_and_normalizes_invalid_rows`.

- [x] R6: Expose FD pressure + retry/dead counters in runtime diagnostics endpoint
 - Status: done
 - Severity: Medium
 - Scope/files: `server.py`, `job_runtime.py`, `routes_jobs_runtime.py`, `tests/test_routes_jobs_runtime.py`
 - Why it matters: FD metrics are currently only in logs; operators lack fast API-visible health signals during incidents.
 - Proposed change: Add lightweight runtime diagnostics fields (e.g., `fd_open`, `fd_limit_soft`, recent dead-letter/retry counters) to `/api/runtime/status` while keeping sensitive data redacted.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_config.py`
 - Notes/dependencies: Should remain best-effort and non-fatal if `/proc`/`resource` are unavailable.
 - Execution result: Completed 2026-03-27. `register_jobs_runtime_routes` now uses a dynamic `runtime_getter`; `/api/runtime/status` includes `runtime` diagnostics payload, and regression coverage verifies exposed counters/FD fields.
 - Follow-up hardening (2026-03-28): Added queue recovery/claim diagnostics surfaced via `runtime.queue_diagnostics` (mirrored as flat aliases) with counters for `startup_recovered_running_total`, `startup_clamped_exhausted_total`, and `preclaim_dead_letter_total`; added store/runtime regression assertions and README operator docs for future incident triage.
 - Follow-up validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_streaming_hardening_guards.py` → `55 passed in 1.86s`.
   - `node --test tests/stream_controller.test.mjs tests/frontend_runtime.test.mjs tests/stream_state_helpers.test.mjs tests/shared_utils.test.mjs` → `22 passed, 0 failed`.
 - Follow-up 2 execution result (2026-03-28): Added `runtime.incident_snapshot` in `JobRuntime.runtime_diagnostics()` with worker health and terminal-event rollup (`JobEventBroker.terminal_rollup`), plus route-level assertion coverage and event-broker unit coverage.
 - Follow-up 2 validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py` → `23 passed in 1.57s`.
 - Follow-up 3 execution result (2026-03-28): Added rolling-window incident telemetry with bounded in-memory timelines (`terminal` done/error windows + runtime retry/dead windows), exposed terminal age quantiles (`median`, `p95`), and added `severity_hint` (`level`, `reason`) in `runtime.incident_snapshot`.
 - Follow-up 3 validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py` → `23 passed in 1.55s`.

- [x] R7: Split `hermes_client_agent.py` into focused modules
 - Status: done
 - Severity: Low
 - Scope/files: `hermes_client_agent.py` (+ new helper modules), `tests/test_hermes_client.py`
 - Why it matters: Single 500+ line module combines persistent runtime, direct subprocess streaming, formatting, and env wiring. This increases cognitive load and raises regression risk for future hardening.
 - Proposed change: Extract direct-agent subprocess orchestration and persistent-session runtime into separate internal modules with narrower responsibilities.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - `.venv/bin/python -m pytest -q` (full suite when host FD pressure is stable)
 - Notes/dependencies: Behavior-preserving refactor; prioritize after high/medium reliability items.
 - Execution result: Completed 2026-03-27. Extracted direct subprocess path into `hermes_client_agent_direct.py` and persistent runtime path into `hermes_client_agent_persistent.py`; kept `hermes_client_agent.py` as a backward-compatible composition shim and preserved monkeypatch compatibility for `logger`/`subprocess` surfaces.

- [x] R8: Add persistent-agent watchdog timeout and queue-drain hardening
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client_agent_persistent.py`, `tests/test_hermes_client.py`
 - Why it matters: `_stream_via_persistent_agent` currently blocks on `event_queue.get()` without timeout. If the worker thread stalls before posting `end` (e.g., dependency deadlock/hung call path), request handling can hang indefinitely.
 - Proposed change: Add bounded wait loop using `self.timeout_seconds` (or explicit persistent timeout knob), emit terminal timeout error with session context, and ensure worker completion path is still drained safely.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "persistent and (timeout or watchdog or stream_events)"`
   - Add regression test that simulates worker non-termination and asserts deterministic `HermesClientError` timeout.
 - Notes/dependencies: Coordinate with existing fallback-to-direct behavior so timeout surfaces remain user-readable and non-duplicative.
 - Execution result: Completed 2026-03-27. `_stream_via_persistent_agent` now uses bounded `event_queue.get(timeout=...)` polling with `self.timeout_seconds` watchdog and raises deterministic timeout errors including session_id. Added `test_persistent_agent_stream_times_out_when_worker_stalls`.

- [x] R9: De-duplicate direct-agent tool-progress formatter and emoji map
 - Status: done
 - Severity: Medium
 - Scope/files: `hermes_client_agent_direct.py`, optional helper module, `tests/test_hermes_client.py`
 - Why it matters: `_format_tool_progress()` and `_agent_runner_script()` each carry near-identical tool emoji maps/formatting logic. This creates drift risk and inconsistent UX when one path changes without the other.
 - Proposed change: Consolidate mapping/format rules in one canonical helper and have both in-process formatter and child runner consume the same serialized map/constants.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "tool_progress or stream_via_agent"`
   - Add snapshot-style assertion that parent/child formatter outputs stay equivalent for representative tool/progress payloads.
 - Notes/dependencies: Keep behavior backward compatible for current tool-progress modes (`all`, `new`, `verbose`, `off`).
 - Execution result: Completed 2026-03-27. Introduced module-level `_TOOL_PROGRESS_EMOJIS` canonical map, removed duplicated child-runner formatter/map, and made parent `_stream_via_agent` compute `display` from normalized `tool_name`/`preview`/`args`. Added tests validating script de-duplication and parent-side tool display formatting.

- [x] R10: Add explicit module-level regression tests for split direct/persistent transports + compatibility shim
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_hermes_client.py` (or split transport-specific test modules), `hermes_client_agent.py`
 - Why it matters: After the split, most coverage still drives behavior through higher-level client flows; there is limited direct contract testing for shim export/proxy behavior and isolated transport failure edges.
 - Proposed change: Add focused tests for shim proxy surfaces (`logger`, `subprocess`), direct transport teardown invariants, and persistent transport checkpoint/exception propagation edge cases.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Optional: split into `tests/test_hermes_client_agent_direct.py` / `tests/test_hermes_client_agent_persistent.py` and run both directly.
 - Notes/dependencies: Can be executed in parallel with R9 if helper extraction lands first.
 - Execution result: Completed 2026-03-27. Added focused regression tests for persistent worker exception wrapping (`test_persistent_agent_wraps_worker_exception_as_hermes_client_error`), shim logger proxy behavior in direct transport cleanup (`test_shim_logger_proxy_is_used_by_direct_module_cleanup`), and shim subprocess replacement contract (`test_shim_subprocess_proxy_supports_module_replacement`).

- [x] R11: Split `server.py` app bootstrap/composition into focused modules
 - Status: done
 - Severity: High
 - Scope/files: `server.py`, `app_factory.py`, new helper module(s) (e.g., `server_bootstrap.py`, `startup_diagnostics.py`), related route wiring tests.
 - Why it matters: `server.py` is ~545 lines and currently mixes env/config constants, auth/session wrappers, startup diagnostics assembly, Flask hook registration, static/public handlers, and route wiring. This monolith increases blast radius for changes and makes behavior-preserving review difficult.
 - Proposed change: Extract (1) startup diagnostics payload/logging, (2) auth/request helper adapters, and (3) blueprint registration/bootstrap orchestration into dedicated modules with explicit data contracts; keep `create_app()`/entrypoint stable.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_routes_auth.py`
   - `.venv/bin/python -m pytest -q tests/test_config.py`
 - Notes/dependencies: Preserve dynamic getter/lambda injection pattern already used for monkeypatch-safe route tests.
 - Execution result: Completed 2026-03-28. Extracted startup diagnostics into `server_startup.py` and public-route registration into `server_public_routes.py`; `server.py` now delegates both concerns while keeping `create_app()` and route wiring entrypoints stable.

- [x] R12: Decompose `hermes_client.py` into config/bootstrap vs transport orchestration
 - Status: done
 - Severity: Medium
 - Scope/files: `hermes_client.py`, potential new modules (e.g., `hermes_client_config.py`, `hermes_client_diagnostics.py`), `tests/test_hermes_client.py`.
 - Why it matters: `hermes_client.py` (~526 lines) combines env/config parsing, auth/config file parsing, diagnostics/health reporting, and runtime transport routing/fallback logic. This coupling makes transport changes risky and obscures regression boundaries.
 - Proposed change: Isolate pure configuration + diagnostics loading into dedicated helpers/dataclasses and leave `HermesClient.stream_events()` transport decision tree as a narrower orchestration layer.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Add focused tests for config-loader helpers independent from transport mocks.
 - Notes/dependencies: Coordinate with existing split modules (`hermes_client_agent_direct.py`, `hermes_client_agent_persistent.py`) to avoid reintroducing shim drift.
 - Execution result: Completed 2026-03-28. Added `hermes_client_bootstrap.py` and routed `HermesClient` config/bootstrap methods (`_resolve_agent_routing`, `_load_*_from_config`, `_load_active_provider_from_auth_store`) through the extracted bootstrap component; follow-up cleanup removed redundant in-class config/bootstrap bodies while preserving logging/diagnostics semantics and reduced `hermes_client.py` to 354 lines.

- [x] R13: Break `JobRuntime` monolith into worker lifecycle + event bus components
 - Status: done
 - Severity: Medium
 - Scope/files: `job_runtime.py`, potential internal helpers (event pub/sub, retry/dead-letter handling, keepalive/touch logic), `tests/test_routes_jobs_runtime.py`.
 - Why it matters: `job_runtime.py` (~746 lines) mixes event queue management, watchdog/worker loops, stream consumption, retry/dead-letter semantics, truncation/chunking, and telemetry counters. The current shape raises defect risk when touching unrelated paths.
 - Proposed change: Extract cohesive units (e.g., event history broker, job execution runner, retry/dead-letter policy helper) behind small private interfaces while preserving `JobRuntime` public API.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_store.py`
   - Add targeted tests for extracted helper boundaries (event pruning + retry/dead transitions).
 - Notes/dependencies: Execute after R11/R12 to keep operational startup/routing stable while runtime internals are split.
 - Execution result: Completed 2026-03-28. Extracted event history/pub-sub/pruning into `job_runtime_events.py` (`JobEventBroker`) and updated `JobRuntime` to delegate event responsibilities through the broker while preserving compatibility shim attributes (`_event_lock`, `_event_history`, `_event_queues`, `_event_timestamps`); follow-up cleanup extracted chat-job stream execution into `job_runtime_chat_job.py` (`execute_chat_job`) and reduced `job_runtime.py` to 509 lines while preserving `run_chat_job()` API behavior.

- [x] R14: Decompose `store_jobs.py` into lifecycle-focused helpers (claim/retry/dead-letter/queries)
 - Status: done
 - Severity: High
 - Scope/files: `store_jobs.py`, potential new modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`), `tests/test_store.py`, `tests/test_routes_jobs_runtime.py`.
 - Why it matters: `store_jobs.py` is still 512 lines and mixes job claiming, retry backoff policy, dead-letter writes, stale cleanup, and API/state projection payload shaping. This coupling raises regression risk in a reliability-critical path.
 - Proposed change: Extract cohesive SQL clusters into private helpers/modules with explicit call contracts while preserving `SessionStore` public methods and transaction boundaries.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py`
   - Add focused tests for each extracted helper path: claim race/no-row update, retry scheduling vs dead-letter, stale cleanup reason mapping.
 - Notes/dependencies: Keep SQL semantics behavior-identical; do not alter retry timing policy in this pass.
 - Execution result: Completed 2026-03-28. Split store-job lifecycle SQL into helper modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`) and reduced `store_jobs.py` to orchestration wrappers + dead-letter insert primitive. Added stale cleanup reason regression `test_cleanup_stale_jobs_thread_missing_reason_mapping`; validated store/runtime job suites.

- [x] R15: Decompose `store_chats.py` into read/query vs mutating/cancellation responsibilities
 - Status: done
 - Severity: Medium
 - Scope/files: `store_chats.py`, potential helper modules (`store_chat_queries.py`, `store_chat_mutations.py`), `tests/test_store.py`, `tests/test_routes_chat.py`.
 - Why it matters: `store_chats.py` is 470 lines and currently combines hydration/query CTEs, mutation flows, archival/reopen, and chat-job cancellation side effects. This broad scope makes chat UX fixes riskier.
 - Proposed change: Isolate read models/query builders from mutation flows and extract shared cancellation/dead-letter side effects into dedicated internal helpers.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_chat.py`
  - Add focused regressions for `clear_chat`/`remove_chat` cancellation behavior and active-chat fallback invariants.
- Notes/dependencies: Coordinate with R14 so chat/job side effects remain single-sourced.
 - Execution result: Completed 2026-03-28. Extracted read/query and mutation/cancellation logic into `store_chat_queries.py` and `store_chat_mutations.py`, keeping `store_chats.py` as orchestration mixin surface. Added cancellation regression coverage (`test_clear_chat_cancels_open_jobs`) and validated with `pytest -q tests/test_store.py tests/test_routes_chat.py`.

- [x] R16: Finish `server.py` composition split by extracting request/auth adapter wiring
 - Status: done
 - Severity: Medium
 - Scope/files: `server.py`, new adapter module(s) (e.g., `server_request_adapters.py`), route registration tests.
 - Why it matters: `server.py` is still 460 lines and retains many thin wrappers (`_verify_*`, `_json_*`, `_sse_*`, token/session adapters) plus middleware/hook composition. This remains a high-blast-radius integration point.
 - Proposed change: Move adapter wrappers and request-context glue into dedicated helper module(s), leaving `server.py` as minimal bootstrap + blueprint assembly.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_chat.py tests/test_routes_jobs_runtime.py`
  - `.venv/bin/python -m pytest -q tests/test_config.py`
- Notes/dependencies: Preserve dynamic getter/lambda injection conventions for monkeypatch-based tests.
 - Execution result: Completed 2026-03-28. Added `server_request_adapters.py` and moved request/auth adapter wiring out of `server.py` while preserving monkeypatch-friendly dynamic call resolution (auth verification wrappers + lambda-based route/template hooks). Verified targeted suites: `pytest -q tests/test_routes_auth.py tests/test_routes_meta.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py tests/test_store.py` (all passing).

- [x] R17: Add direct contract tests for newly extracted modules to prevent hidden drift
 - Status: done
 - Severity: High
 - Scope/files: `tests/test_hermes_client_bootstrap.py` (new), `tests/test_job_runtime_events.py` (new), `tests/test_job_runtime_chat_job.py` (new), `tests/test_server_startup.py` (new), `tests/test_server_public_routes.py` (new).
 - Why it matters: Current coverage still primarily exercises these extractions indirectly via larger integration tests. There are currently no direct module-level tests referencing `hermes_client_bootstrap`, `job_runtime_events`, `job_runtime_chat_job`, `server_startup`, or `server_public_routes`, increasing silent drift risk.
 - Proposed change: Add focused contract tests around each extracted module’s API behavior (inputs/outputs, edge paths, monkeypatch contracts) so future refactors fail fast.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client_bootstrap.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py tests/test_server_startup.py tests/test_server_public_routes.py`
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py`
 - Notes/dependencies: Can run in parallel with R14/R15/R16 since this is additive coverage.
 - Execution result: Completed 2026-03-28. Added five direct module-level suites with focused contract assertions across bootstrap routing/config parsing, event broker replay/pruning/terminal delivery semantics, extracted chat-job execution behavior, startup diagnostics payload/log invariants, and public-route registration behavior.

- [x] R18: Introduce shared job-status/domain constants to reduce cross-module string drift
 - Status: done
 - Severity: Medium
 - Scope/files: `store_jobs.py`, `store_chats.py`, `job_runtime.py`, `routes_chat_stream.py`, `routes_jobs_runtime.py`, potential new `job_status.py` constants module, related tests.
 - Why it matters: Status domains (`queued/running/done/error/dead`) and open-job predicates are repeated across store/runtime/routes with inline string literals and slightly different predicates, increasing future drift risk.
 - Proposed change: Centralize status constants and common predicates (open/terminal/retryable sets) in one shared module and migrate call sites incrementally.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py`
  - Add focused tests asserting shared constants drive open/terminal checks consistently.
- Notes/dependencies: Keep SQL text readable; avoid over-abstracting query clauses.
 - Execution result: Completed 2026-03-28. Added shared status/event domain module `job_status.py` (status constants, open/terminal sets, SQL status-list helpers, and terminal event constants) and migrated store/runtime/route call sites (`store_jobs*.py`, `store_chat_mutations.py`, `job_runtime.py`, `routes_chat_stream.py`, `routes_jobs_runtime.py`) off ad-hoc literals while preserving response contracts. Validation: `pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py tests/test_job_runtime_chat_job.py tests/test_job_runtime_events.py` and full `pytest -q` (185 passed).



- [x] R19: Extract keyboard/navigation/focus-control helpers out of `static/app.js`
 - Status: done
 - Severity: Medium
 - Scope/files: `static/app.js`, `static/keyboard_shortcuts_helpers.js`, `templates/app.html`, `server_public_routes.py`, `server.py`, `tests/keyboard_shortcuts_helpers.test.mjs`, `tests/test_routes_meta.py`
 - Why it matters: `static/app.js` still carried a large high-churn block for desktop keyboard shortcuts and sticky-focus cleanup, increasing blast radius and making targeted regressions harder to isolate.
 - Proposed change: Move keyboard/focus handlers into a dedicated helper module and keep app-level wrappers thin/behavior-preserving.
 - Validation steps:
   - `node --test tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_routes_auth.py`
 - Notes/dependencies: Keep helper loaded before `app.js`; maintain no-store cache policy parity with other helper modules.
 - Execution result: Completed 2026-03-28. Added `HermesMiniappKeyboardShortcuts` helper module, rewired app delegates, added helper static asset template/version wiring, and validated Python + Node test suites.

- [x] R20: Wire helper-module asset versioning/cache behavior for keyboard split
 - Status: done
 - Severity: Low
 - Scope/files: `server_public_routes.py`, `templates/app.html`, `server.py`, `tests/test_routes_meta.py`
 - Why it matters: New helper modules must preserve asset version pinning and no-store semantics to avoid stale browser behavior during rapid refactor loops.
 - Proposed change: Add explicit template version key and no-store header coverage for `keyboard_shortcuts_helpers.js`.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py`
 - Execution result: Completed 2026-03-28. Added template/version injection + cache policy regression coverage for the new helper asset.

- [x] R21: Expand helper-split contract assertions for frontend module extraction
 - Status: done
 - Severity: Low
 - Scope/files: `tests/test_routes_meta.py`
 - Why it matters: Module split safety depends on route/template script ordering and expected global export contracts; these regress silently without direct assertions.
 - Proposed change: Extend route-meta assertions to include the newly extracted keyboard helper module in script-order checks and split-module contract coverage.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py`
 - Execution result: Completed 2026-03-28. Route/meta tests now assert keyboard helper script presence/order and split-module wiring expectations.

- [x] R22: Continue `static/app.js` decomposition (remaining keyboard+interaction orchestration tranche)
 - Status: done
 - Severity: Medium
 - Scope/files: `static/app.js`, `static/interaction_helpers.js`, `templates/app.html`, `server_public_routes.py`, `server.py`, `tests/interaction_helpers.test.mjs`, `tests/test_routes_meta.py`.
 - Why it matters: `static/app.js` remains a large integration surface; completing decomposition lowers blast radius and increases direct contract-testability of interaction logic.
 - Proposed change: Extract the next cohesive interaction tranche into helper module(s), keep app wrappers stable, and preserve behavior contracts.
 - Validation steps:
   - `node --test tests/interaction_helpers.test.mjs tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_routes_auth.py`
 - Notes/dependencies: Maintain script-load order (`shared/runtime/helpers` before `app.js`) and no-store policy for newly split helper assets.
 - Execution result: Completed 2026-03-28. Added `HermesMiniappInteraction` helper module exposing `handleComposerSubmitShortcut` and `createSelectionQuoteController`; rewired `app.js` to use delegate wrappers while preserving existing scheduling/cancellation behavior. Added template/version injection and no-store route coverage for `interaction_helpers.js` and expanded route/meta contract checks. Validation: Node helper suite passed (`38` tests) and Python route/meta/auth suite passed (`39` tests).

- [x] R23: Run refactor-regression verification sweep for helper-module extraction batch
 - Status: done
 - Severity: Low
 - Scope/files: frontend helper tests + route/meta regression coverage
 - Why it matters: Incremental helper extraction needs repeated cross-layer checks to ensure browser/runtime behavior does not drift.
 - Proposed change: Run Python route/meta and Node helper suites together after each extraction batch and record evidence in plan.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_meta.py`
   - `node --test tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
 - Execution result: Completed 2026-03-28. Verification sweep passed (`38` Python tests and `33` Node tests).

- [x] R24: Remove secret-bearing dev-auth bootstrap and redact sensitive request logging
 - Status: done
 - Severity: High
 - Scope/files: `static/app.js`, `templates/app.html`, `routes_auth.py`, `server.py` or request-logging helper, service `.env` docs/tests.
 - Why it matters: Desktop QA currently auto-signs in via `?dev_secret=...` query params. Live server logs captured the full query string and even TelegramBot preview traffic requesting `/app?dev_sec...`, which leaks the dev secret into logs, URL previews, browser history, and potentially analytics/proxy layers.
 - Proposed change: Replace query-string secret bootstrap with a safer desktop-testing flow (e.g. explicit modal/prompt only, one-time short-lived bootstrap token, or fragment-based client-only secret handoff) and redact sensitive query params from request logs before they hit journal output.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_meta.py`
   - Browser smoke: open `/app` outside Telegram and verify desktop sign-in works without secrets appearing in logged request paths.
   - Log assertion: request logs must redact `dev_secret`/future sensitive params.
 - Notes/dependencies: Highest-priority hardening item now that browser QA is unlocked; keep dev bypass feature but remove passive secret exposure.
 - Execution result: Completed 2026-03-28. Removed query-string dev-auth bootstrap from `static/app.js`, replaced prompt-only sign-in with a dedicated in-app `dev-auth-modal`, and added `sanitized_request_target()` for request-debug redaction of sensitive query params. Live browser verification confirmed desktop sign-in still works via the modal without secrets appearing in request paths.

- [ ] R25: Split remaining `static/app.js` monolith into behavior-scoped modules
 - Status: in_progress
 - Severity: High
 - Scope/files: `static/app.js` (~3560 LOC), new helper modules under `static/`, `templates/app.html`, route/meta asset tests, Node helper tests.
 - Why it matters: `static/app.js` still carries bootstrap/auth, chat hydration, stream send/resume orchestration, modal state, skin sync, drafts, tab rendering glue, and browser interaction wiring in one file. Recent regressions (SSE framing visibility, reconnect latency flash, dev-auth bootstrap) were harder to isolate because multiple subsystems still cohabit the same file.
 - Proposed change: Extract the remaining monolith into focused modules with stable namespaces/contracts, likely: `(1) bootstrap/auth/dev-auth`, `(2) chat/history hydration + tabs`, `(3) stream orchestration + latency/reconnect`, `(4) modal/admin actions`. Keep `app.js` as a thin composition/wiring layer only.
 - Validation steps:
   - `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/shared_utils.test.mjs`
   - Add direct Node tests for each newly extracted helper module.
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py`
 - Notes/dependencies: Do this in small batches; preserve current browser-verified behavior after each slice.
 - Progress note (2026-03-28): Extracted desktop auth/bootstrap concerns into new `static/bootstrap_auth_helpers.js`, wired it into `templates/app.html` + `server_public_routes.py`, added direct Node tests (`tests/bootstrap_auth_helpers.test.mjs`), and verified the live app still opens the dev-sign-in modal correctly.
 - Progress note (2026-03-28, follow-up): Extracted chat/history hydration and warm-cache orchestration into new `static/chat_history_helpers.js`, delegated `loadChatHistory`, `hydrateChatFromServer`, `openChat`, `prefetchChatHistory`, and `warmChatHistoryCache` from `app.js`, added direct Node tests (`tests/chat_history_helpers.test.mjs`), and verified the live app still opens the dev-sign-in modal after the script-order expansion.
 - Progress note (2026-03-29): Extracted modal/admin/chat-management orchestration into new `static/chat_admin_helpers.js`, delegated chat title modal/tag logic plus `createChat`, `renameActiveChat`, `removeActiveChat`, `openPinnedChat`, `toggleActiveChatPin`, and settings modal open/close from `app.js`, added direct Node tests (`tests/chat_admin_helpers.test.mjs`), and verified the live app still opens both the dev-sign-in and settings dialogs after the new script-order expansion. Remaining slice: residual fullscreen/close/top-bar helpers and any small UI utility glue still embedded in `app.js`.
 - Progress note (2026-03-29, follow-up 2): Extracted top-bar/shell control logic into new `static/shell_ui_helpers.js`, delegated closing-confirmation sync, Telegram chrome skin updates, fullscreen button state/toggle, and close-app handling from `app.js`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/shell_ui_helpers.test.mjs`). Validation: `node --test tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
 - Progress note (2026-03-29, follow-up 3): Extracted composer/keyboard viewport utility logic into new `static/composer_viewport_helpers.js`, delegated `ensureComposerVisible`, `dismissKeyboard`, `installTapToDismissKeyboard`, and `installKeyboardViewportSync` from `app.js`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/composer_viewport_helpers.test.mjs`). Validation: `node --test tests/composer_viewport_helpers.test.mjs tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
 - Progress note (2026-03-29, follow-up 4): Extracted visibility/skin/dev-lifecycle glue into new `static/visibility_skin_helpers.js`, delegated skin normalize/store/apply sync (`normalizeSkin`, `getStoredSkin`, `setSkin`, `syncSkinFromStorage`), visibility reconciliation (`syncVisibleActiveChat`, `handleVisibilityChange`), and dev auto-reload polling (`shouldApplyDevReload`, `startDevAutoRefresh`) from `app.js`, moved visibility/focus/storage/broadcast listener wiring behind `installVisibilitySkinLifecycle()`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/visibility_skin_helpers.test.mjs`). Validation: `node --test tests/visibility_skin_helpers.test.mjs tests/composer_viewport_helpers.test.mjs tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
- Progress note (2026-03-29, follow-up 7): Moved viewport-mutation stability helpers (`runAfterUiMutation`, `preserveViewportDuringUiMutation`) from `static/app.js` into `static/composer_viewport_helpers.js` so keyboard/composer viewport orchestration owns both visibility and mutation phases. `app.js` now delegates through `composerViewportController` with dynamic getters for active chat + scroll bookkeeping state. Added direct Node assertions for RAF/timeout mutation scheduling and pinned-bottom viewport preservation behavior while keeping route/meta wiring unchanged. Validation: `node --check static/composer_viewport_helpers.js && node --check static/app.js && node --test tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k composer_viewport_helpers`.
- Progress note (2026-03-29, follow-up 8): Extracted latency chip orchestration from `static/app.js` into `runtime_helpers.createLatencyController(...)` and rewired `setChatLatency`/`syncActiveLatencyChip` to delegate via `latencyViewController` with injected dependencies (`getActiveChatId`, `setActivityChip`, `preserveViewportDuringUiMutation`, `latencyChip`, `streamDebugLog`). Added direct runtime helper tests covering active-chat latency updates, viewport-preserving chip mutation wrapping, and placeholder restore behavior when no active chat is selected. Validation: `node --check static/runtime_helpers.js && node --check static/app.js && node --test tests/frontend_runtime.test.mjs tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k "runtime_helpers or composer_viewport_helpers or app_js"`.
- Progress note (2026-03-29, follow-up 9): Extracted stream/activity chip status transitions from `static/app.js` into `runtime_helpers.createStreamActivityController(...)`, delegated `syncActivePendingStatus` and send/resume status transitions (`markStreamActive`, `markStreamError`, `markNetworkFailure`, `markStreamReconnecting`, `markResumeAlreadyComplete`, `markReconnectFailed`) through `streamActivityController`, and preserved existing active-chat gating + latency sync behavior by injecting dynamic getters and existing chip/status mutators. Added direct runtime-helper tests for pending/idle transitions, active stream chip updates, reconnect/complete/failure behavior, and active-chat guards. Validation: `node --check static/runtime_helpers.js && node --check static/app.js && node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k "runtime_helpers or app_js"`.
- Progress note (2026-03-29, follow-up 10): Extracted chat-list + pinned-collapse orchestration from `static/app.js` into `static/chat_tabs_helpers.js` (`normalizeChat`, `upsertChat`, `syncPinnedChats`, `syncChats`, pinned-collapse persistence/UI sync/toggle/auto-collapse, pin-button label sync), rewired `app.js` through `chatTabsController` delegates, and wired helper asset ordering/version/no-store coverage in route/template tests.
- Progress note (2026-03-29, follow-up 11): Extended `static/composer_state_helpers.js` with `createDraftController(...)` and delegated draft-storage lifecycle (`loadDraftsFromStorage`, `persistDraftsToStorage`, `setDraft`, `getDraft`) from `app.js` to the controller. Added direct Node coverage in `tests/composer_state_helpers.test.mjs` and reran helper + route meta/public route validations.
- Progress note (2026-03-29, follow-up 12): Extended `static/stream_controller.js` with `createToolTraceController(...)` and delegated inline tool-trace lifecycle (`resetToolStream`, `findPendingToolTraceMessage`, `ensurePendingToolTraceMessage`, `appendInlineToolTrace`, `finalizeInlineToolTrace`) from `app.js` through helper-managed wrappers. Added direct Node coverage in `tests/stream_controller.test.mjs` and refreshed hardening guard assertions to tolerate helper ownership moves.
- Progress note (2026-03-29, follow-up 13): Extended `static/runtime_helpers.js` with `createHapticUnreadController(...)` + `latestCompletedAssistantHapticKey(...)` and delegated haptic/unread lifecycle (`latestCompletedAssistantHapticKey`, `triggerIncomingMessageHaptic`, `incrementUnread`) from `app.js` to controller wrappers. Added runtime-helper Node coverage for haptic dedupe + unread increments (active/hidden gating) and updated streaming hardening guard coverage for helper export ownership.
- Progress note (2026-03-29, follow-up 14): Extended `static/interaction_helpers.js` with quote text utility ownership (`unwrapLegacyQuoteBlock`, `normalizeQuoteSelection`, `splitGraphemes`, `wrapQuoteLine`, `formatQuoteBlock`) and rewired matching `app.js` wrappers to thin delegates. Added direct helper tests in `tests/interaction_helpers.test.mjs` plus route-meta guard updates in `tests/test_routes_meta.py` so quote-format internals can continue moving out of `app.js` without brittle false failures. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 15): Extended `static/interaction_helpers.js` with selection-quote/input utility ownership (`getQuoteWrapWidth`, `isCoarsePointer`, `applyQuoteIntoPrompt`, `activeSelectionQuote`, `quotePlacementKey`, `showSelectionQuoteAction`, `syncSelectionQuoteAction`) and rewired the corresponding `app.js` functions into thin delegates. Added direct helper tests for quote-width estimation bounds, coarse-pointer detection, and prompt quote insertion/caret sync, and updated route-meta ownership assertions for selection quote positioning internals. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 16): Extended `static/interaction_helpers.js` with selection-quote timer/reset helper ownership (`clearSelectionQuoteState`, `cancelSelectionQuoteTimer`, `scheduleSelectionQuoteClear`, `scheduleSelectionQuoteSync`) and rewired matching `app.js` wrappers to delegate through helper APIs with injected callbacks/state. Added direct helper tests for state reset/hide behavior, timer cancellation delegation, clear-on-empty-selection scheduling, and sync reschedule semantics. Updated route-meta coverage to assert helper-side ownership for the extracted timer/reset functions while preserving existing app-side delegate/wiring guards. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 17): Extended `static/render_trace_helpers.js` with rendering ownership (`renderBody`, `renderToolTraceBody`, `roleLabelForMessage`) and rewired matching `app.js` functions to thin delegates with injected formatting/DOM dependencies (`cleanDisplayText`, `escapeHtml`, `document`, `operatorDisplayName`). Added direct helper tests in `tests/render_trace_helpers.test.mjs` for fenced/non-fenced body rendering, tool-trace DOM toggle behavior, and role-label mapping; updated `tests/test_routes_meta.py` assertions to track helper ownership and app delegation for the extracted render functions. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 18): Extended `static/render_trace_helpers.js` with message card render ownership (`messageVariantForRole`, `shouldSkipMessageRender`, `applyMessageMeta`, `renderMessageContent`) and rewired matching `app.js` wrappers into thin delegates with injected dependencies/callbacks (`formatMessageTime`, `roleLabelForMessage`, `renderBody`, `renderToolTraceBody`). Added direct helper tests in `tests/render_trace_helpers.test.mjs` for role/body-skip mapping, metadata application (role/pending class + timestamp), and tool-vs-body renderer dispatch; updated `tests/test_routes_meta.py` assertions to track helper ownership and app-side delegation for these extracted message-node helpers. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, combined follow-ups 19-21): Extended `static/render_trace_helpers.js` with message-node orchestration ownership (`messageStableKey`, `messageStableKeyForPendingState`, `upsertMessageNode`, `createMessageNode`, `appendMessages`, `findMessageNodeByKey`) and rewired matching `app.js` wrappers into thin delegates with injected rendering/template dependencies. Added direct helper tests in `tests/render_trace_helpers.test.mjs` for stable key generation, upsert pipeline composition, node creation/dataset behavior, append-skip semantics, and selector-based key lookup fallback; updated `tests/test_routes_meta.py` ownership/delegation assertions for all newly extracted functions. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, combined follow-ups 22-24): Extended `static/render_trace_helpers.js` with history-target lookup + visible stream patch ownership (`findLatestHistoryMessageByRole`, `findLatestAssistantHistoryMessage`, `patchVisiblePendingAssistant`, `patchVisibleToolTrace`) and rewired matching `app.js` wrappers into thin delegates with injected phase guards, key lookup, logging, and render/viewport dependencies. Added direct helper tests in `tests/render_trace_helpers.test.mjs` for latest target derivation contracts, assistant visible pending-body patch behavior, and tool trace body/time patch behavior (including no-target success path); updated `tests/test_routes_meta.py` ownership/delegation assertions for all newly extracted helpers. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 25): Extended `static/chat_history_helpers.js` with chat-history mutation and active-view scheduling ownership (`addLocalMessage`, `updatePendingAssistant`, `syncActiveMessageView`, `scheduleActiveMessageView`) and rewired matching `static/app.js` wrappers into thin delegates through `chatHistoryController`. Injected `runAfterUiMutation` into chat-history controller creation (with timeout fallback inside helper) to preserve deferred active-chat rerender semantics while removing `activeRenderScheduled`/`activeRenderChatId` bookkeeping from `app.js`. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for pending assistant mutation/finalize behavior and active-chat-only sync/scheduled rerenders, and updated `tests/test_routes_meta.py` assertions for helper ownership + app-side delegation. Validation: `node --check static/chat_history_helpers.js && node --check static/app.js && node --test tests/chat_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 26): Extended `static/chat_history_helpers.js` with read-sync gating ownership (`maybeMarkRead`) and rewired `static/app.js` `maybeMarkRead(...)` into a thin controller delegate. Injected read-gating dependencies into chat-history controller creation (`getIsAuthenticated`, `isNearBottomFn`, `messagesContainer`, `unseenStreamChats`, `markReadInFlight`, `markReadFn`) so active-chat/auth/near-bottom/unread checks and in-flight dedupe live in the helper while preserving behavior. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for auth/active/visibility/unread gating and forced in-flight dedupe behavior, and updated `tests/test_routes_meta.py` ownership/delegation assertions for helper + app delegate wiring. Validation: `node --check static/chat_history_helpers.js && node --check static/app.js && node --test tests/chat_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.

- [x] R26: Finish stream lifecycle extraction and contract-test the send/resume/hydration state machine
 - Status: done
 - Severity: High
 - Scope/files: `static/app.js`, `static/stream_controller.js`, `static/stream_state_helpers.js`, `tests/stream_controller.test.mjs`, `tests/frontend_runtime.test.mjs`, possibly new dedicated stream-lifecycle test module.
 - Why it matters: The live tool-stream bug was ultimately a protocol/framing issue, but debugging also showed lifecycle complexity spread across `app.js` and `stream_controller.js`: initial send, early-close suppression, resume recursion, 409 terminalization races, latency lifecycle, and history reconciliation all span multiple helpers. This is still the highest-blast-radius UI subsystem.
 - Proposed change: Move remaining stream send/resume/finalize orchestration behind one explicit controller/state-machine API with direct contract tests for live tool events, early close, resume-409 graceful completion, hydration merge, and latency preservation during reconnect.
 - Validation steps:
   - `node --test tests/stream_controller.test.mjs tests/frontend_runtime.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_streaming_hardening_guards.py`
   - Live browser smoke against `app.cronpulse.app` with dev auth and a tool-heavy prompt.
 - Notes/dependencies: Pair naturally with R25 but can start earlier as a targeted hardening pass.
 - Execution result: Completed 2026-03-28. Moved `hydrateChatAfterGracefulResumeCompletion(...)` and `consumeStreamWithReconnect(...)` into `static/stream_controller.js`, wired `app.js` through controller delegates, and added direct Node contract tests (`tests/stream_controller.test.mjs`) plus guard coverage in `tests/test_streaming_hardening_guards.py` / `tests/test_routes_meta.py`. Browser smoke confirmed the desktop entry path still works after the consolidation.

- [ ] R27: Reduce production stream/debug logging noise and sensitive payload exposure
 - Status: todo
 - Severity: Medium
 - Scope/files: `.env`, `miniapp_config.py`, `job_runtime.py`, `routes_chat_stream.py`, frontend debug wiring/tests/docs.
 - Why it matters: The live environment currently runs with `MINIAPP_REQUEST_DEBUG=1` and `MINI_APP_STREAM_TIMING_DEBUG=1`, and browser/server traces include chunk previews, timing payloads, and request metadata. That was useful for current forensics, but it increases log volume and can expose prompt/tool details longer than necessary.
 - Proposed change: Introduce a safer operator-debug mode with explicit temporary enablement, bounded preview lengths/redaction rules, and a documented default-off production posture while preserving lightweight non-sensitive health telemetry.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_routes_jobs_runtime.py`
   - Manual log inspection before/after toggling debug envs.
 - Notes/dependencies: Coordinate with R24 so secret/log redaction and debug policy land together.

- [ ] R28: Decompose oversized test monoliths and add browser-regression fixtures for live-risk paths
 - Status: todo
 - Severity: Medium
 - Scope/files: `tests/test_routes_meta.py` (498 LOC), `tests/test_routes_chat.py` (406 LOC), `tests/test_hermes_client.py` (1087 LOC), shared fixtures/util helpers under `tests/`.
 - Why it matters: Core regression suites are themselves becoming hard to reason about. The browser-derived regressions we just fixed (SSE literal newline framing, reconnect latency behavior, dev-auth desktop path) are mixed into already-large files, which raises maintenance cost and makes missing coverage harder to spot.
 - Proposed change: Split test monoliths by concern (auth/meta, stream contracts, browser-bootstrap guards, Hermes client direct vs persistent transport) and add reusable fixtures/utilities for dev-auth/browser-flow assertions.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_server_public_routes.py`
   - `node --test tests/*.test.mjs` (or targeted helper suites after split)
 - Notes/dependencies: Lower risk than product-code refactors and will make future regression work faster.

## Evidence from execution + refresh passes
- `git status --short --branch`:
  - `## main...origin/main`
  - tracked modifications in runtime/store/routes/tests and untracked docs/new modules.
- Initial baseline signal before execution:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_broad_exception_policy.py`
  - `42 passed, 1 failed`
  - failure: `tests/test_broad_exception_policy.py::test_broad_exception_handlers_require_policy_and_observability`
  - violations: `job_runtime.py` lines 221 and 229 missing `broad-except-policy:` justification.
- Execution validations (controller session, 2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py` → `1 passed in 0.03s`
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py` → `26 passed in 1.52s`
  - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py` → `18 passed in 1.62s`
  - `.venv/bin/python -m pytest -q tests/test_store.py` → `27 passed in 0.17s`
  - `.venv/bin/python -m pytest -q tests/test_config.py` → `7 passed in 0.02s`
  - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_store.py tests/test_config.py` → `52 passed in 1.74s`
- Code audit signals addressed:
  - `hermes_client_agent.py` subprocess PIPE lifecycle now has explicit idempotent stdio close handling in teardown paths.
- Planning refresh evidence (2026-03-27 post-R7):
  - `git status --short --branch` → dirty working tree with split transport modules and runtime/store/route updates still in-flight.
  - `search_files("except Exception")` highlights persistent broad handlers and embedded child-runner broad catch in `_agent_runner_script()` (string code path not AST-checked by broad-exception policy gate).
  - `search_files("hermes_client_agent_(direct|persistent)", tests/)` indicates transport modules are mostly covered via top-level client tests, with limited direct module contract coverage.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `27 passed in 1.56s`.
- Execution validations for R8/R9 (2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "persistent and (timeout or watchdog or stream_events)"` → `3 passed, 26 deselected in 0.15s`.
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "tool_progress or stream_via_agent"` → `5 passed, 24 deselected in 1.70s`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `30 passed in 2.80s`.
- Execution validations for R10 (2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py` → `32 passed in 3.23s`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.18s`.
- Refactor-pass refresh evidence (monolith audit, 2026-03-27):
  - `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` shows top monoliths: `job_runtime.py` (746), `server.py` (545), `hermes_client.py` (526), `store_jobs.py` (512), `store_chats.py` (470).
  - `read_file` audit confirmed multi-concern mixing in `server.py` (bootstrap/hooks/routes), `hermes_client.py` (config/diagnostics + routing), and `job_runtime.py` (worker loop + event bus + policy).
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_hermes_client.py` → `81 passed in 5.40s`.
- Follow-up cleanup evidence (2026-03-28, hermes_client monolith reduction):
  - `wc -l hermes_client.py` → `354`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.18s`.
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_routes_auth.py` → `55 passed in 2.50s`.
  - `.venv/bin/python -m pytest -q tests/test_config.py tests/test_store.py` → `34 passed in 0.19s`.
- Follow-up cleanup evidence (2026-03-28, job_runtime monolith reduction):
  - `wc -l job_runtime.py job_runtime_chat_job.py` → `509 job_runtime.py`, `194 job_runtime_chat_job.py`.
  - `python -m py_compile job_runtime.py job_runtime_chat_job.py` (with `.venv` active) → success.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_routes_jobs_runtime.py` → `19 passed in 1.57s`.
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_auth.py` → `37 passed in 0.98s`.
  - `.venv/bin/python -m pytest -q tests/test_config.py tests/test_store.py` → `34 passed in 0.16s`.
- Full-project refactor/hardening assessment refresh (2026-03-28):
  - `git status --short --branch` confirms broad dirty tree still centered in server/runtime/store/routes/tests with newly extracted helper modules untracked.
  - `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` current top monoliths: `store_jobs.py` (512), `job_runtime.py` (509), `store_chats.py` (470), `server.py` (460), `hermes_client_agent_direct.py` (372).
  - `search_files("TODO|FIXME|XXX|HACK", "*.py")` → no placeholder debt markers in code.
  - `search_files("job_runtime_chat_job|job_runtime_events|server_startup|server_public_routes|hermes_client_bootstrap", path=tests/)` → no direct test references for newly extracted modules; added R17 to close this gap.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.22s`.
- Execution validations for R17 (2026-03-28):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client_bootstrap.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py tests/test_server_startup.py tests/test_server_public_routes.py` → `16 passed in 0.12s`.
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py` → `81 passed in 5.40s`.
- Execution validations for R14 (2026-03-28):
  - `python -m py_compile store_jobs.py store_jobs_claim.py store_jobs_retry.py store_jobs_queries.py` (with `.venv` active) → success.
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py` → `46 passed in 1.81s`.
  - Added targeted stale-cleanup reason regression: `test_cleanup_stale_jobs_thread_missing_reason_mapping`.
- Post-batch policy sanity check (2026-03-28):
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py` → `1 passed in 0.03s`.
- Live browser refactor audit (2026-03-28 late):
  - `git status --short --branch` → dirty working tree centered in stream/auth/server/frontend files with untracked local scratch demos.
  - Python hotspot scan: `job_runtime.py` (627), `server.py` (436), `routes_chat_stream.py` (308), `routes_auth.py` (180), `miniapp_config.py` (157).
  - Frontend hotspot scan: `static/app.js` (3562), `static/stream_controller.js` (410), helper modules all far smaller (`<= 299`).
  - Live browser QA against `https://app.cronpulse.app/app` now works via dev auth; console showed that after SSE framing fix, live `meta`/`tool`/`chunk`/`done` events parse correctly and render during pending state.
  - Service logs during that QA captured `GET /app?dev_secret=...` plus TelegramBot preview traffic to the same path, proving current query-string desktop bootstrap leaks secrets into logs and previews.
  - Production `.env` currently keeps `MINIAPP_REQUEST_DEBUG=1` and `MINI_APP_STREAM_TIMING_DEBUG=1`, which was useful for forensics but increases log volume and sensitive preview exposure.
