# Refactor Log — hermes-miniapp-v4

> Historical refactor archive for the Hermes Mini App.
> This file holds completed-item detail and refresh/execution evidence that used to live in the canonical plan.
> The active backlog now lives in `docs/maintainers/history/refactor-plan.hermes-miniapp-v4.md`.


## Execution 2026-04-23 16:30Z
- Ran `refactor-execute-pass` for R75 on a clean non-live worktree (`refactor/r75-fail-open-telemetry`) based on `origin/main`.
- R75: Hardened child-process telemetry so subprocess memory sampling is fail-open and spawn-tracker bookkeeping no longer holds `_spawn_tracker_lock` across `/proc` and host-memory reads. `SubprocessStreamLifecycle.maybe_emit_memory_sample(...)` now logs and suppresses telemetry-sampling failures, while `HermesClient.observe_child_process_sample(...)` and `observe_descendant_finish(...)` gather process snapshots outside the critical section and only reacquire the lock to append finalized event payloads.
- Added targeted regressions in `tests/test_job_runtime_worker_launcher.py` for failed memory sampling and in `tests/test_hermes_client.py` for the lock-scope contract around child/descendant snapshot collection.
- Validation evidence:
  - `python3 -m py_compile hermes_client.py job_runtime_worker_launcher_subprocess.py tests/test_hermes_client.py tests/test_job_runtime_worker_launcher.py` → passed
  - `python3 -m pytest -q tests/test_job_runtime_worker_launcher.py -k "memory_sample or descendant or subprocess"` → `26 passed, 4 deselected`
  - `python3 -m pytest -q tests/test_hermes_client.py -k "child_spawn or descendant or recall_health"` → `9 passed, 108 deselected`

## Execution 2026-04-23 15:20Z
- Ran `refactor-execute-pass` for R73, R74, and R77 on a clean non-live worktree (`refactor/r73-r74-r77`) based on `origin/main` because the live worktree was dirty with unrelated clusters.
- R73: Hardened subprocess register-failure cleanup in `job_runtime_worker_launcher.py` / `job_runtime_worker_launcher_subprocess.py` so `register_child_spawn(...)` failures stay inside the normal finalize path. Added direct regression coverage in `tests/test_job_runtime_worker_launcher.py` for SIGKILL/reap, stderr handle closure, and launcher diagnostic updates on register failure.
- R74: Hardened `job_runtime_chat_job.py` so post-`done` session eviction failures cannot skip `complete_job(...)`, while keeping eviction ahead of completion for non-warm sessions to avoid reopening a same-chat claim race. Added regression coverage in `tests/test_job_runtime_chat_job.py` for eviction failure after terminal `done` publication.
- R77: Centralized `removeActiveChat(...)` reconciliation in `static/chat_admin_helpers.js` with `reconcilePostRemoveRender(...)`, covering optimistic close, rollback, explicit no-active-chat, lightweight reopen, and authoritative server-active responses without duplicate production tab renders. Expanded `tests/chat_admin_helpers.test.mjs` coverage and aligned the harness with runtime meta-render behavior.
- Validation evidence:
  - `.venv/bin/python -m pytest -q tests/test_job_runtime_worker_launcher.py -k "register_failed or subprocess"` → `26 passed, 4 deselected`
  - `.venv/bin/python -m pytest -q tests/test_job_runtime_worker_launcher.py` → `30 passed`
  - `.venv/bin/python -m pytest -q tests/test_job_runtime_chat_job.py -k "evict_session or done"` → `5 passed, 20 deselected`
  - `.venv/bin/python -m pytest -q tests/test_job_runtime_chat_job.py` → `25 passed`
  - `node --test tests/chat_admin_helpers.test.mjs` → `38 passed`
- Mandatory review gate: independent review over the actual branch contents passed for R73, R74, and R77 after rework for the same-chat completion-order race and duplicate-tab-render concern.

## Refresh 2026-04-12 01:11Z
- Ran a fresh refactor-pass refresh against the active dirty worktree after closing R56-R57.
- Current git status at refresh time: `## main...origin/main` plus modified `docs/maintainers/history/refactor-plan.hermes-miniapp-v4.md`, `hermes_client_types.py`, `job_runtime_chat_job.py`, `job_runtime_worker_launcher.py`, `miniapp_config.py`, `routes_auth_service.py`, `routes_chat_management_service.py`, `server.py`, `static/app.js`, `static/chat_history_helpers.js`, `static/chat_tabs_helpers.js`, `static/composer_viewport_helpers.js`, `static/interaction_helpers.js`, `static/stream_controller.js`, `store_chat_queries.py`, `store_models.py`, `store_schema.py`, `templates/app.html`, `tests/bootstrap_auth_app_delegation.test.mjs`, `tests/chat_history_open_hydrate.test.mjs`, `tests/chat_history_read_mutation.test.mjs`, `tests/chat_history_test_harness.mjs`, `tests/chat_history_visibility_sync.test.mjs`, `tests/chat_tabs_app_delegation.test.mjs`, `tests/composer_viewport_helpers.test.mjs`, `tests/frontend_runtime.test.mjs`, `tests/interaction_app_delegation.test.mjs`, `tests/interaction_helpers.test.mjs`, `tests/stream_controller_policy_session.test.mjs`, `tests/template_startup_script_order.test.mjs`, `tests/test_job_runtime_chat_job.py`, `tests/test_job_runtime_worker_launcher.py`, `tests/test_routes_auth_service.py`, `tests/test_routes_chat.py`, `tests/test_routes_chat_management_service.py`, `tests/test_routes_meta.py`, plus untracked `tests/frontend_runtime_chat_tab_cycle.test.mjs`, `tests/frontend_runtime_history_render.test.mjs`, `tests/frontend_runtime_stream_activity.test.mjs`, `tests/frontend_runtime_test_harness.mjs`, `tests/frontend_runtime_unread_latency.test.mjs`, `tests/frontend_runtime_viewport_bootstrap.test.mjs`, and `tests/render_trace_history_app_delegation.test.mjs`.
- Hotspot snapshot:
  - Frontend: `static/app.js` 2982 LOC, `static/stream_controller.js` 1846 LOC, `static/chat_history_helpers.js` 1634 LOC, `static/chat_tabs_helpers.js` 962 LOC, `static/bootstrap_auth_helpers.js` 846 LOC, `static/interaction_helpers.js` 795 LOC.
  - Backend: `hermes_client.py` 2964 LOC, `hermes_client_types.py` 856 LOC, `job_runtime_worker_launcher.py` 835 LOC, `server.py` 682 LOC, `miniapp_config.py` 483 LOC, `store_chats.py` 468 LOC, `routes_chat_management_service.py` 463 LOC, `store_schema.py` 458 LOC.
  - Notable regrown functions/controllers: `createStreamSendController(...)` and `createStreamResumeController(...)` still dominate `static/stream_controller.js`; `createController(...)` plus hydration/read-sync bands dominate `static/chat_history_helpers.js`; `static/app.js` still contains large deferred-helper / controller-composition infrastructure at the top of the file.
- Validation evidence:
  - `node --check static/app.js static/chat_tabs_helpers.js static/chat_history_helpers.js static/interaction_helpers.js static/stream_controller.js static/composer_viewport_helpers.js tests/chat_tabs_app_delegation.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/composer_viewport_helpers.test.mjs tests/interaction_app_delegation.test.mjs tests/interaction_helpers.test.mjs tests/stream_controller_policy_session.test.mjs tests/frontend_runtime.test.mjs tests/frontend_runtime_test_harness.mjs tests/frontend_runtime_unread_latency.test.mjs tests/frontend_runtime_viewport_bootstrap.test.mjs tests/frontend_runtime_stream_activity.test.mjs tests/frontend_runtime_history_render.test.mjs tests/frontend_runtime_chat_tab_cycle.test.mjs tests/render_trace_history_app_delegation.test.mjs` (passed).
  - `source .venv/bin/activate && python -m py_compile hermes_client_types.py job_runtime_chat_job.py job_runtime_worker_launcher.py miniapp_config.py routes_auth_service.py routes_chat_management_service.py server.py store_chat_queries.py store_models.py store_schema.py tests/test_job_runtime_chat_job.py tests/test_job_runtime_worker_launcher.py tests/test_routes_auth_service.py tests/test_routes_chat.py tests/test_routes_chat_management_service.py tests/test_routes_meta.py` (passed).
  - `node --test tests/chat_tabs_app_delegation.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/composer_viewport_helpers.test.mjs tests/interaction_app_delegation.test.mjs tests/interaction_helpers.test.mjs tests/stream_controller_policy_session.test.mjs tests/frontend_runtime.test.mjs tests/frontend_runtime_unread_latency.test.mjs tests/frontend_runtime_viewport_bootstrap.test.mjs tests/frontend_runtime_stream_activity.test.mjs tests/frontend_runtime_history_render.test.mjs tests/frontend_runtime_chat_tab_cycle.test.mjs tests/render_trace_history_app_delegation.test.mjs` → `174 passed, 1 failed` (`tests/render_trace_history_app_delegation.test.mjs` expects an older inline composition string in `app.js`).
  - `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_chat_job.py tests/test_job_runtime_worker_launcher.py tests/test_routes_auth_service.py tests/test_routes_chat_management_service.py -k "worker_launcher or auth or unread or bootstrap or runtime or management or chat_job"` → `53 passed, 1 failed`; the remaining failure is still `tests/test_job_runtime_worker_launcher.py::test_runtime_run_chat_job_uses_configured_worker_launcher`, where `server.store.claim_next_job()` returns `None` before launcher behavior is exercised.
- Reopened canonical backlog items R58-R63 to track the fresh frontend composition regrowth (`app.js`, `chat_history_helpers.js`, `stream_controller.js`, `chat_tabs_helpers.js`), the regrown warm-session registry type module (`hermes_client_types.py`), and the new direct-contract drift surfaced by the failing history-render delegation test.

## Archived recently completed
- R48 (2026-04-11): Closed the remaining unread-notification cross-layer coverage gap without changing product behavior. Added focused tests in `tests/test_config_telegram_notifications.py` for `MINI_APP_TELEGRAM_NOTIFICATION_TIMEOUT_SECONDS`, added focused store/schema regressions in `tests/test_store_telegram_notifications.py` for `get/set_telegram_unread_notifications_enabled(...)` persistence plus legacy `user_preferences` migration coverage, and refreshed the direct notifier tests in `tests/test_miniapp_telegram_notifications.py` so they match the current active+visibly-open suppression contract. Validation: `source .venv/bin/activate && python -m py_compile miniapp_telegram_notifications.py miniapp_config.py store_chats.py store_schema.py tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py` (passed); `source .venv/bin/activate && python -m pytest -q tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py` (`10 passed`); and `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py -k "telegram_unread"` (`3 passed, 19 deselected`).
- R47 (2026-04-11): Split `static/startup_bindings_helpers.js` into focused internal controllers while preserving the exported `createController(...)` facade and existing `static/app.js` delegation surface. The helper now composes `createTranscriptBindingsController(...)` for transcript scroll/jump/tab bindings, `createActionBindingsController(...)` for async button wiring, `createStartupBootstrapController(...)` for binding checks + auth/bootstrap orchestration, `createShellModalController(...)` for shell/settings/dev-auth modal listeners, and `createPendingWatchdogController(...)` for the best-effort pending refresh loop. Added direct export coverage in `tests/startup_bindings_helpers.test.mjs` and kept the existing behavior/delegation suites green. Validation: `node --check static/startup_bindings_helpers.js tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs static/app.js` (passed); `node --test tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs` (`24 passed`); and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "startup_bindings or bootstrap"` (`5 passed, 63 deselected`).
- R45 (2026-04-10): Split `job_runtime_worker_launcher.py` subprocess launch/stream handling into focused helper bands while preserving `SubprocessJobWorkerLauncher` public behavior and diagnostics fields. The launcher now routes subprocess ownership through narrower helpers for spawn/config (`_child_env`, `_assert_spawn_allowed`, `_spawn_subprocess`, `_register_spawn`, `_write_payload`), event transport/decode (`_stdout_line_queue`, `_decode_subprocess_event`, `_iter_subprocess_events`, `_record_stream_event`), attach/terminal event handling (`_handle_child_spawn_event`, `_handle_attach_ready_event`, `_handle_worker_terminal_event`), and teardown/error mapping (`_emit_timeout_error`, `_wait_for_subprocess_exit`, `_emit_nonzero_exit_error`, `_finalize_subprocess_stream`), shrinking `_stream_events_via_subprocess(...)` to a 78-line orchestration method. Validation: `source .venv/bin/activate && python -m py_compile job_runtime_worker_launcher.py tests/test_job_runtime_worker_launcher.py tests/test_routes_jobs_runtime.py` (passed); `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_worker_launcher.py` (`1 failed, 24 passed`) with the remaining failure isolated to the pre-existing dirty-tree/open-job-claim test `test_runtime_run_chat_job_uses_configured_worker_launcher`; and `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_worker_launcher.py -k "not test_runtime_run_chat_job_uses_configured_worker_launcher"` (`23 passed, 1 deselected`).
- R43 (2026-04-10): Decomposed the old `tests/chat_history_helpers.test.mjs` monolith into focused suites aligned with the chat-history seams. Added `tests/chat_history_test_harness.mjs` for shared fixtures, split meta/deferred-tab-switch coverage into `tests/chat_history_meta_controller.test.mjs`, split history open/hydrate/resume coverage into `tests/chat_history_open_hydrate.test.mjs`, split unread/read-threshold plus local mutation coverage into `tests/chat_history_read_mutation.test.mjs`, and split visibility-driven active-sync coverage into `tests/chat_history_visibility_sync.test.mjs`. Kept the existing `tests/chat_history_app_delegation.test.mjs` wrapper contract coverage intact. Validation: `node --check static/chat_history_helpers.js tests/chat_history_test_harness.mjs tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (passed) and `node --test tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (`65 passed`).
- R42 (2026-04-10): Split `static/chat_history_helpers.js` into focused internal controllers while preserving the exported `createController(...)` and `createMetaController(...)` facade that `static/app.js` consumes. The module now composes `createReadSyncController(...)` for unread/read-threshold + mark-read behavior, `createLocalMutationController(...)` for local system-message/pending-assistant/message-view mutations, and `createHistoryOpenController(...)` for load/open/hydrate/refresh/visibility flows, while keeping `createMetaController(...)` focused on active-chat metadata/deferred tab-switch work. Validation: `node --check static/chat_history_helpers.js tests/chat_history_test_harness.mjs tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (passed), `node --test tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (`65 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py::test_chat_history_helpers_static_asset_is_no_store tests/test_routes_meta.py::test_desktop_dev_auth_bootstrap_guards_present` (`2 passed`).
- R41 (2026-04-10): Split `static/bootstrap_auth_helpers.js` into focused internal helper bands while preserving the existing `createController(...)` facade and `static/app.js` contract. The module now composes focused `createRequestController(...)`, `createDevAuthUiController(...)`, `createBootstrapApplyController(...)`, and `createDevAuthSignInController(...)` helpers around the existing identity/delay utilities, so auth-request transport, dev-auth UI/default persistence, bootstrap-apply orchestration, and dev-auth sign-in flow each have explicit ownership. Coverage now includes the helper facade regressions plus pinned-chat bootstrap hydration behavior without introducing any new static asset/script-order churn. Validation: `node --check static/bootstrap_auth_helpers.js tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs` (passed), `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs` (`19 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "bootstrap or auth"` (`9 passed, 59 deselected`).
- R40 (2026-04-10): Split `routes_auth.py` into thin route adapters plus a focused `routes_auth_service.py` auth/bootstrap service layer. `register_auth_routes(...)` now stays concentrated on request parsing, auth-cookie/skin-cookie response wiring, guard checks, and delegation, while `AuthBootstrapService` owns allow-empty parsing, auth-success payload shaping, pending-history augmentation from runtime checkpoints, active-chat/default-chat bootstrap state resolution, file-ref-enriched turn serialization, and dev-user construction. Added dedicated service regressions in `tests/test_routes_auth_service.py` alongside the existing route integration coverage so the new backend seam is directly guarded. Validation: `source .venv/bin/activate && python -m py_compile routes_auth.py routes_auth_service.py tests/test_routes_auth.py tests/test_routes_auth_service.py` (passed) and `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py tests/test_routes_auth_service.py` (`23 passed`).
- R46 (2026-04-10): Decomposed the regrown `hermes_client.py` attach/resume transport seam into focused helper bands while preserving the `HermesClient` public API. Added `_build_attach_handshake_result(...)` + `_probe_live_worker_attach_handshake(...)` so `_attempt_live_worker_attach_handshake(...)` shrank to a loop/controller over explicit result builders, added `_build_attach_resume_request_payload(...)` + `_connect_attach_resume_transport(...)` so `_attempt_live_worker_attach_resume(...)` now focuses on transport resolution + error mapping, added `_attempt_validated_warm_reuse(...)` to separate validation gating from attempt recording, and split `stream_events(...)` into `_stream_events_via_configured_remote_transport(...)` + `_stream_events_after_remote_transport(...)` so configured HTTP fallback/reporting is isolated from local warm-reuse launch flow. Current hotspot sizes after the pass: `stream_events(...)` 35 LOC, `_attempt_live_worker_attach_handshake(...)` 50 LOC, `_attempt_live_worker_attach_resume(...)` 64 LOC, `attempt_warm_reuse(...)` 48 LOC; file size is now `2964` LOC. Validation: `source .venv/bin/activate && python -m py_compile hermes_client.py tests/test_hermes_client.py tests/test_config.py`, `source .venv/bin/activate && python -m pytest -q tests/test_hermes_client.py -k "warm_reuse or attach or runtime_status_reports_recall_health or stream_events"` (42 passed, 64 deselected), and `source .venv/bin/activate && python -m pytest -q tests/test_config.py -k "persistent_runtime_ownership or warm_reuse"` (1 passed, 18 deselected).
- R44 (2026-04-10): Further split `static/stream_controller.js` by decomposing the still-large transcript/lifecycle subcontrollers into focused helper factories while preserving the app-facing `createController(...)` facade. Added transcript-side `createTranscriptSignatureHelpers(...)`, `createTranscriptHydrationController(...)`, `createTranscriptEventController(...)`, and `createTranscriptConsumeController(...)`, leaving `createStreamTranscriptController(...)` as a thin composition wrapper. Added lifecycle-side `createStreamFocusController(...)`, `createStreamFinalizeController(...)`, `createStreamSendController(...)`, and `createStreamResumeController(...)`, leaving `createStreamLifecycleController(...)` as composition/wiring only. Current hotspot sizes after the pass: `createStreamTranscriptController(...)` 14 LOC, `createStreamLifecycleController(...)` 27 LOC, with the owned work split into `createTranscriptEventController(...)` 297 LOC, `createTranscriptConsumeController(...)` 194 LOC, `createStreamSendController(...)` 209 LOC, and `createStreamResumeController(...)` 229 LOC; file size is now `1782` LOC. Validation: `node --check static/stream_controller.js tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`, `node --test tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs` (51 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "stream_controller or resume or pending_stream_snapshot"` → `1 failed, 2 passed, 6 deselected` on an unrelated pre-existing dirty-tree assertion in `runtime_latency_helpers.js` (`if (activeKey && !hasLiveController) {`), while the... [truncated]
- R39 (2026-04-09): Decomposed the old `tests/stream_controller.test.mjs` monolith into focused suites aligned with the helper-owned stream controller seams. Added a shared harness in `tests/stream_controller_test_harness.mjs`, split direct policy/session/lifecycle tests into `tests/stream_controller_policy_session.test.mjs`, split stream-consumption regressions into `tests/stream_controller_response.test.mjs`, split hydration/reconnect/finalize coverage into `tests/stream_controller_resume_finalize.test.mjs`, and moved tool-trace contracts into `tests/stream_controller_tool_trace.test.mjs`. Kept the existing `tests/stream_controller_app_delegation.test.mjs` app-wrapper contract coverage unchanged. Validation: `node --check tests/stream_controller_test_harness.mjs tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`, `node --test tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs` (48 passed).
- R38 (2026-04-09): Verified the runtime-helper split is already fully landed and wired. `static/runtime_helpers.js` is now a compatibility facade that resolves and re-exports the focused helper slices in `static/runtime_unread_helpers.js`, `static/runtime_latency_helpers.js`, and `static/runtime_history_helpers.js`, while app-side contracts already delegate through `createLatencyPersistenceController(...)`, `createLatencyController(...)`, and `createHapticUnreadController(...)`. Route/meta coverage already asserts the split runtime helper assets are versioned/no-store and loaded in the expected page script order. Validation: `node --check static/runtime_helpers.js tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs`, `node --test tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs` (43 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "runtime or latency or unread"` (5 passed, 57 deselected).
- R37 (2026-04-09): Finished the remaining `job_runtime.py` decomposition by extracting the runner-state and best-effort/support utility bands into new helper modules while preserving the `JobRuntime` public/monkeypatch surface. Added `job_runtime_runner_state.py` for `_try_start_job_runner(...)`, `_finish_job_runner(...)`, and `_terminate_job_children(...)` ownership, plus `job_runtime_support.py` for `_clear_touch_tracking(...)`, `_record_best_effort_failure(...)`, `best_effort_failure_counts()`, `_record_runtime_counter(...)`, `_touch_job_best_effort(...)`, `is_stale_chat_job_error(...)`, `_bounded_attempts_for_display(...)`, `_fd_metrics()`, and `_safe_add_system_message(...)` ownership. Rewired `job_runtime.py` so those methods are compatibility delegates only, leaving the existing `JobRuntime` contract and monkeypatch-friendly tests intact. Also expanded `tests/test_broad_exception_policy.py` to cover the two new helper modules so their policy-tagged broad exception handlers remain guarded. Validation: `source .venv/bin/activate && python -m py_compile job_runtime.py job_runtime_runner_state.py job_runtime_support.py tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py tests/test_broad_exception_policy.py` (passed); `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py::test_sweep_stale_running_jobs_clears_active_runner_record tests/test_routes_jobs_runtime.py::test_runtime_status_endpoint_exposes_runtime_diagnostics tests/test_routes_jobs_runtime.py::test_touch_job_best_effort_records_failure_and_logs_warning tests/test_routes_jobs_runtime.py::test_safe_add_system_message_records_failure_and_logs_warning tests/test_routes_jobs_runtime.py::test_safe_add_system_message_emits_only_one_terminal_message_per_job tests/test_routes_jobs_runtime.py::test_bounded_attempts_for_display_caps_retry_count tests/test_routes_jobs_runtime.py::test_runtime_duplicate_job_runner_guard tests/test_job_ru... [truncated]

- R35 (2026-04-09): Broke `static/stream_controller.js` `createController(...)` into focused internal subcontrollers while preserving the existing module facade that `static/app.js` consumes. Added helper-owned `createStreamSessionController(...)` for replay cursor / abort-controller / first-assistant notification state, `createStreamTranscriptController(...)` for transcript patching / terminal application / hydration / reconnect consumption, and `createStreamLifecycleController(...)` for finalize / send / resume orchestration. Rewired `createController(...)` into composition-only glue over those three factories, exported the new subcontroller factories for direct coverage, and kept the public `createController(...)` return surface unchanged for app compatibility. Added direct subcontroller regressions in `tests/stream_controller.test.mjs` and refreshed `tests/test_streaming_hardening_guards.py` so ownership assertions track the new split while keeping behavior checks intact. Validation: `node --check static/stream_controller.js tests/stream_controller.test.mjs`, `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs` (46 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "test_stream_controller_module_exports_core_api or test_app_resume_handles_no_active_job_reconnect_gracefully or test_app_persists_pending_stream_snapshot_for_pre_resume_rehydrate"` (3 passed, 6 deselected).
- R34 (2026-04-09): Finished the last meaningful `static/app.js` runtime/shell orchestration extractions by moving the remaining ownership seams into adjacent helpers. `static/composer_viewport_helpers.js` now owns `focusComposerForNewChat(...)`, `static/shell_ui_helpers.js` owns `confirmAction(...)`, `static/runtime_helpers.js` owns unread increment trace logging, `static/chat_tabs_helpers.js` owns reconnect resume-budget/block bookkeeping (`suppressBlockedChatPending`, `clearReconnectResumeBlock`, `resetReconnectResumeBudget`, `consumeReconnectResumeBudget`, `blockReconnectResume`, `isReconnectResumeBlocked`), and `static/stream_state_helpers.js` owns pending-finalize trace logging via `createLifecycleController(...)`. Rewired `static/app.js` so those functions are thin delegates only, added direct helper regressions in `tests/composer_viewport_helpers.test.mjs`, `tests/shell_ui_helpers.test.mjs`, `tests/chat_tabs_helpers.test.mjs`, and `tests/stream_state_helpers.test.mjs`, expanded app delegation coverage in `tests/runtime_app_delegation.test.mjs`, `tests/shell_ui_app_delegation.test.mjs`, `tests/chat_tabs_app_delegation.test.mjs`, and `tests/stream_state_app_delegation.test.mjs`, and refreshed route/meta + hardening assertions so helper ownership is explicit. Validation: `node --check static/app.js static/composer_viewport_helpers.js static/runtime_helpers.js static/shell_ui_helpers.js static/chat_tabs_helpers.js static/stream_controller.js static/stream_state_helpers.js tests/composer_viewport_helpers.test.mjs tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs tests/shell_ui_helpers.test.mjs tests/shell_ui_app_delegation.test.mjs tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs`, `node --test tests/composer_viewport_helpers.test.mjs tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs tests/... [truncated]

- R33 (2026-04-09): Added direct extracted-module contract coverage for the new chat-management and stream helper/service boundaries. Landed `tests/test_routes_chat_management_service.py` for create/branch/remove payload shaping and runtime eviction behavior, added `tests/test_routes_chat_stream_service.py` for verified-user/chat activation, stale-open-job dead-lettering, synthetic terminal recovery, segment-rollover logging, helper parsing/env controls, and the segment-deadline-vs-terminal-state boundary so the SSE stream emits a synthetic terminal event instead of silently closing when DB state turns terminal at rollover time. Tightened the resumed route tests back to deterministic claimed-job assertions now that `_authed_client(...)` shuts the runtime down during setup, and refreshed route/hardening coverage to point monkeypatches/ownership assertions at `routes_chat_stream_generator.py` and `routes_chat_stream_service.py` instead of the old monolith internals. Validation: `source .venv/bin/activate && python -m py_compile routes_chat_stream.py routes_chat_stream_service.py routes_chat_stream_generator.py tests/test_routes_chat.py tests/test_routes_chat_stream_service.py tests/test_routes_chat_management_service.py tests/test_streaming_hardening_guards.py`, `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat.py tests/test_routes_chat_stream_service.py tests/test_routes_chat_management_service.py tests/test_streaming_hardening_guards.py` (82 passed), and `node --check static/runtime_helpers.js static/stream_controller.js tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs && node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs` (94 passed) to keep the pre-existing runtime latency/history tweak validated alongside the route split.
- R25 follow-up 56 (2026-04-09): Moved the last meaningful tool-trace snapshot side effects out of bespoke `static/app.js` wrappers and into `static/stream_controller.js`, which now owns pending tool-trace snapshot persistence alongside the tool-trace lifecycle. Extended `createToolTraceController({ histories, cleanDisplayText, persistPendingStreamSnapshot })` so `appendInlineToolTrace(...)`, `dropPendingToolTraceMessages(...)`, and `finalizeInlineToolTrace(...)` persist pending stream snapshots inside the helper owner when tool-trace history changes. Rewired `static/app.js` to inject `persistPendingStreamSnapshot` once at `toolTraceController` composition time and reduced the three wrappers to thin delegates. Added direct helper regressions in `tests/stream_controller.test.mjs`, added new app-side delegation coverage in `tests/stream_controller_app_delegation.test.mjs`, and refreshed `tests/test_streaming_hardening_guards.py` so the helper owns the snapshot side effects while app assertions track only controller composition/delegation. Validation: `node --check static/stream_controller.js static/app.js tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs`, `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs` (39 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "test_app_persists_pending_stream_snapshot_for_pre_resume_rehydrate or test_stream_controller_module_exports_core_api or test_app_resume_handles_no_active_job_reconnect_gracefully"` (3 passed, 6 deselected).

- R30 (2026-04-09): Decomposed `routes_chat_management.py` into thin route adapters plus a new service-layer module. Added `routes_chat_management_service.py` with getter-injected `ChatManagementService` operations for chat serialization/history payloads, file-preview resolution/building, runtime eviction, pin/reopen/branch/clear/remove flows, and chats-status response shaping while preserving monkeypatch-friendly dynamic getter behavior. Rewrote `routes_chat_management.py` to keep route registration focused on request parsing, boolean/title validation, guard wiring, and delegation into the service module. During validation, fixed a missing `_authed_client(...)` setup line in `tests/test_routes_chat.py` and refreshed a stale file-preview ownership assertion in `tests/test_routes_meta.py` to match the current app-side click binding contract. Validation: `source .venv/bin/activate && python -m py_compile routes_chat_management.py routes_chat_management_service.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_server_public_routes.py`; `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat.py tests/test_routes_meta.py` (124 passed); `source .venv/bin/activate && python -m pytest -q tests/test_server_public_routes.py` (4 passed). Post-refactor line counts: `routes_chat_management.py` 290 LOC, `routes_chat_management_service.py` 418 LOC.
- R25 follow-up 55 (2026-04-09): Moved stream-phase wrapper ownership out of bespoke `static/app.js` helpers and into `static/stream_state_helpers.js`. Added `createPhaseController({ streamPhaseByChat, renderTraceLog })` so the stream-state helper now owns `getStreamPhase(chatId)` and `setStreamPhase(chatId, phase)` composition, including helper-owned `renderTraceLog("stream-phase", ...)` emission on phase changes. Rewired `static/app.js` to build `streamPhaseController` through `streamStateHelpers.createPhaseController(...)`, removed the old raw `getStreamPhaseFromState` / `setStreamPhaseInState` aliases, and kept `getStreamPhase(...)` / `setStreamPhase(...)` as thin delegates. Expanded `tests/stream_state_helpers.test.mjs` with direct phase-controller regression coverage, refreshed `tests/stream_state_app_delegation.test.mjs` to assert app-side controller composition and delegation, and updated `tests/test_streaming_hardening_guards.py` so the stream-state helper is the explicit owner of the stream-phase wrapper boundary. Validation: `node --check static/stream_state_helpers.js static/app.js tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs`, `node --test tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs` (11 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "test_app_wires_stream_state_helper_before_stream_lifecycle_calls or test_stream_state_helper_exports_phase_api_and_guardrails or test_app_persists_pending_stream_snapshot_for_pre_resume_rehydrate"` (3 passed, 6 deselected).
- R25 follow-up 54 (2026-04-09): Moved the remaining render-trace patch/find-latest composition out of bespoke `static/app.js` wrappers and behind the existing message render controller boundary. Extended `static/render_trace_message_helpers.js` `createController({...})` to own `findLatestHistoryMessageByRole(chatId, role, { pendingOnly })`, `findLatestAssistantHistoryMessage(chatId, { pendingOnly })`, `findMessageNodeByKey(selector, messageKey, alternateMessageKey)`, `patchVisiblePendingAssistant(chatId, nextBody, pendingState)`, and `patchVisibleToolTrace(chatId)` using live getters for history/messages container/active chat/stream phase plus injected patch-policy callbacks. Updated `static/render_trace_helpers.js` fallback `createMessageRenderController(deps)` facade to mirror the same ownership, rewired `static/app.js` so those wrappers now delegate through `messageRenderController`, and expanded `tests/render_trace_message_helpers.test.mjs` plus `tests/render_trace_app_delegation.test.mjs` to cover the new helper-owned patch orchestration + app-side delegation. Refreshed `tests/test_routes_meta.py` so render-trace ownership assertions follow the message render controller instead of stale inline `renderTraceHelpers.*` composition in `app.js`. Validation: `node --check static/render_trace_message_helpers.js static/render_trace_helpers.js static/app.js tests/render_trace_message_helpers.test.mjs tests/render_trace_app_delegation.test.mjs`, `node --test tests/render_trace_message_helpers.test.mjs tests/render_trace_app_delegation.test.mjs` (18 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "render_trace or desktop_dev_auth_bootstrap_guards_present"` (6 passed).
- R25 follow-up 53 (2026-04-09): Moved the stream reconnect/recovery policy cluster out of bespoke `static/app.js` helpers and into `static/stream_controller.js` so reconnect ownership now lives with the stream controller. Added `createResumeRecoveryPolicy({...})` to `stream_controller.js`, exporting helper-owned defaults + utilities for `RESUME_RECOVERY_MAX_ATTEMPTS`, `RESUME_REATTACH_MIN_INTERVAL_MS`, `RESUME_COMPLETE_SETTLE_MS`, `delayMs(...)`, `isTransientResumeRecoveryError(...)`, and `nextResumeRecoveryDelayMs(...)`. Rewired `static/app.js` to build `resumeRecoveryPolicy` through `streamControllerHelpers.createResumeRecoveryPolicy(...)` and pass the resulting values/functions into `streamControllerHelpers.createController(...)` instead of keeping inline reconnect constants/functions in app scope. Added direct policy regression coverage in `tests/stream_controller.test.mjs`, added new app-side delegation/ownership coverage in `tests/stream_controller_app_delegation.test.mjs`, and updated `tests/test_routes_meta.py` plus `tests/test_streaming_hardening_guards.py` so the stream helper is the explicit owner of reconnect policy while app assertions track only composition/delegation. Validation: `node --check static/stream_controller.js static/app.js tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs`, `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs` (37 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py -k "stream_resume_and_graceful_completion_helpers_are_centralized or app_resume_handles_no_active_job_reconnect_gracefully or app_streaming_keeps_status_copy_out_of_terminal_error_payloads"` (2 passed).
- R25 follow-up 52 (2026-04-09): Moved the remaining render/message wrapper composition out of bespoke `static/app.js` helpers and behind a new render-trace message controller boundary. Added `createController({...})` to `static/render_trace_message_helpers.js`, exposed it through `static/render_trace_helpers.js` as `createMessageRenderController(deps)`, and rewired `static/app.js` so it now builds `messageRenderController` with live getters (`getAllowedRoots`, `getOperatorDisplayName`) plus the message template/time formatter. `renderBody(...)`, `renderToolTraceBody(...)`, `roleLabelForMessage(...)`, `messageVariantForRole(...)`, `shouldSkipMessageRender(...)`, `applyMessageMeta(...)`, `renderMessageContent(...)`, `messageStableKey(...)`, `messageStableKeyForPendingState(...)`, `upsertMessageNode(...)`, `createMessageNode(...)`, and `appendMessages(...)` are now thin delegates through the helper-owned controller instead of app-local composition shims. Added direct controller regression coverage in `tests/render_trace_message_helpers.test.mjs`, refreshed `tests/render_trace_app_delegation.test.mjs` to assert app-side controller composition/delegation, and updated `tests/test_routes_meta.py` so helper ownership is explicit while app assertions track only `messageRenderController` delegation. Validation: `node --check static/render_trace_message_helpers.js static/render_trace_helpers.js static/app.js tests/render_trace_message_helpers.test.mjs tests/render_trace_app_delegation.test.mjs`, `node --test tests/render_trace_message_helpers.test.mjs tests/render_trace_app_delegation.test.mjs` (18 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "render_trace or desktop_dev_auth_bootstrap_guards_present"` (6 passed).
- R25 follow-up 51 (2026-04-09): Moved runtime latency persistence/logging orchestration out of bespoke `static/app.js` wrappers and behind runtime-owned controllers. Added `createLatencyPersistenceController(...)` to `static/runtime_helpers.js`, extended `createLatencyController(...)` to own persistence callbacks plus latency render-trace logging, and rewired `static/app.js` so it now builds `latencyPersistenceController`, loads storage through that controller, passes `onLatencyMapMutated`, `renderTraceLog`, and document visibility into `latencyViewController`, and keeps `setChatLatency(...)`, `syncActiveLatencyChip()`, `loadLatencyByChatFromStorage()`, and `persistLatencyByChatToStorage()` as thin delegates. Added direct runtime regressions in `tests/frontend_runtime.test.mjs` for the new persistence controller plus relocated latency logging/persist side effects, added new app-wrapper delegation coverage in `tests/runtime_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions so the runtime helper boundary is explicit. Validation: `node --check static/runtime_helpers.js static/app.js tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs`, `node --test tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs` (33 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "desktop_dev_auth_bootstrap_guards_present or resume_keeps_existing_latency_value_instead_of_recalculating_flash"` (2 passed).
- R25 follow-up 50 (2026-04-09): Moved shell/debug-pill visibility glue out of bespoke `static/app.js` helpers and behind `shellUiController` ownership. `static/shell_ui_helpers.js` now owns `setElementHidden(...)` and `syncDebugOnlyPillVisibility()` alongside the existing Telegram shell/fullscreen controls, while `static/app.js` keeps only thin delegates (`shellUiController.setElementHidden(element, hidden)` and `shellUiController.syncDebugOnlyPillVisibility()`). Added direct helper regressions in `tests/shell_ui_helpers.test.mjs` for hidden-attribute toggling plus desktop/dev-pill visibility gating, added new app-wrapper delegation coverage in `tests/shell_ui_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions so shell-ui helper ownership is explicit. During the pass, the initial fuzzy patch truncated `static/app.js` at the `streamController` composition block; recovered the file by reconstructing the missing tail against the current helper/controller contracts (stream/controller wrappers, interaction/message-action wiring, keyboard wrappers, shell/settings/composer delegates, startup/install calls), then re-ran syntax + delegation checks to verify the restored tail stayed behavior-preserving. Validation: `node --check static/app.js static/shell_ui_helpers.js tests/shell_ui_helpers.test.mjs tests/shell_ui_app_delegation.test.mjs`, `node --test tests/shell_ui_helpers.test.mjs tests/shell_ui_app_delegation.test.mjs tests/interaction_app_delegation.test.mjs tests/message_actions_app_delegation.test.mjs tests/keyboard_shortcuts_app_delegation.test.mjs tests/startup_bindings_app_delegation.test.mjs` (17 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "shell_ui_helpers_static_asset_is_no_store or startup_auth_allows_empty_chat_state or pinned_chat_mvp_ui_wiring_present_in_client_script"` (3 passed).
- R25 follow-up 49 (2026-04-09): Moved operator handle/label bootstrap glue out of bespoke `static/app.js` helpers and behind `bootstrapAuthController` ownership. `static/bootstrap_auth_helpers.js` now owns `normalizeHandle(value)`, `fallbackHandleFromDisplayName(value)`, and `refreshOperatorRoleLabels()` with safe built-in defaults plus override hooks, while `static/app.js` keeps only thin delegates (`bootstrapAuthController.normalizeHandle(value)`, `bootstrapAuthController.fallbackHandleFromDisplayName(value)`, `bootstrapAuthController.refreshOperatorRoleLabels()`) and passes raw stateful dependencies (`getOperatorDisplayName`, `messagesEl`) into `bootstrapAuthHelpers.createController(...)`. Added direct helper regressions in `tests/bootstrap_auth_helpers.test.mjs` for default normalization/fallback/role-label refresh behavior, expanded `tests/bootstrap_auth_app_delegation.test.mjs` to assert the new app-side delegates, and refreshed `tests/test_routes_meta.py` ownership assertions so bootstrap auth helper ownership is explicit. Validation: `node --check static/bootstrap_auth_helpers.js static/app.js tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs`, `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs` (13 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "startup_auth_allows_empty_chat_state"` (1 passed).
- R25 follow-up 43 (2026-04-09): Moved interaction/composer-submit plus selection-quote binding orchestration out of bespoke `static/app.js` shims and behind a new `interactionHelpers.createController(...)` boundary. `app.js` now builds `interactionController`, keeps `handleComposerSubmitShortcut(event)` as a thin delegate to `interactionController.handleComposerSubmitShortcut(event)`, and replaces the old `createSelectionQuoteController()`/`selectionQuoteController.bind()` glue with `installSelectionQuoteBindings()` -> `interactionController.bindSelectionQuoteBindings()`. Extended `static/interaction_helpers.js` with a helper-owned controller that lazily resolves active chat ID, memoizes the underlying selection-quote controller, and makes binding idempotent; added direct controller regressions in `tests/interaction_helpers.test.mjs`, new app-wrapper delegation coverage in `tests/interaction_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions to track the new controller boundary. Validation: `node --check static/interaction_helpers.js static/app.js tests/interaction_helpers.test.mjs tests/interaction_app_delegation.test.mjs`, `node --test tests/interaction_helpers.test.mjs tests/interaction_app_delegation.test.mjs` (17 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "message_action_copy_helpers_are_split_to_module or quote_selection_sync_is_debounced_in_client_script"` (2 passed).
- R25 follow-up 42 (2026-04-09): Collapsed the redundant chat-ui composition shim in `static/app.js` by delegating tab/pinned wrapper functions straight through `chatTabsController`, which already owns the `chat_ui_helpers.js` bridge. `app.js` no longer builds a duplicate `chatUiController`; `getOrCreateTabNode(...)`, `getTabBadgeState(...)`, `applyTabBadgeState(...)`, `applyTabNodeState(...)`, `removeMissingTabNodes(...)`, `renderTabs()`, `renderPinnedChats()`, `refreshTabNode(...)`, and `syncActiveTabSelection(...)` are now thin `chatTabsController` delegates. Updated `tests/chat_ui_app_delegation.test.mjs` to assert the redundant shim is gone and refreshed `tests/test_routes_meta.py` ownership assertions to track direct `chatTabsController` delegation. Validation: `node --check static/app.js tests/chat_ui_app_delegation.test.mjs`, `node --test tests/chat_ui_helpers.test.mjs tests/chat_ui_app_delegation.test.mjs tests/chat_tabs_helpers.test.mjs` (17 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "pinned_chat_mvp_ui_wiring_present_in_client_script or chat_ui_helpers_static_asset_is_no_store"` (2 passed).
- R25 follow-up 41 (2026-04-09): Moved chat tab/pinned UI delegates out of `static/app.js` and behind a new `chatUiHelpers.createController(...)` composition boundary, so `app.js` now builds `chatUiController` from `static/chat_ui_helpers.js` and keeps `getOrCreateTabNode(...)`, `getTabBadgeState(...)`, `applyTabBadgeState(...)`, `applyTabNodeState(...)`, `removeMissingTabNodes(...)`, `renderTabs()`, `refreshTabNode(...)`, and `syncActiveTabSelection(...)` as thin delegates to the helper-owned controller instead of routing them through `chatTabsController`. Kept pinned-collapse state ownership in `static/chat_tabs_helpers.js`, but rewired `renderPinnedChats()` to compose `chatTabsController.maybeAutoCollapsePinnedChats()` + `chatUiController.renderPinnedChats()` + `chatTabsController.syncPinnedChatsCollapseUi()` so UI rendering and collection state are cleaner. Extended `static/chat_ui_helpers.js` with `createController(deps = {})`, added direct helper controller regressions in `tests/chat_ui_helpers.test.mjs`, added new app-wrapper delegation coverage in `tests/chat_ui_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions so the new helper boundary is explicit. Validation: `node --check static/chat_ui_helpers.js static/app.js tests/chat_ui_helpers.test.mjs tests/chat_ui_app_delegation.test.mjs`, `node --test tests/chat_ui_helpers.test.mjs tests/chat_ui_app_delegation.test.mjs` (9 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "pinned_chat_mvp_ui_wiring_present_in_client_script"` (1 passed).
- R25 follow-up 40 (2026-04-09): Moved message-action copy binding orchestration out of `static/app.js` and behind `static/message_actions_helpers.js`, so `app.js` now builds a `messageActionsController` and keeps `installMessageActionBindings()` as a thin delegate instead of owning copy-state creation + direct listener wiring. Extended `message_actions_helpers.js` with `createController(...)`, added helper-level controller regression coverage in `tests/message_actions_helpers.test.mjs`, added new app-wrapper delegation coverage in `tests/message_actions_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions so the helper boundary is explicit. Validation: `node --check static/message_actions_helpers.js static/app.js tests/message_actions_helpers.test.mjs tests/message_actions_app_delegation.test.mjs`, `node --test tests/message_actions_helpers.test.mjs tests/message_actions_app_delegation.test.mjs` (5 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "message_action_copy_helpers_are_split_to_module"` (1 passed).
- R25 follow-up 39 (2026-04-08): Moved skin-preference persistence out of `static/app.js` and behind `static/visibility_skin_helpers.js`, so `saveSkinPreference(skin)` is now a thin `visibilitySkinController` delegate beside the existing skin normalize/storage/apply lifecycle helpers. Extended the visibility controller with helper-owned `/api/preferences/skin` persistence + returned-skin application, added direct helper regression coverage in `tests/visibility_skin_helpers.test.mjs`, added new app-wrapper delegation coverage in `tests/visibility_skin_app_delegation.test.mjs`, and refreshed `tests/test_routes_meta.py` ownership assertions so the new helper boundary is explicit. Validation: `node --check static/visibility_skin_helpers.js static/app.js tests/visibility_skin_helpers.test.mjs tests/visibility_skin_app_delegation.test.mjs`, `node --test tests/visibility_skin_helpers.test.mjs tests/visibility_skin_app_delegation.test.mjs` (10 passed), and `python -m pytest -q tests/test_routes_meta.py -k visibility_skin` (1 passed).
- R25 follow-up 38 (2026-04-08): Moved the remaining startup/bootstrap orchestration behind helper-owned controllers so `static/app.js` keeps only thin delegates for startup flow entrypoints. Extended `static/bootstrap_auth_helpers.js` with auth-bootstrap retry/version-refresh ownership and shared `apiPost(...)` handling, extended `static/startup_bindings_helpers.js` with startup binding validation/bootstrap/watchdog ownership, and rewired `static/app.js` to delegate `getMissingBootstrapBindings()`, `reportBootstrapMismatch()`, `fetchAuthBootstrapWithRetry()`, `maybeRefreshForBootstrapVersionMismatch()`, `bootstrap()`, and `installPendingCompletionWatchdog()`. Updated `tests/test_routes_meta.py` and `tests/test_streaming_hardening_guards.py` so ownership assertions follow the new helper boundaries. Validation: `node --check static/app.js static/bootstrap_auth_helpers.js static/startup_bindings_helpers.js static/startup_metrics_helpers.js static/chat_tabs_helpers.js static/stream_controller.js`, `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs tests/startup_metrics_helpers.test.mjs tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` (98 passed), and `python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py tests/test_server_public_routes.py` (75 passed).
- R25 follow-up 37 (2026-04-08): Moved shared authenticated POST request handling out of `static/app.js` and into `static/bootstrap_auth_helpers.js`, so `apiPost(url, payload)` now stays a thin `bootstrapAuthController` delegate beside the other bootstrap/auth request utilities. Added direct helper coverage for auth-payload composition and Telegram-session-expiry UI handling in `tests/bootstrap_auth_helpers.test.mjs`, expanded app-wrapper delegation coverage in `tests/bootstrap_auth_app_delegation.test.mjs`, and refreshed route/meta + streaming hardening assertions so the extracted ownership is tracked in `bootstrap_auth_helpers.js` / `stream_controller.js` instead of the old `app.js` internals. Validation: `node --check static/bootstrap_auth_helpers.js static/app.js tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs`, `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs tests/frontend_runtime.test.mjs` (38 passed), and `python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py` (71 passed).
- R25 follow-up 36 (2026-04-08): Moved chat-history system-message rendering and pending-stream snapshot side effects out of `static/app.js` and into `static/chat_history_helpers.js`, reusing a shared system-card builder for both empty-state rendering and inline notices. `app.js` now keeps `appendSystemMessage(...)` and `updatePendingAssistant(...)` as thin `chatHistoryController` delegates, while helper-owned tests cover no-active-chat system cards, active-chat history insertion, and snapshot persist/clear behavior. Validation: `node --check static/chat_history_helpers.js static/app.js tests/chat_history_helpers.test.mjs tests/chat_history_app_delegation.test.mjs`, `node --test tests/chat_history_helpers.test.mjs tests/chat_history_app_delegation.test.mjs` (42 passed), `python -m py_compile tests/test_routes_meta.py`, and `python -m pytest -q tests/test_routes_meta.py` (62 passed).
- R25 follow-up 35 (2026-04-08): Moved resume-cooldown + blocked-reconnect pending suppression ownership out of `static/app.js` and into `static/chat_tabs_helpers.js`, making `upsertChat(...)` and `syncChats(...)` thin delegates again while preserving reconnect guard behavior. Extended `chat_tabs_helpers.createController(...)` with cooldown/block suppression hooks (`resumeCooldownUntilByChat`, `reconnectResumeBlockedChats`, `suppressBlockedChatPending`, `nowFn`) and added direct regressions in `tests/chat_tabs_helpers.test.mjs` plus new app-wrapper coverage in `tests/chat_tabs_app_delegation.test.mjs`. Validation: `node --check static/chat_tabs_helpers.js static/app.js tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs`, `node --test tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs` (9 passed). Scoped Python validation caveat: `.venv/bin/python -m pytest -q tests/test_streaming_hardening_guards.py tests/test_routes_meta.py` currently reports pre-existing unrelated failures in dirty-tree guard assertions and runtime-thread shutdown (`5 failed, 66 passed`), so this slice was validated with direct Node helper/delegation coverage.
- R28 (2026-04-08): Decomposed the render-trace/browser regression test monolith into focused suites so the extracted helper boundaries now have direct ownership coverage. Split the old `tests/render_trace_helpers.test.mjs` into `tests/render_trace_debug_helpers.test.mjs`, `tests/render_trace_text_helpers.test.mjs`, `tests/render_trace_message_helpers.test.mjs`, `tests/render_trace_history_helpers.test.mjs`, and a slim `tests/render_trace_helpers.test.mjs` facade contract test; also moved the anchored viewport regression out of `tests/frontend_runtime.test.mjs` into the history-focused suite. Validation: `node --check tests/render_trace_debug_helpers.test.mjs tests/render_trace_text_helpers.test.mjs tests/render_trace_message_helpers.test.mjs tests/render_trace_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/frontend_runtime.test.mjs`, `node --test tests/render_trace_debug_helpers.test.mjs tests/render_trace_text_helpers.test.mjs tests/render_trace_message_helpers.test.mjs tests/render_trace_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/render_trace_app_delegation.test.mjs tests/frontend_runtime.test.mjs` (61 passed), and `python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py` (71 passed).
- R29 (2026-04-08): Split `static/render_trace_helpers.js` into focused facade-backed helper slices and shrank the old monolith to a compatibility layer. Added `static/render_trace_text_helpers.js`, `static/render_trace_debug_helpers.js`, `static/render_trace_message_helpers.js`, and `static/render_trace_history_helpers.js`; rewired `static/render_trace_helpers.js` to re-export the split ownership surface while keeping `static/app.js` and existing delegation contracts stable. Wired the new assets through `templates/app.html`, `server_public_routes.py`, `server.py`, and route-meta cache/no-store coverage. Validation: `python -m py_compile server.py server_public_routes.py tests/test_routes_meta.py tests/test_streaming_hardening_guards.py`, `node --check static/render_trace_helpers.js static/render_trace_text_helpers.js static/render_trace_debug_helpers.js static/render_trace_message_helpers.js static/render_trace_history_helpers.js`, `node --test tests/render_trace_debug_helpers.test.mjs tests/render_trace_text_helpers.test.mjs tests/render_trace_message_helpers.test.mjs tests/render_trace_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/render_trace_app_delegation.test.mjs tests/frontend_runtime.test.mjs` (61 passed), and `python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py` (71 passed).
- R25 follow-up 34 (2026-04-08): Extracted startup/open telemetry ownership from `static/app.js` into new `static/startup_metrics_helpers.js`, leaving `app.js` with a thinner composition layer for boot-stage logging, shell reveal state, and startup latency summaries. Wired the helper through `templates/app.html`, `server_public_routes.py`, and route-meta static-asset coverage, and added direct helper regressions in `tests/startup_metrics_helpers.test.mjs`. Validation: `node --check static/startup_metrics_helpers.js static/app.js`, `node --test tests/startup_metrics_helpers.test.mjs tests/frontend_runtime.test.mjs` (30 passed within the broader helper run), and `python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py` (66 passed).
- R31 (2026-04-08): Hardened warm-runtime ownership/reuse env semantics so requested vs resolved ownership stays explicit and warm-reuse defaults/tests no longer drift with ambient host env. Stopped `MiniAppConfig` from collapsing requested ownership into resolved ownership for subprocess mode, made `HermesClient` report `persistent_runtime_ownership_requested` strictly from `MINI_APP_PERSISTENT_RUNTIME_OWNERSHIP_REQUESTED` (defaulting to `auto`), and updated `server.py` client bootstrap to inject config-requested ownership separately from resolved ownership. Added autouse env-clearing fixtures in `tests/test_hermes_client.py` and `tests/test_config.py` so ambient miniapp env no longer silently flips warm-reuse/persistent-session behavior under test, and updated config coverage to assert requested `shared` + resolved `checkpoint_only` in subprocess mode. Validations: `python -m py_compile hermes_client.py miniapp_config.py server.py tests/test_hermes_client.py tests/test_config.py`, `python -m pytest -q tests/test_config.py tests/test_hermes_client.py -k "warm_reuse or runtime_status_reports_recall_health or persistent_runtime_ownership"` (17 passed), `python -m pytest -q tests/test_streaming_hardening_guards.py tests/test_server_startup.py` (11 passed), and direct `HermesClient().runtime_status()` spot-check confirmed startup routing now reports requested `auto` alongside resolved `shared`.
- R27 (2026-04-02): Reduced production stream/debug logging noise and sensitive payload exposure with behavior-preserving gates and redaction hardening. Added `operator_debug` + `stream_timing_debug` config wiring in `miniapp_config.py` and `server.py`, passed stream timing controls through `ChatRouteContext` into `routes_chat_stream.py` (replacing inline env reads), and required operator-debug enablement before publishing `_timing` payloads in `job_runtime.py`. Trimmed frontend stream debug payloads in `static/stream_controller.js` by removing chunk/tail preview content while preserving event/length telemetry. Extended startup diagnostics payload shape in `server_startup.py` for operator/timing debug visibility. Updated regressions in `tests/test_routes_meta.py`, `tests/test_streaming_hardening_guards.py`, and `tests/test_server_startup.py`; validations: `python -m py_compile miniapp_config.py server.py routes_chat_context.py routes_chat_stream.py job_runtime.py server_startup.py`, `node --check static/stream_controller.js`, and `python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py tests/test_server_startup.py` (60 passed).
- R25 follow-up 33 (2026-04-02): Extended `static/bootstrap_auth_helpers.js` with bootstrap/auth request utility ownership (`authPayload`, `safeReadJson`, `summarizeUiFailure`, `parseStreamErrorPayload`) and moved the remaining app-local bootstrap/auth helpers out of `static/app.js` into helper-managed delegates. Rewired `app.js` through thin `bootstrapAuthController` wrappers, injected `initData` + `parseSseEvent` into the controller, and kept helper-level `safeReadJson` override compatibility for existing test harnesses. Added direct helper coverage in `tests/bootstrap_auth_helpers.test.mjs` for payload/JSON fallback and stream error sanitization behavior, plus app-wrapper contract coverage in `tests/bootstrap_auth_app_delegation.test.mjs`; updated `tests/test_routes_meta.py` and `tests/test_streaming_hardening_guards.py` ownership/delegation assertions accordingly.
- R25 follow-up 32 (2026-04-02): Extended `static/chat_history_helpers.js` with `createMetaController(deps)` and moved active-chat metadata orchestration (`setActiveChatMeta`, `setNoActiveChatMeta`) out of `static/app.js` into helper-managed delegates with injected state getters/setters and UI callbacks. Rewired `app.js` through a thin `activeChatMetaController`, added direct helper coverage in `tests/chat_history_helpers.test.mjs` for deferred non-critical updates + empty-chat fallback rendering, and added wrapper delegation coverage in `tests/chat_history_meta_app_delegation.test.mjs`. Updated `tests/test_routes_meta.py` ownership assertions for `createMetaController` and app-side delegation.
- R25 follow-up 31 (2026-04-02): Extended `static/keyboard_shortcuts_helpers.js` with `createController(deps)` and moved keyboard shortcut dependency wiring/state resolution out of `static/app.js` into helper-managed delegates for tab cycling, arrow jumps, composer focus shortcut, and sticky-focus cleanup handlers. Rewired `app.js` through a thin `keyboardShortcutsController`, added direct helper coverage for lazy active-chat resolution in `tests/keyboard_shortcuts_helpers.test.mjs`, and added wrapper delegation coverage in `tests/keyboard_shortcuts_app_delegation.test.mjs`.
- R25 follow-up 30 (2026-04-02): Extended `static/chat_admin_helpers.js` with chat-tab context menu ownership (`closeChatTabContextMenu`, `openChatTabContextMenu`, `handleTabOverflowTriggerClick`, `handleGlobalChatContextMenuDismiss`, `handleTabContextForkClick`) and moved tab-context state/positioning/toggle logic out of `static/app.js` into helper-managed delegates. Rewired `app.js` through thin `chatAdminController` wrappers, added direct helper coverage for viewport clamping + active-tab gating + fork/dismiss behavior in `tests/chat_admin_helpers.test.mjs`, and added wrapper contract coverage in `tests/chat_admin_app_delegation.test.mjs`.
- R25 follow-up 29 (2026-04-02): Extended `static/render_trace_helpers.js` with `createHistoryRenderController(...)` and moved chat-history viewport/virtualization orchestration (`isNearBottom`, `shouldVirtualizeHistory`, `getEstimatedMessageHeight`, `updateVirtualMetrics`, `updateJumpLatestVisibility`, `markStreamUpdate`, `computeVirtualRange`, `renderVirtualizedHistory`, `renderFullHistory`, `tryAppendOnlyRender`, `restoreMessageViewport`, `finalizeRenderMessages`, `renderMessages`) out of `static/app.js` into helper-managed delegates. Rewired `app.js` through a thin `historyRenderController`, added wrapper delegation coverage in `tests/render_trace_app_delegation.test.mjs`, and added direct helper coverage for range computation + active-chat stream update gating in `tests/render_trace_helpers.test.mjs`.
- R25 follow-up 28 (2026-04-02): Extended `static/stream_state_helpers.js` with `createPersistenceController(...)` and moved stream resume-cursor plus pending-stream snapshot persistence (`read/write/get/set/clearStoredStreamCursor`, `read/write/clearPendingStreamSnapshot`, `normalizeSnapshotLines`, `mergeSnapshotToolJournalLines`, `persistPendingStreamSnapshot`, `restorePendingStreamSnapshot`) out of `static/app.js` into helper-managed delegates. Rewired `app.js` through a thin `streamPersistenceController`, added direct Node coverage in `tests/stream_state_helpers.test.mjs` plus a new wrapper guard in `tests/stream_state_app_delegation.test.mjs`, and updated streaming hardening assertions to follow helper ownership. Validation: `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/shared_utils.test.mjs tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- R25 follow-up 27 (2026-04-02): Extracted file-preview modal/orchestration logic from `static/app.js` into new `static/file_preview_helpers.js`, rewired app-side preview functions into thin `filePreviewController` delegates, and wired the new helper through `templates/app.html`, `server_public_routes.py`, and static no-store coverage in `server.py`. Added direct Node tests in `tests/file_preview_helpers.test.mjs` plus `tests/file_preview_app_delegation.test.mjs`, updated `tests/test_routes_meta.py` to assert helper ownership/delegation instead of brittle app-local implementation strings, and hardened the helper against stale overlapping preview responses / close-before-resolve races with explicit regression coverage. Validation: `node --check static/file_preview_helpers.js && node --check tests/file_preview_helpers.test.mjs && node --test tests/file_preview_helpers.test.mjs tests/file_preview_app_delegation.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py`.
- R25 follow-up 26 (2026-03-29): Extended `static/chat_history_helpers.js` with read-sync gating ownership (`maybeMarkRead`) and rewired `static/app.js` `maybeMarkRead(...)` into a thin `chatHistoryController` delegate. Injected read-gating dependencies into chat-history controller creation (`getIsAuthenticated`, `isNearBottomFn`, `messagesContainer`, `unseenStreamChats`, `markReadInFlight`, `markReadFn`) so in-flight dedupe and near-bottom/unread/active-chat checks stay centralized in the helper while preserving behavior. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for auth/active/visibility/unread gating and forced/in-flight dedupe behavior; updated `tests/test_routes_meta.py` ownership/delegation assertions for the newly extracted helper functions.
- R25 follow-up 25 (2026-03-29): Extended `static/chat_history_helpers.js` with chat history/view state ownership (`addLocalMessage`, `updatePendingAssistant`, `syncActiveMessageView`, `scheduleActiveMessageView`) and rewired matching `static/app.js` wrappers into thin delegates through `chatHistoryController`. Injected `runAfterUiMutation` into the chat-history controller (with timeout fallback) so deferred active-chat rerenders remain centralized while preserving viewport-safe scheduling semantics. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for pending-assistant mutation contracts, empty-finalize guard behavior, and active-chat-only sync/scheduled render behavior; updated `tests/test_routes_meta.py` ownership/delegation assertions for the newly extracted helper functions.

- R25 follow-up 18 (2026-03-29): Extended `static/render_trace_helpers.js` with message card render ownership (`messageVariantForRole`, `shouldSkipMessageRender`, `applyMessageMeta`, `renderMessageContent`) and rewired matching `static/app.js` wrappers to delegate through helper APIs with injected dependencies/callbacks (`formatMessageTime`, `roleLabelForMessage`, `renderBody`, `renderToolTraceBody`). Added direct helper tests for role/body skip contracts, metadata class/timestamp application, and tool-vs-body renderer dispatch; updated route-meta assertions for helper ownership and app-side delegation.
- R25 follow-up 17 (2026-03-29): Extended `static/render_trace_helpers.js` with message rendering ownership (`renderBody`, `renderToolTraceBody`, `roleLabelForMessage`) and rewired matching `static/app.js` wrappers to delegate through helper APIs with injected dependencies (`cleanDisplayText`, `escapeHtml`, `document`, `operatorDisplayName`). Added direct helper tests for fenced/non-fenced body rendering, tool-trace DOM/toggle behavior, and role-label mapping; updated route-meta assertions for helper ownership and app-side delegation.
- R25 follow-up 16 (2026-03-29): Extended `static/interaction_helpers.js` with selection-quote timer/reset helper ownership (`clearSelectionQuoteState`, `cancelSelectionQuoteTimer`, `scheduleSelectionQuoteClear`, `scheduleSelectionQuoteSync`) and rewired matching `static/app.js` wrappers to delegate through the helper module. Added direct helper tests for timer cancellation + scheduling behavior and selection-state clear/hide semantics; updated route-meta guard assertions for the newly extracted helper functions.
- R25 follow-up 15 (2026-03-29): Extended `static/interaction_helpers.js` with selection-quote/input utility ownership (`getQuoteWrapWidth`, `isCoarsePointer`, `applyQuoteIntoPrompt`, `activeSelectionQuote`, `quotePlacementKey`, `showSelectionQuoteAction`, `syncSelectionQuoteAction`) and rewired matching `static/app.js` functions to thin delegates. Added direct helper tests for quote-width bounds, coarse-pointer detection, and prompt quote insertion behavior; updated route-meta guard assertions so selection-quote positioning internals (`mobileToolbarUnsafeTop`, `composerTop`) track helper ownership.
- R25 follow-up 14 (2026-03-29): Extended `static/interaction_helpers.js` with quote text utilities (`unwrapLegacyQuoteBlock`, `normalizeQuoteSelection`, `splitGraphemes`, `wrapQuoteLine`, `formatQuoteBlock`) and rewired the corresponding `static/app.js` wrappers to delegate through the interaction helper module. Added direct Node helper coverage for legacy-frame unwrapping, whitespace/newline normalization, grapheme wrapping, and quote-block formatting; updated route-meta guard assertions to follow helper ownership of quote-format internals.
- R25 follow-up 13 (2026-03-29): Extended `static/runtime_helpers.js` with `createHapticUnreadController(...)` + `latestCompletedAssistantHapticKey(...)` and moved haptic/unread orchestration (`latestCompletedAssistantHapticKey`, `triggerIncomingMessageHaptic`, `incrementUnread`) out of `static/app.js` into runtime-controller delegates. Added direct Node runtime coverage and updated streaming hardening guards for new helper ownership.
- R25 follow-up 12 (2026-03-29): Extended `static/stream_controller.js` with `createToolTraceController(...)` and moved inline tool-trace orchestration (`resetToolStream`, `findPendingToolTraceMessage`, `ensurePendingToolTraceMessage`, `appendInlineToolTrace`, `finalizeInlineToolTrace`) out of `static/app.js` into helper-managed delegates. Added direct Node coverage in `tests/stream_controller.test.mjs` and updated streaming hardening guard assertions to follow extracted helper ownership.
- R25 follow-up 11 (2026-03-29): Extended `static/composer_state_helpers.js` with `createDraftController(...)` and moved draft storage orchestration (`loadDraftsFromStorage`, `persistDraftsToStorage`, `setDraft`, `getDraft`) out of `static/app.js` into controller-managed delegates. Added direct Node helper coverage in `tests/composer_state_helpers.test.mjs` and revalidated route/meta wiring suites.
- R25 follow-up 10 (2026-03-29): Extracted chat-list + pinned-collapse orchestration from `static/app.js` into `static/chat_tabs_helpers.js` via `createController(...)` (chat normalization/upsert/sync cleanup, pinned collapse state persistence/UI sync, auto-collapse threshold behavior, pin-button label sync), rewired `app.js` through thin delegates, and wired the new helper asset through template/version/no-store route plumbing. Added direct Node contract tests (`tests/chat_tabs_helpers.test.mjs`) plus route/template/static asset coverage updates.
- R25 follow-up 9 (2026-03-29): Extracted stream/activity chip orchestration into `runtime_helpers.createStreamActivityController(...)` and rewired `syncActivePendingStatus` plus send/resume status transitions (`active`, `error`, `network failure`, `reconnecting`, `already complete`, `reconnect failed`) to delegate from `static/app.js` through `streamActivityController`. Added direct runtime-helper tests covering pending/idle sync, active stream marker updates, reconnect/complete/failure gating, and preserved latency chip synchronization hooks.
- R25 follow-up 8 (2026-03-29): Extracted latency-chip orchestration from `static/app.js` into `runtime_helpers.createLatencyController(...)`, rewired `setChatLatency`/`syncActiveLatencyChip` in `app.js` to delegate through `latencyViewController`, and preserved viewport-safe chip updates by injecting `preserveViewportDuringUiMutation` + `setActivityChip` dependencies. Added direct runtime-helper tests for controller behavior (`setChatLatency` active update, `syncActiveLatencyChip` placeholder/restore).
- R25 follow-up 7 (2026-03-29): Extracted viewport-stability mutation helpers (`runAfterUiMutation`, `preserveViewportDuringUiMutation`) from `static/app.js` into `static/composer_viewport_helpers.js`, rewired `app.js` through controller delegates with dynamic getters (`activeChatId`, scroll bookkeeping maps, jump-visibility callback), and expanded direct Node coverage for RAF/timeout scheduling plus pinned-bottom viewport preservation behavior.
- R25 follow-up 6 (2026-03-29): Extracted render-trace state/toggle/logging helpers from `static/app.js` into `static/render_trace_helpers.js`, rewired `app.js` via a dedicated controller (`syncRenderTraceBadge`, toggle handler, trace logging), and added helper-specific Node tests plus template/version/no-store route metadata coverage for the new static asset.
- R25 follow-up 5 (2026-03-29): Extracted startup/event-binding shell glue from `static/app.js` into `static/startup_bindings_helpers.js` (core event listeners, async action button binding, shell/settings/dev-auth lifecycle wiring), rewired `app.js` through thin delegates + startup installers, and added direct Node helper contract coverage with route/template/static no-store assertions for the new helper asset.
- R26 (2026-03-28): Moved stream lifecycle helpers (`hydrateChatAfterGracefulResumeCompletion`, `consumeStreamWithReconnect`) into `static/stream_controller.js`, wired `app.js` through controller delegates, and added direct Node contract tests plus guard coverage for the consolidated send/resume/hydration flow.
- R24 (2026-03-28): Removed secret-bearing dev-auth query-string bootstrap, replaced prompt-based desktop auth with an in-app modal, and redacted sensitive query params from debug request logging; live browser verification confirmed desktop sign-in works without secrets appearing in request paths.
- R6 follow-up 3 (2026-03-28): Extended `runtime.incident_snapshot` with rolling `5m/15m/60m` rate windows (`runtime` retry/dead-letter + `terminal` done/error), terminal age quantiles (`median`, `p95`), and a lightweight `severity_hint` (`level` + `reason`) for fast triage; added route/event-broker regressions and README operator guidance.
- R6 follow-up 2 (2026-03-28): Added operator-facing `runtime.incident_snapshot` diagnostics to `/api/runtime/status` (worker health + terminal-event rollup + deduped recent error messages), added runtime/event-broker regressions, and expanded README troubleshooting docs.
- R22 (2026-03-28): Extracted interaction orchestration (composer Enter-submit shortcut + selection-quote controller/event wiring) from `static/app.js` into new `static/interaction_helpers.js`, kept behavior-preserving delegates in `app.js`, and added direct Node contract tests plus template/version/no-store route coverage.
- R6 follow-up (2026-03-28): Added operator-facing queue-recovery diagnostics to `/api/runtime/status` (`startup_recovered_running_total`, `startup_clamped_exhausted_total`, `preclaim_dead_letter_total`) and documented runtime-status troubleshooting workflow in README for future incidents.
- R19 (2026-03-28): Extracted desktop keyboard navigation/focus-control helpers from `static/app.js` into `static/keyboard_shortcuts_helpers.js`, rewired `app.js` to delegate through thin wrappers, and added dedicated Node helper tests plus asset/version/cache-route wiring coverage.
- R21 (2026-03-28): Extended frontend helper-module split contract checks in route/template tests to ensure script ordering + cache semantics remain stable as helper modules are introduced.
- R20 (2026-03-28): Stabilized no-store/version wiring for helper JS static assets by centralizing helper-version injection through `server_public_routes.py` and static asset cache policy assertions.
- R23 (2026-03-28): Added cross-layer regression sweep for frontend helper extraction path (Python route tests + Node helper suites) to prevent behavior drift during ongoing app.js decomposition.
- R14 (2026-03-28): Decomposed `store_jobs.py` lifecycle internals into focused helper modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`) while preserving `SessionStore` public method contracts and SQL behavior.
- R17 (2026-03-28): Added direct contract/regression suites for extracted modules (`tests/test_hermes_client_bootstrap.py`, `tests/test_job_runtime_events.py`, `tests/test_job_runtime_chat_job.py`, `tests/test_server_startup.py`, `tests/test_server_public_routes.py`) to prevent silent drift.
- R13 (2026-03-28): Split runtime event-bus responsibilities into a dedicated `JobEventBroker` (`job_runtime_events.py`) and wired `JobRuntime` through compatibility-preserving delegation/proxy attributes for monkeypatch/test contract stability.
- R12 (2026-03-28): Extracted Hermes client bootstrap/config parsing logic into `hermes_client_bootstrap.py` and routed `HermesClient` config-loading/routing methods through the dedicated bootstrap layer.
- R11 (2026-03-28): Extracted server startup diagnostics and public-route registration into `server_startup.py` and `server_public_routes.py`, reducing `server.py` composition blast radius while preserving entrypoints.
- R10 (2026-03-27): Expanded module-level transport/shim regressions with explicit tests for persistent worker exception wrapping, shim logger proxy behavior under close-failure cleanup, and shim subprocess module replacement contract.
- R9 (2026-03-27): Removed duplicated direct-agent tool-display formatting drift by making the parent process own `display` rendering from `tool_name`/`preview`/`args`; child runner now emits normalized tool payload only.
- R8 (2026-03-27): Added persistent-agent watchdog timeout around `event_queue.get(...)` in `_stream_via_persistent_agent` with deterministic `HermesClientError` timeout messaging including session context.
- R7 (2026-03-27): Split `hermes_client_agent.py` into focused modules (`hermes_client_agent_direct.py`, `hermes_client_agent_persistent.py`) and kept a compatibility composition shim for monkeypatch surfaces (`logger`, `subprocess`) and existing imports.

## Archived completed backlog items

- [x] R47: Split `static/startup_bindings_helpers.js` into focused startup-binding / bootstrap / watchdog controllers
 - Status: done
 - Severity: High
 - Scope/files: `static/startup_bindings_helpers.js`, `static/app.js`, `tests/startup_bindings_helpers.test.mjs`, and any route/meta guard coverage that asserts startup helper ownership
 - Why it matters: `static/startup_bindings_helpers.js` was still a regrown frontend monolith centered on one large `createController(...)` body that mixed transcript scroll/read-marker behavior, action-button wiring, shell/settings modal lifecycle, bootstrap orchestration, Telegram WebApp readiness, boot-metric reporting, pending-stream snapshot restoration, and pending-completion watchdog logic.
 - Proposed change: Split the module into focused internal controllers for transcript/shell event bindings, startup/bootstrap orchestration, and pending/watchdog refresh behavior while preserving the exported `createController(...)` facade and `static/app.js` contract.
 - Validation steps:
   - `node --check static/startup_bindings_helpers.js tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs static/app.js`
   - `node --test tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "startup_bindings or bootstrap"`
 - Notes/dependencies: Kept template/script-order stability intact; no new helper asset files were introduced.
 - Execution result: Completed 2026-04-11. `static/startup_bindings_helpers.js` now composes `createTranscriptBindingsController(...)`, `createActionBindingsController(...)`, `createStartupBootstrapController(...)`, `createShellModalController(...)`, and `createPendingWatchdogController(...)` beneath the stable exported `createController(...)` facade. Added direct export coverage in `tests/startup_bindings_helpers.test.mjs` and kept the existing behavior/delegation suites green. Validation: `node --check static/startup_bindings_helpers.js tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs static/app.js` (passed), `node --test tests/startup_bindings_helpers.test.mjs tests/startup_bindings_app_delegation.test.mjs` (`24 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "startup_bindings or bootstrap"` (`5 passed, 63 deselected`).

- [x] R48: Finish direct coverage for the Telegram unread-notification config/store/schema seam
 - Status: done
 - Severity: High
 - Scope/files: `miniapp_telegram_notifications.py`, `miniapp_config.py`, `store_chats.py`, `store_schema.py`, and focused tests under `tests/`
 - Why it matters: The remaining unread-notification risk had moved out of the sender itself and into the cross-layer seam: env parsing for `MINI_APP_TELEGRAM_NOTIFICATION_TIMEOUT_SECONDS`, store-level persistence for `get/set_telegram_unread_notifications_enabled(...)`, and legacy schema migration for `user_preferences.telegram_unread_notifications_enabled`.
 - Proposed change: Keep the sender/text tests, then add focused config/store/schema regressions for timeout env parsing, unread-preference getter/setter persistence, and migration coverage for the `user_preferences` preference column.
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile miniapp_telegram_notifications.py miniapp_config.py store_chats.py store_schema.py tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py -k "telegram_unread"`
 - Notes/dependencies: Kept tests network-free by mocking `requests.post`; used focused new test files instead of expanding the large generic store/config suites further.
 - Execution result: Completed 2026-04-11. Added `tests/test_config_telegram_notifications.py` for timeout env parsing, added `tests/test_store_telegram_notifications.py` for unread-preference persistence plus legacy `user_preferences` migration coverage, and refreshed `tests/test_miniapp_telegram_notifications.py` so the direct notifier assertions match the current active+visibly-open suppression contract. Validation: `source .venv/bin/activate && python -m py_compile miniapp_telegram_notifications.py miniapp_config.py store_chats.py store_schema.py tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py` (passed), `source .venv/bin/activate && python -m pytest -q tests/test_miniapp_telegram_notifications.py tests/test_config_telegram_notifications.py tests/test_store_telegram_notifications.py` (`10 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py -k "telegram_unread"` (`3 passed, 19 deselected`).

- [x] R40: Split `routes_auth.py` into thin route adapters plus auth/bootstrap service helpers
 - Status: done
 - Severity: High
 - Scope/files: `routes_auth.py` (+ new helper/service module[s]), `tests/test_routes_auth.py`, and any dynamic getter wiring in `server.py`/route registration touched by the split
 - Why it matters: `routes_auth.py` is back up to 269 LOC and now mixes request parsing, auth success/bootstrap payload shaping, runtime-pending history augmentation, dev-auth logic, logout-all, and skin preference handling. The current dirty diff shows active growth here, so its route/service ownership boundary is the highest-signal backend refactor seam in the mini app right now.
 - Proposed change: Extract bootstrap/auth-success response shaping plus pending-history augmentation and dev-auth/session helpers into focused service/helper module(s), leaving `register_auth_routes(...)` as request parsing, guard checks, and delegation. Preserve existing getter-injection and monkeypatch-friendly behavior.
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile routes_auth.py routes_auth_service.py tests/test_routes_auth.py tests/test_routes_auth_service.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py tests/test_routes_auth_service.py`
 - Notes/dependencies: Coordinate carefully with dev-auth enablement/env semantics already covered by `tests/server_test_utils.py`; avoid mixing unrelated startup/server config changes into this pass.
 - Execution result: Completed 2026-04-10. Added `routes_auth_service.py` with `AuthBootstrapService`, which now owns allow-empty parsing, runtime-pending history augmentation, active/default chat bootstrap state resolution, turn serialization with file-ref enrichment, auth-success payload shaping, and dev-user construction. `routes_auth.py` is down to a thin 152-line route-registration layer focused on request parsing, cookie wiring, guard checks, and delegation. Validation: `source .venv/bin/activate && python -m py_compile routes_auth.py routes_auth_service.py tests/test_routes_auth.py tests/test_routes_auth_service.py` (passed) and `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py tests/test_routes_auth_service.py` (`23 passed`).

- [x] R41: Split `static/bootstrap_auth_helpers.js` into focused auth-request / dev-auth-ui / bootstrap-apply helpers
 - Status: done
 - Severity: High
 - Scope/files: `static/bootstrap_auth_helpers.js` (+ helper slices if needed), `tests/bootstrap_auth_helpers.test.mjs`, `tests/test_routes_meta.py`
 - Why it matters: `static/bootstrap_auth_helpers.js` is now 575 LOC and co-locates request utilities (`authPayload`, `safeReadJson`, `apiPost`), error summarization, dev-auth UI state/default storage, bootstrap retry/version-reload policy, and full auth-bootstrap application orchestration. It has become the next frontend helper monolith after the completed stream/runtime splits.
 - Proposed change: Separate request/bootstrap transport utilities from dev-auth UI/default persistence and auth-bootstrap application orchestration behind focused helper slices or subcontrollers while preserving the current `createController(...)` facade and script-order stability.
 - Validation steps:
   - `node --check static/bootstrap_auth_helpers.js tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs`
   - `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "bootstrap or auth"`
 - Notes/dependencies: Keep `static/app.js` compatibility stable; route/meta asset assertions will need updates if new helper files are introduced.
 - Execution result: Completed 2026-04-10. `static/bootstrap_auth_helpers.js` now composes focused request, dev-auth UI, bootstrap-apply, and dev-auth sign-in controllers around the shared identity/delay helpers while preserving the exported `createController(...)` facade and all existing `static/app.js` delegates. Added/kept direct regression coverage for helper ownership plus pinned-chat bootstrap hydration without introducing any new helper asset files. Validation: `node --check static/bootstrap_auth_helpers.js tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs` (passed), `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_pinned_chats.test.mjs` (`19 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "bootstrap or auth"` (`9 passed, 59 deselected`).

- [x] R42: Split `static/chat_history_helpers.js` into focused history/read-sync/open/meta helpers
 - Status: done
 - Severity: Medium
 - Scope/files: `static/chat_history_helpers.js` (+ helper slices if needed), `tests/chat_history_helpers.test.mjs`, related app-delegation/meta coverage
 - Why it matters: `static/chat_history_helpers.js` is now 872 LOC and combines active-chat metadata, open/hydrate flows, unread/mark-read thresholds, pending snapshot reconciliation, system-message rendering, and active-view scheduling. It is the highest-blast-radius remaining frontend helper module tied to transcript correctness.
 - Proposed change: Extract focused helper slices or subcontrollers for (a) active-chat meta, (b) open/hydrate/sync flows, (c) unread/read-threshold state, and (d) local system-message / pending-assistant mutation helpers, while keeping the current controller facade app-compatible.
 - Validation steps:
   - `node --check static/chat_history_helpers.js tests/chat_history_helpers.test.mjs`
   - `node --test tests/chat_history_helpers.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "chat_history or active chat"`
 - Notes/dependencies: Coordinate with `static/render_trace_history_helpers.js` and `static/stream_controller.js` contracts so hydrate/render/resume ownership stays explicit and non-duplicated.
 - Execution result: Completed 2026-04-10. `static/chat_history_helpers.js` now composes focused `createReadSyncController(...)`, `createLocalMutationController(...)`, and `createHistoryOpenController(...)` helpers beneath the existing exported `createController(...)` facade, while leaving `createMetaController(...)` as the dedicated owner of active-chat metadata + deferred non-critical tab-switch work. Validation: `node --check static/chat_history_helpers.js tests/chat_history_test_harness.mjs tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (passed), `node --test tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (`65 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py::test_chat_history_helpers_static_asset_is_no_store tests/test_routes_meta.py::test_desktop_dev_auth_bootstrap_guards_present` (`2 passed`).

- [x] R43: Decompose `tests/chat_history_helpers.test.mjs` into focused suites aligned with the chat-history seams
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/chat_history_helpers.test.mjs` → focused suites/harness files aligned with chat-history helper ownership
 - Why it matters: `tests/chat_history_helpers.test.mjs` is now 1722 LOC, making it the next major frontend regression monolith after the stream-controller split. Its breadth makes future chat-history refactors harder to reason about and slows targeted iteration.
 - Proposed change: Split the monolith into focused suites for meta/deferred activation, open/hydrate/resume flows, unread/mark-read threshold behavior, and local mutation/system-message helpers, with a shared harness for common DOM/history fixtures.
 - Validation steps:
   - `node --check tests/chat_history_helpers*.mjs`
   - `node --test <new focused chat-history suite files>`
 - Notes/dependencies: Do R42 first so the production ownership seams are stable before splitting the tests around them.
 - Execution result: Completed 2026-04-10. Replaced the old `tests/chat_history_helpers.test.mjs` monolith with `tests/chat_history_test_harness.mjs`, `tests/chat_history_meta_controller.test.mjs`, `tests/chat_history_open_hydrate.test.mjs`, `tests/chat_history_read_mutation.test.mjs`, and `tests/chat_history_visibility_sync.test.mjs`, while keeping `tests/chat_history_app_delegation.test.mjs` as the app-wrapper contract suite. Validation: `node --check static/chat_history_helpers.js tests/chat_history_test_harness.mjs tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (passed) and `node --test tests/chat_history_meta_controller.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/chat_history_app_delegation.test.mjs` (`65 passed`).

- [x] R36: Finish replay-cursor commit hardening in `static/stream_controller.js`
 - Status: done
 - Severity: High
 - Scope/files: `static/stream_controller.js`, `tests/stream_controller.test.mjs`, `tests/stream_controller_app_delegation.test.mjs`
 - Why it matters: The current dirty diff fixes a real correctness seam: replay cursor persistence used to advance as soon as an event was parsed, even if local tool/assistant patching threw before the UI/history state was actually updated. That can cause resumed streams to skip events that never fully applied locally.
 - Proposed change: Keep the new `commitProcessedStreamEvent(...)` contract, audit all non-terminal event paths so cursor persistence only happens after successful local processing, and add focused regressions for failure-before-commit and terminal-clear semantics.
 - Validation steps:
   - `node --check static/stream_controller.js tests/stream_controller.test.mjs`
   - `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs`
 - Notes/dependencies: This work is already partially in flight in the working tree; complete/commit it before larger stream-controller reshaping resumes.
 - Execution result: Completed 2026-04-09. `createStreamSessionController(...)` now owns explicit `commitProcessedStreamEvent(chatId, payload)` persistence, `shouldSkipReplayedEvent(...)` is a pure replay gate again, and `consumeStreamResponse(...)` persists `_event_id` only after non-terminal tool/chunk/meta frames finish local processing. Terminal `done`/`error` flows continue to clear the cursor instead of persisting terminal event IDs. Added focused regressions for failure-before-commit and non-terminal-only persistence, and refreshed hardening guard coverage for the new commit helper contract.

- [x] R37: Split `job_runtime.py` into focused lifecycle/claim/diagnostics helpers
 - Status: done
 - Severity: High
 - Scope/files: `job_runtime.py` (+ new helper modules), `tests/test_routes_jobs_runtime.py`, `tests/test_job_runtime_events.py`, `tests/test_job_runtime_chat_job.py`
 - Why it matters: `job_runtime.py` is still a 945-line miniapp backend monolith combining queue claim flow, retry/dead-letter transitions, worker orchestration, telemetry, and runtime diagnostics. It is the highest-blast-radius backend file remaining after the route/service splits.
 - Proposed change: Extract focused helpers for claim/process loop control, terminal/retry bookkeeping, and diagnostics snapshot shaping while preserving the existing `JobRuntime` public contract and monkeypatch-friendly tests.
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile job_runtime.py tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py`
 - Notes/dependencies: Keep `job_runtime_worker_launcher.py` ownership stable; avoid mixing launcher refactors into this pass.
 - Execution result: Completed 2026-04-09. `job_runtime.py` now delegates its remaining runner-state and best-effort/support helper bands into `job_runtime_runner_state.py` and `job_runtime_support.py`, leaving `JobRuntime` with a thinner compatibility facade over already-extracted lifecycle/loop/diagnostics modules. The public/monkeypatch method names (`_try_start_job_runner`, `_finish_job_runner`, `_terminate_job_children`, `_touch_job_best_effort`, `_safe_add_system_message`, `best_effort_failure_counts`, `_fd_metrics`, etc.) were preserved as delegates, and `tests/test_broad_exception_policy.py` now guards the broad exception handlers in the new helper modules. Validation caveat: the direct helper-surface regressions and compile checks passed, but the larger combined `tests/test_routes_jobs_runtime.py` run still shows pre-existing/orthogonal failures in subprocess launcher limit expectations and open-job claim races, so this item was validated with isolated runtime helper regressions plus `tests/test_job_runtime_events.py`, `tests/test_job_runtime_chat_job.py`, and policy coverage rather than the noisy full file run.

- [x] R38: Split `static/runtime_helpers.js` into focused runtime helper slices
 - Status: done
 - Severity: Medium
 - Scope/files: `static/runtime_helpers.js` (+ new helper slices if needed), `tests/frontend_runtime.test.mjs`, `tests/runtime_app_delegation.test.mjs`, `tests/test_routes_meta.py`
 - Why it matters: `static/runtime_helpers.js` is now 969 LOC and still co-locates latency chips, activity chips, unread/haptic flows, storage persistence, and assorted runtime formatting/controller glue. The module has become the next frontend helper monolith.
 - Proposed change: Separate runtime latency/activity state from unread/haptic/storage concerns behind focused helper modules or subcontrollers while preserving the current app-facing facade and script-order stability.
 - Validation steps:
   - `node --check static/runtime_helpers.js tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs`
   - `node --test tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "runtime or latency or unread"`
 - Notes/dependencies: Coordinate with existing route-meta static asset assertions if the split introduces new files.
 - Execution result: Verified completed 2026-04-09. `static/runtime_helpers.js` is already a thin facade that resolves and re-exports `static/runtime_unread_helpers.js`, `static/runtime_latency_helpers.js`, and `static/runtime_history_helpers.js`, while `tests/runtime_app_delegation.test.mjs` already guards the app-side controller composition/delegation into `createLatencyPersistenceController(...)`, `createLatencyController(...)`, and `createHapticUnreadController(...)`. Route/meta asset assertions already cover the split runtime helper assets and script-order plumbing. Validation: `node --check static/runtime_helpers.js tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs`, `node --test tests/frontend_runtime.test.mjs tests/runtime_app_delegation.test.mjs` (43 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "runtime or latency or unread"` (5 passed, 57 deselected).

- [x] R39: Decompose `tests/stream_controller.test.mjs` into focused suites aligned with the new subcontrollers
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/stream_controller.test.mjs` → `tests/stream_controller_test_harness.mjs`, `tests/stream_controller_policy_session.test.mjs`, `tests/stream_controller_response.test.mjs`, `tests/stream_controller_resume_finalize.test.mjs`, `tests/stream_controller_tool_trace.test.mjs`
 - Why it matters: The production module was split into `createStreamSessionController(...)`, `createStreamTranscriptController(...)`, and `createStreamLifecycleController(...)`, but the main JS regression file was still a 1734-line monolith. That slowed iteration and made future stream-controller refactors harder to reason about.
 - Proposed change: Split the test monolith into focused suites (session/replay, transcript consumption, lifecycle/send-resume, tool-trace helpers) while keeping a shared harness for the public controller surface.
 - Validation steps:
   - `node --check tests/stream_controller_test_harness.mjs tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`
   - `node --test tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`
 - Notes/dependencies: R36 replay-cursor hardening was completed first, so the session/replay contract was stable before the split.
 - Execution result: Completed 2026-04-09. Removed the old `tests/stream_controller.test.mjs` monolith, introduced `tests/stream_controller_test_harness.mjs` for shared stream-controller fixtures/SSE helpers, split direct policy/session/lifecycle tests into `tests/stream_controller_policy_session.test.mjs`, split stream-consumption regressions into `tests/stream_controller_response.test.mjs`, split hydration/reconnect/finalize coverage into `tests/stream_controller_resume_finalize.test.mjs`, and moved tool-trace helper coverage into `tests/stream_controller_tool_trace.test.mjs`. Existing app-side wrapper contract coverage in `tests/stream_controller_app_delegation.test.mjs` remained unchanged. Validation: `node --check ...` on the new split files plus `tests/stream_controller_app_delegation.test.mjs`; `node --test ...` on those same files → `48 passed`.

- [x] R1: Harden direct-agent subprocess FD lifecycle
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client_agent.py`, `tests/test_hermes_client.py`
 - Why it matters: `_stream_via_agent` uses `Popen(..., stdin/stdout/stderr=PIPE)` and kills/waits process in `finally`, but does not explicitly close `process.stdout`/`process.stderr` handles after reader completion. Under repeated retries/failures this can contribute to transient FD pressure and amplify `[Errno 24] Too many open files` incidents.
 - Proposed change: Add deterministic handle cleanup (`stdin`/`stdout`/`stderr`) with idempotent close helpers after stream loop exits and on timeout/error paths; ensure reader-thread completion ordering does not retain open pipes.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Add/execute targeted regression that asserts pipe close methods are called in success + timeout paths.
 - Notes/dependencies: Should land before additional retry/worker hardening so FD behavior is stable.
 - Execution result: Completed 2026-03-27. Added `_safe_close_stream(...)` idempotent cleanup helper in `_stream_via_agent`, explicit teardown closes for stdin/stdout/stderr, and regression assertions for handle close behavior in success + timeout + non-zero exit tests.

- [x] R2: Repair broad-exception policy regression in `job_runtime.py`
 - Status: done
 - Severity: High
 - Scope/files: `job_runtime.py`, `tests/test_broad_exception_policy.py`
 - Why it matters: Current quality gate fails because newly introduced broad handlers in `_fd_metrics` are missing the required `broad-except-policy:` annotation.
 - Proposed change: Add policy-tagged justification comments (and logging/intentional-no-log declarations) for the two `except Exception` blocks.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py`
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py`
 - Notes/dependencies: Unblocks CI/static gate confidence for future refactors.
 - Execution result: Completed 2026-03-27. Added required `broad-except-policy:` annotations (with intentional-no-log rationale) on `_fd_metrics` broad handlers while preserving best-effort fallback behavior.

- [x] R3: Extend broad-exception policy coverage to `hermes_client_agent.py`
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_broad_exception_policy.py`, `hermes_client_agent.py`
 - Why it matters: `hermes_client_agent.py` contains multiple broad handlers (`except Exception`) that are currently outside the static policy gate. This creates a blind spot where new broad catches can regress observability standards.
 - Proposed change: Include `hermes_client_agent.py` in `TARGET_FILES`, annotate existing broad handlers with policy rationale, and ensure each handler logs or explicitly declares intentional-no-log.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py`
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
 - Notes/dependencies: Coordinate with R1 so cleanup-path handlers get final policy text once behavior is settled.
 - Execution result: Completed 2026-03-27. `TARGET_FILES` in policy test now includes `hermes_client_agent.py`; broad handlers are now policy-annotated and satisfy logging/intentional-no-log policy checks.

- [x] R4: Add runtime-level retry-exhaustion regression coverage (not only store-level)
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_routes_jobs_runtime.py`, `job_runtime.py`
 - Why it matters: Store-level tests now guard bounded attempts and dead-letter transitions, but runtime worker behavior still lacks a direct regression for user-facing exhaustion messaging and retry/dead telemetry under repeated `JobRetryableError` paths.
 - Proposed change: Add runtime test harness that forces retryable failures across attempts and asserts: (a) bounded attempt text, (b) terminal dead-letter behavior, (c) retrying stops at max.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py`
 - Notes/dependencies: Complements R1/R2 by proving end-to-end worker semantics.
 - Execution result: Completed 2026-03-27. Added `test_worker_retry_exhaustion_stops_at_max_and_surfaces_terminal_error` to assert retry exhaustion path semantics (no retries past max, dead-letter transition, bounded terminal messaging/events).

- [x] R5: Enforce `chat_jobs` invariants at schema level
 - Status: done
 - Severity: Medium
 - Scope/files: `store_schema.py`, migration path, `tests/test_store.py`
 - Why it matters: Retry logic now enforces limits in code, but DB still allows invalid states (`attempts < 0`, `max_attempts <= 0`, arbitrary status strings). Schema-level checks reduce corruption risk from manual edits/tooling mistakes and make recovery behavior predictable.
 - Proposed change: Introduce migration-safe check constraints (or equivalent guard/migration table rewrite for SQLite) for status domain and numeric bounds; backfill/coerce invalid rows during migration.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_store.py`
   - Migration regression on pre-existing DB fixture with invalid values.
 - Notes/dependencies: Design carefully for SQLite ALTER TABLE limitations.
 - Execution result: Completed 2026-03-27. Added `chat_jobs` CHECK invariants, legacy-row normalization, and migration-safe table rebuild path in `store_schema.py`; added `test_store_init_migrates_chat_jobs_invariants_and_normalizes_invalid_rows`.

- [x] R6: Expose FD pressure + retry/dead counters in runtime diagnostics endpoint
 - Status: done
 - Severity: Medium
 - Scope/files: `server.py`, `job_runtime.py`, `routes_jobs_runtime.py`, `tests/test_routes_jobs_runtime.py`
 - Why it matters: FD metrics are currently only in logs; operators lack fast API-visible health signals during incidents.
 - Proposed change: Add lightweight runtime diagnostics fields (e.g., `fd_open`, `fd_limit_soft`, recent dead-letter/retry counters) to `/api/runtime/status` while keeping sensitive data redacted.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_config.py`
 - Notes/dependencies: Should remain best-effort and non-fatal if `/proc`/`resource` are unavailable.
 - Execution result: Completed 2026-03-27. `register_jobs_runtime_routes` now uses a dynamic `runtime_getter`; `/api/runtime/status` includes `runtime` diagnostics payload, and regression coverage verifies exposed counters/FD fields.
 - Follow-up hardening (2026-03-28): Added queue recovery/claim diagnostics surfaced via `runtime.queue_diagnostics` (mirrored as flat aliases) with counters for `startup_recovered_running_total`, `startup_clamped_exhausted_total`, and `preclaim_dead_letter_total`; added store/runtime regression assertions and README operator docs for future incident triage.
 - Follow-up validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_streaming_hardening_guards.py` → `55 passed in 1.86s`.
   - `node --test tests/stream_controller.test.mjs tests/frontend_runtime.test.mjs tests/stream_state_helpers.test.mjs tests/shared_utils.test.mjs` → `22 passed, 0 failed`.
 - Follow-up 2 execution result (2026-03-28): Added `runtime.incident_snapshot` in `JobRuntime.runtime_diagnostics()` with worker health and terminal-event rollup (`JobEventBroker.terminal_rollup`), plus route-level assertion coverage and event-broker unit coverage.
 - Follow-up 2 validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py` → `23 passed in 1.57s`.
 - Follow-up 3 execution result (2026-03-28): Added rolling-window incident telemetry with bounded in-memory timelines (`terminal` done/error windows + runtime retry/dead windows), exposed terminal age quantiles (`median`, `p95`), and added `severity_hint` (`level`, `reason`) in `runtime.incident_snapshot`.
 - Follow-up 3 validation (2026-03-28):
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_job_runtime_events.py` → `23 passed in 1.55s`.

- [x] R7: Split `hermes_client_agent.py` into focused modules
 - Status: done
 - Severity: Low
 - Scope/files: `hermes_client_agent.py` (+ new helper modules), `tests/test_hermes_client.py`
 - Why it matters: Single 500+ line module combines persistent runtime, direct subprocess streaming, formatting, and env wiring. This increases cognitive load and raises regression risk for future hardening.
 - Proposed change: Extract direct-agent subprocess orchestration and persistent-session runtime into separate internal modules with narrower responsibilities.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - `.venv/bin/python -m pytest -q` (full suite when host FD pressure is stable)
 - Notes/dependencies: Behavior-preserving refactor; prioritize after high/medium reliability items.
 - Execution result: Completed 2026-03-27. Extracted direct subprocess path into `hermes_client_agent_direct.py` and persistent runtime path into `hermes_client_agent_persistent.py`; kept `hermes_client_agent.py` as a backward-compatible composition shim and preserved monkeypatch compatibility for `logger`/`subprocess` surfaces.

- [x] R8: Add persistent-agent watchdog timeout and queue-drain hardening
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client_agent_persistent.py`, `tests/test_hermes_client.py`
 - Why it matters: `_stream_via_persistent_agent` currently blocks on `event_queue.get()` without timeout. If the worker thread stalls before posting `end` (e.g., dependency deadlock/hung call path), request handling can hang indefinitely.
 - Proposed change: Add bounded wait loop using `self.timeout_seconds` (or explicit persistent timeout knob), emit terminal timeout error with session context, and ensure worker completion path is still drained safely.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "persistent and (timeout or watchdog or stream_events)"`
   - Add regression test that simulates worker non-termination and asserts deterministic `HermesClientError` timeout.
 - Notes/dependencies: Coordinate with existing fallback-to-direct behavior so timeout surfaces remain user-readable and non-duplicative.
 - Execution result: Completed 2026-03-27. `_stream_via_persistent_agent` now uses bounded `event_queue.get(timeout=...)` polling with `self.timeout_seconds` watchdog and raises deterministic timeout errors including session_id. Added `test_persistent_agent_stream_times_out_when_worker_stalls`.

- [x] R9: De-duplicate direct-agent tool-progress formatter and emoji map
 - Status: done
 - Severity: Medium
 - Scope/files: `hermes_client_agent_direct.py`, optional helper module, `tests/test_hermes_client.py`
 - Why it matters: `_format_tool_progress()` and `_agent_runner_script()` each carry near-identical tool emoji maps/formatting logic. This creates drift risk and inconsistent UX when one path changes without the other.
 - Proposed change: Consolidate mapping/format rules in one canonical helper and have both in-process formatter and child runner consume the same serialized map/constants.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "tool_progress or stream_via_agent"`
   - Add snapshot-style assertion that parent/child formatter outputs stay equivalent for representative tool/progress payloads.
 - Notes/dependencies: Keep behavior backward compatible for current tool-progress modes (`all`, `new`, `verbose`, `off`).
 - Execution result: Completed 2026-03-27. Introduced module-level `_TOOL_PROGRESS_EMOJIS` canonical map, removed duplicated child-runner formatter/map, and made parent `_stream_via_agent` compute `display` from normalized `tool_name`/`preview`/`args`. Added tests validating script de-duplication and parent-side tool display formatting.

- [x] R10: Add explicit module-level regression tests for split direct/persistent transports + compatibility shim
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_hermes_client.py` (or split transport-specific test modules), `hermes_client_agent.py`
 - Why it matters: After the split, most coverage still drives behavior through higher-level client flows; there is limited direct contract testing for shim export/proxy behavior and isolated transport failure edges.
 - Proposed change: Add focused tests for shim proxy surfaces (`logger`, `subprocess`), direct transport teardown invariants, and persistent transport checkpoint/exception propagation edge cases.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Optional: split into `tests/test_hermes_client_agent_direct.py` / `tests/test_hermes_client_agent_persistent.py` and run both directly.
 - Notes/dependencies: Can be executed in parallel with R9 if helper extraction lands first.
 - Execution result: Completed 2026-03-27. Added focused regression tests for persistent worker exception wrapping (`test_persistent_agent_wraps_worker_exception_as_hermes_client_error`), shim logger proxy behavior in direct transport cleanup (`test_shim_logger_proxy_is_used_by_direct_module_cleanup`), and shim subprocess replacement contract (`test_shim_subprocess_proxy_supports_module_replacement`).

- [x] R11: Split `server.py` app bootstrap/composition into focused modules
 - Status: done
 - Severity: High
 - Scope/files: `server.py`, `app_factory.py`, new helper module(s) (e.g., `server_bootstrap.py`, `startup_diagnostics.py`), related route wiring tests.
 - Why it matters: `server.py` is ~545 lines and currently mixes env/config constants, auth/session wrappers, startup diagnostics assembly, Flask hook registration, static/public handlers, and route wiring. This monolith increases blast radius for changes and makes behavior-preserving review difficult.
 - Proposed change: Extract (1) startup diagnostics payload/logging, (2) auth/request helper adapters, and (3) blueprint registration/bootstrap orchestration into dedicated modules with explicit data contracts; keep `create_app()`/entrypoint stable.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_routes_auth.py`
   - `.venv/bin/python -m pytest -q tests/test_config.py`
 - Notes/dependencies: Preserve dynamic getter/lambda injection pattern already used for monkeypatch-safe route tests.
 - Execution result: Completed 2026-03-28. Extracted startup diagnostics into `server_startup.py` and public-route registration into `server_public_routes.py`; `server.py` now delegates both concerns while keeping `create_app()` and route wiring entrypoints stable.

- [x] R12: Decompose `hermes_client.py` into config/bootstrap vs transport orchestration
 - Status: done
 - Severity: Medium
 - Scope/files: `hermes_client.py`, potential new modules (e.g., `hermes_client_config.py`, `hermes_client_diagnostics.py`), `tests/test_hermes_client.py`.
 - Why it matters: `hermes_client.py` (~526 lines) combines env/config parsing, auth/config file parsing, diagnostics/health reporting, and runtime transport routing/fallback logic. This coupling makes transport changes risky and obscures regression boundaries.
 - Proposed change: Isolate pure configuration + diagnostics loading into dedicated helpers/dataclasses and leave `HermesClient.stream_events()` transport decision tree as a narrower orchestration layer.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py`
   - Add focused tests for config-loader helpers independent from transport mocks.
 - Notes/dependencies: Coordinate with existing split modules (`hermes_client_agent_direct.py`, `hermes_client_agent_persistent.py`) to avoid reintroducing shim drift.
 - Execution result: Completed 2026-03-28. Added `hermes_client_bootstrap.py` and routed `HermesClient` config/bootstrap methods (`_resolve_agent_routing`, `_load_*_from_config`, `_load_active_provider_from_auth_store`) through the extracted bootstrap component; follow-up cleanup removed redundant in-class config/bootstrap bodies while preserving logging/diagnostics semantics and reduced `hermes_client.py` to 354 lines.

- [x] R13: Break `JobRuntime` monolith into worker lifecycle + event bus components
 - Status: done
 - Severity: Medium
 - Scope/files: `job_runtime.py`, potential internal helpers (event pub/sub, retry/dead-letter handling, keepalive/touch logic), `tests/test_routes_jobs_runtime.py`.
 - Why it matters: `job_runtime.py` (~746 lines) mixes event queue management, watchdog/worker loops, stream consumption, retry/dead-letter semantics, truncation/chunking, and telemetry counters. The current shape raises defect risk when touching unrelated paths.
 - Proposed change: Extract cohesive units (e.g., event history broker, job execution runner, retry/dead-letter policy helper) behind small private interfaces while preserving `JobRuntime` public API.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_store.py`
   - Add targeted tests for extracted helper boundaries (event pruning + retry/dead transitions).
 - Notes/dependencies: Execute after R11/R12 to keep operational startup/routing stable while runtime internals are split.
 - Execution result: Completed 2026-03-28. Extracted event history/pub-sub/pruning into `job_runtime_events.py` (`JobEventBroker`) and updated `JobRuntime` to delegate event responsibilities through the broker while preserving compatibility shim attributes (`_event_lock`, `_event_history`, `_event_queues`, `_event_timestamps`); follow-up cleanup extracted chat-job stream execution into `job_runtime_chat_job.py` (`execute_chat_job`) and reduced `job_runtime.py` to 509 lines while preserving `run_chat_job()` API behavior.

- [x] R14: Decompose `store_jobs.py` into lifecycle-focused helpers (claim/retry/dead-letter/queries)
 - Status: done
 - Severity: High
 - Scope/files: `store_jobs.py`, potential new modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`), `tests/test_store.py`, `tests/test_routes_jobs_runtime.py`.
 - Why it matters: `store_jobs.py` is still 512 lines and mixes job claiming, retry backoff policy, dead-letter writes, stale cleanup, and API/state projection payload shaping. This coupling raises regression risk in a reliability-critical path.
 - Proposed change: Extract cohesive SQL clusters into private helpers/modules with explicit call contracts while preserving `SessionStore` public methods and transaction boundaries.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py`
   - Add focused tests for each extracted helper path: claim race/no-row update, retry scheduling vs dead-letter, stale cleanup reason mapping.
 - Notes/dependencies: Keep SQL semantics behavior-identical; do not alter retry timing policy in this pass.
 - Execution result: Completed 2026-03-28. Split store-job lifecycle SQL into helper modules (`store_jobs_claim.py`, `store_jobs_retry.py`, `store_jobs_queries.py`) and reduced `store_jobs.py` to orchestration wrappers + dead-letter insert primitive. Added stale cleanup reason regression `test_cleanup_stale_jobs_thread_missing_reason_mapping`; validated store/runtime job suites.

- [x] R15: Decompose `store_chats.py` into read/query vs mutating/cancellation responsibilities
 - Status: done
 - Severity: Medium
 - Scope/files: `store_chats.py`, potential helper modules (`store_chat_queries.py`, `store_chat_mutations.py`), `tests/test_store.py`, `tests/test_routes_chat.py`.
 - Why it matters: `store_chats.py` is 470 lines and currently combines hydration/query CTEs, mutation flows, archival/reopen, and chat-job cancellation side effects. This broad scope makes chat UX fixes riskier.
 - Proposed change: Isolate read models/query builders from mutation flows and extract shared cancellation/dead-letter side effects into dedicated internal helpers.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_chat.py`
  - Add focused regressions for `clear_chat`/`remove_chat` cancellation behavior and active-chat fallback invariants.
- Notes/dependencies: Coordinate with R14 so chat/job side effects remain single-sourced.
 - Execution result: Completed 2026-03-28. Extracted read/query and mutation/cancellation logic into `store_chat_queries.py` and `store_chat_mutations.py`, keeping `store_chats.py` as orchestration mixin surface. Added cancellation regression coverage (`test_clear_chat_cancels_open_jobs`) and validated with `pytest -q tests/test_store.py tests/test_routes_chat.py`.

- [x] R16: Finish `server.py` composition split by extracting request/auth adapter wiring
 - Status: done
 - Severity: Medium
 - Scope/files: `server.py`, new adapter module(s) (e.g., `server_request_adapters.py`), route registration tests.
 - Why it matters: `server.py` is still 460 lines and retains many thin wrappers (`_verify_*`, `_json_*`, `_sse_*`, token/session adapters) plus middleware/hook composition. This remains a high-blast-radius integration point.
 - Proposed change: Move adapter wrappers and request-context glue into dedicated helper module(s), leaving `server.py` as minimal bootstrap + blueprint assembly.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_chat.py tests/test_routes_jobs_runtime.py`
  - `.venv/bin/python -m pytest -q tests/test_config.py`
- Notes/dependencies: Preserve dynamic getter/lambda injection conventions for monkeypatch-based tests.
 - Execution result: Completed 2026-03-28. Added `server_request_adapters.py` and moved request/auth adapter wiring out of `server.py` while preserving monkeypatch-friendly dynamic call resolution (auth verification wrappers + lambda-based route/template hooks). Verified targeted suites: `pytest -q tests/test_routes_auth.py tests/test_routes_meta.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py tests/test_store.py` (all passing).

- [x] R17: Add direct contract tests for newly extracted modules to prevent hidden drift
 - Status: done
 - Severity: High
 - Scope/files: `tests/test_hermes_client_bootstrap.py` (new), `tests/test_job_runtime_events.py` (new), `tests/test_job_runtime_chat_job.py` (new), `tests/test_server_startup.py` (new), `tests/test_server_public_routes.py` (new).
 - Why it matters: Current coverage still primarily exercises these extractions indirectly via larger integration tests. There are currently no direct module-level tests referencing `hermes_client_bootstrap`, `job_runtime_events`, `job_runtime_chat_job`, `server_startup`, or `server_public_routes`, increasing silent drift risk.
 - Proposed change: Add focused contract tests around each extracted module’s API behavior (inputs/outputs, edge paths, monkeypatch contracts) so future refactors fail fast.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_hermes_client_bootstrap.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py tests/test_server_startup.py tests/test_server_public_routes.py`
   - `.venv/bin/python -m pytest -q tests/test_hermes_client.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py`
 - Notes/dependencies: Can run in parallel with R14/R15/R16 since this is additive coverage.
 - Execution result: Completed 2026-03-28. Added five direct module-level suites with focused contract assertions across bootstrap routing/config parsing, event broker replay/pruning/terminal delivery semantics, extracted chat-job execution behavior, startup diagnostics payload/log invariants, and public-route registration behavior.

- [x] R18: Introduce shared job-status/domain constants to reduce cross-module string drift
 - Status: done
 - Severity: Medium
 - Scope/files: `store_jobs.py`, `store_chats.py`, `job_runtime.py`, `routes_chat_stream.py`, `routes_jobs_runtime.py`, potential new `job_status.py` constants module, related tests.
 - Why it matters: Status domains (`queued/running/done/error/dead`) and open-job predicates are repeated across store/runtime/routes with inline string literals and slightly different predicates, increasing future drift risk.
 - Proposed change: Centralize status constants and common predicates (open/terminal/retryable sets) in one shared module and migrate call sites incrementally.
- Validation steps:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py`
  - Add focused tests asserting shared constants drive open/terminal checks consistently.
- Notes/dependencies: Keep SQL text readable; avoid over-abstracting query clauses.
 - Execution result: Completed 2026-03-28. Added shared status/event domain module `job_status.py` (status constants, open/terminal sets, SQL status-list helpers, and terminal event constants) and migrated store/runtime/route call sites (`store_jobs*.py`, `store_chat_mutations.py`, `job_runtime.py`, `routes_chat_stream.py`, `routes_jobs_runtime.py`) off ad-hoc literals while preserving response contracts. Validation: `pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py tests/test_job_runtime_chat_job.py tests/test_job_runtime_events.py` and full `pytest -q` (185 passed).

- [x] R19: Extract keyboard/navigation/focus-control helpers out of `static/app.js`
 - Status: done
 - Severity: Medium
 - Scope/files: `static/app.js`, `static/keyboard_shortcuts_helpers.js`, `templates/app.html`, `server_public_routes.py`, `server.py`, `tests/keyboard_shortcuts_helpers.test.mjs`, `tests/test_routes_meta.py`
 - Why it matters: `static/app.js` still carried a large high-churn block for desktop keyboard shortcuts and sticky-focus cleanup, increasing blast radius and making targeted regressions harder to isolate.
 - Proposed change: Move keyboard/focus handlers into a dedicated helper module and keep app-level wrappers thin/behavior-preserving.
 - Validation steps:
   - `node --test tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_routes_auth.py`
 - Notes/dependencies: Keep helper loaded before `app.js`; maintain no-store cache policy parity with other helper modules.
 - Execution result: Completed 2026-03-28. Added `HermesMiniappKeyboardShortcuts` helper module, rewired app delegates, added helper static asset template/version wiring, and validated Python + Node test suites.

- [x] R20: Wire helper-module asset versioning/cache behavior for keyboard split
 - Status: done
 - Severity: Low
 - Scope/files: `server_public_routes.py`, `templates/app.html`, `server.py`, `tests/test_routes_meta.py`
 - Why it matters: New helper modules must preserve asset version pinning and no-store semantics to avoid stale browser behavior during rapid refactor loops.
 - Proposed change: Add explicit template version key and no-store header coverage for `keyboard_shortcuts_helpers.js`.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py`
 - Execution result: Completed 2026-03-28. Added template/version injection + cache policy regression coverage for the new helper asset.

- [x] R21: Expand helper-split contract assertions for frontend module extraction
 - Status: done
 - Severity: Low
 - Scope/files: `tests/test_routes_meta.py`
 - Why it matters: Module split safety depends on route/template script ordering and expected global export contracts; these regress silently without direct assertions.
 - Proposed change: Extend route-meta assertions to include the newly extracted keyboard helper module in script-order checks and split-module contract coverage.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py`
 - Execution result: Completed 2026-03-28. Route/meta tests now assert keyboard helper script presence/order and split-module wiring expectations.

- [x] R22: Continue `static/app.js` decomposition (remaining keyboard+interaction orchestration tranche)
 - Status: done
 - Severity: Medium
 - Scope/files: `static/app.js`, `static/interaction_helpers.js`, `templates/app.html`, `server_public_routes.py`, `server.py`, `tests/interaction_helpers.test.mjs`, `tests/test_routes_meta.py`.
 - Why it matters: `static/app.js` remains a large integration surface; completing decomposition lowers blast radius and increases direct contract-testability of interaction logic.
 - Proposed change: Extract the next cohesive interaction tranche into helper module(s), keep app wrappers stable, and preserve behavior contracts.
 - Validation steps:
   - `node --test tests/interaction_helpers.test.mjs tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_routes_auth.py`
 - Notes/dependencies: Maintain script-load order (`shared/runtime/helpers` before `app.js`) and no-store policy for newly split helper assets.
 - Execution result: Completed 2026-03-28. Added `HermesMiniappInteraction` helper module exposing `handleComposerSubmitShortcut` and `createSelectionQuoteController`; rewired `app.js` to use delegate wrappers while preserving existing scheduling/cancellation behavior. Added template/version injection and no-store route coverage for `interaction_helpers.js` and expanded route/meta contract checks. Validation: Node helper suite passed (`38` tests) and Python route/meta/auth suite passed (`39` tests).

- [x] R23: Run refactor-regression verification sweep for helper-module extraction batch
 - Status: done
 - Severity: Low
 - Scope/files: frontend helper tests + route/meta regression coverage
 - Why it matters: Incremental helper extraction needs repeated cross-layer checks to ensure browser/runtime behavior does not drift.
 - Proposed change: Run Python route/meta and Node helper suites together after each extraction batch and record evidence in plan.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_meta.py`
   - `node --test tests/keyboard_shortcuts_helpers.test.mjs tests/shared_utils.test.mjs tests/frontend_runtime.test.mjs tests/composer_state_helpers.test.mjs tests/stream_state_helpers.test.mjs tests/message_actions_helpers.test.mjs tests/chat_ui_helpers.test.mjs`
 - Execution result: Completed 2026-03-28. Verification sweep passed (`38` Python tests and `33` Node tests).

- [x] R24: Remove secret-bearing dev-auth bootstrap and redact sensitive request logging
 - Status: done
 - Severity: High
 - Scope/files: `static/app.js`, `templates/app.html`, `routes_auth.py`, `server.py` or request-logging helper, service `.env` docs/tests.
 - Why it matters: Desktop QA currently auto-signs in via `?dev_secret=...` query params. Live server logs captured the full query string and even TelegramBot preview traffic requesting `/app?dev_sec...`, which leaks the dev secret into logs, URL previews, browser history, and potentially analytics/proxy layers.
 - Proposed change: Replace query-string secret bootstrap with a safer desktop-testing flow (e.g. explicit modal/prompt only, one-time short-lived bootstrap token, or fragment-based client-only secret handoff) and redact sensitive query params from request logs before they hit journal output.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_meta.py`
   - Browser smoke: open `/app` outside Telegram and verify desktop sign-in works without secrets appearing in logged request paths.
   - Log assertion: request logs must redact `dev_secret`/future sensitive params.
 - Notes/dependencies: Highest-priority hardening item now that browser QA is unlocked; keep dev bypass feature but remove passive secret exposure.
 - Execution result: Completed 2026-03-28. Removed query-string dev-auth bootstrap from `static/app.js`, replaced prompt-only sign-in with a dedicated in-app `dev-auth-modal`, and added `sanitized_request_target()` for request-debug redaction of sensitive query params. Live browser verification confirmed desktop sign-in still works via the modal without secrets appearing in request paths.

- [x] R25: Split remaining `static/app.js` monolith into behavior-scoped modules
 - Status: done
 - Severity: High
 - Scope/files: `static/app.js` (~3560 LOC), new helper modules under `static/`, `templates/app.html`, route/meta asset tests, Node helper tests.
 - Why it matters: `static/app.js` still carries bootstrap/auth, chat hydration, stream send/resume orchestration, modal state, skin sync, drafts, tab rendering glue, and browser interaction wiring in one file. Recent regressions (SSE framing visibility, reconnect latency flash, dev-auth bootstrap) were harder to isolate because multiple subsystems still cohabit the same file.
 - Proposed change: Extract the remaining monolith into focused modules with stable namespaces/contracts, likely: `(1) bootstrap/auth/dev-auth`, `(2) chat/history hydration + tabs`, `(3) stream orchestration + latency/reconnect`, `(4) modal/admin actions`. Keep `app.js` as a thin composition/wiring layer only.
 - Validation steps:
   - `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/shared_utils.test.mjs`
   - Add direct Node tests for each newly extracted helper module.
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py`
 - Notes/dependencies: Do this in small batches; preserve current browser-verified behavior after each slice.
 - Execution result: Completed 2026-04-09. Finished the remaining behavior-preserving `static/app.js` decomposition in small helper-owned controller passes across runtime latency persistence, shell/debug-pill state, operator bootstrap labels/handles, interaction/selection-quote binding, render-message composition + visible pending/tool patching, stream reconnect policy, stream-phase ownership, and final tool-trace snapshot persistence. `app.js` is now primarily a composition/wiring layer that injects live state into focused helpers/controllers rather than owning bespoke subsystem logic inline.
 - Progress note (2026-03-28): Extracted desktop auth/bootstrap concerns into new `static/bootstrap_auth_helpers.js`, wired it into `templates/app.html` + `server_public_routes.py`, added direct Node tests (`tests/bootstrap_auth_helpers.test.mjs`), and verified the live app still opens the dev-sign-in modal correctly.
 - Progress note (2026-03-28, follow-up): Extracted chat/history hydration and warm-cache orchestration into new `static/chat_history_helpers.js`, delegated `loadChatHistory`, `hydrateChatFromServer`, `openChat`, `prefetchChatHistory`, and `warmChatHistoryCache` from `app.js`, added direct Node tests (`tests/chat_history_helpers.test.mjs`), and verified the live app still opens the dev-sign-in modal after the script-order expansion.
 - Progress note (2026-03-29): Extracted modal/admin/chat-management orchestration into new `static/chat_admin_helpers.js`, delegated chat title modal/tag logic plus `createChat`, `renameActiveChat`, `removeActiveChat`, `openPinnedChat`, `toggleActiveChatPin`, and settings modal open/close from `app.js`, added direct Node tests (`tests/chat_admin_helpers.test.mjs`), and verified the live app still opens both the dev-sign-in and settings dialogs after the new script-order expansion. Remaining slice: residual fullscreen/close/top-bar helpers and any small UI utility glue still embedded in `app.js`.
 - Progress note (2026-03-29, follow-up 2): Extracted top-bar/shell control logic into new `static/shell_ui_helpers.js`, delegated closing-confirmation sync, Telegram chrome skin updates, fullscreen button state/toggle, and close-app handling from `app.js`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/shell_ui_helpers.test.mjs`). Validation: `node --test tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
 - Progress note (2026-03-29, follow-up 3): Extracted composer/keyboard viewport utility logic into new `static/composer_viewport_helpers.js`, delegated `ensureComposerVisible`, `dismissKeyboard`, `installTapToDismissKeyboard`, and `installKeyboardViewportSync` from `app.js`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/composer_viewport_helpers.test.mjs`). Validation: `node --test tests/composer_viewport_helpers.test.mjs tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
 - Progress note (2026-03-29, follow-up 4): Extracted visibility/skin/dev-lifecycle glue into new `static/visibility_skin_helpers.js`, delegated skin normalize/store/apply sync (`normalizeSkin`, `getStoredSkin`, `setSkin`, `syncSkinFromStorage`), visibility reconciliation (`syncVisibleActiveChat`, `handleVisibilityChange`), and dev auto-reload polling (`shouldApplyDevReload`, `startDevAutoRefresh`) from `app.js`, moved visibility/focus/storage/broadcast listener wiring behind `installVisibilitySkinLifecycle()`, wired the new asset through `templates/app.html` + `server_public_routes.py` + static no-store coverage, and added direct Node tests (`tests/visibility_skin_helpers.test.mjs`). Validation: `node --test tests/visibility_skin_helpers.test.mjs tests/composer_viewport_helpers.test.mjs tests/shell_ui_helpers.test.mjs tests/bootstrap_auth_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/chat_admin_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py`.
- Progress note (2026-03-29, follow-up 7): Moved viewport-mutation stability helpers (`runAfterUiMutation`, `preserveViewportDuringUiMutation`) from `static/app.js` into `static/composer_viewport_helpers.js` so keyboard/composer viewport orchestration owns both visibility and mutation phases. `app.js` now delegates through `composerViewportController` with dynamic getters for active chat + scroll bookkeeping state. Added direct Node assertions for RAF/timeout mutation scheduling and pinned-bottom viewport preservation behavior while keeping route/meta wiring unchanged. Validation: `node --check static/composer_viewport_helpers.js && node --check static/app.js && node --test tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k composer_viewport_helpers`.
- Progress note (2026-03-29, follow-up 8): Extracted latency chip orchestration from `static/app.js` into `runtime_helpers.createLatencyController(...)` and rewired `setChatLatency`/`syncActiveLatencyChip` to delegate via `latencyViewController` with injected dependencies (`getActiveChatId`, `setActivityChip`, `preserveViewportDuringUiMutation`, `latencyChip`, `streamDebugLog`). Added direct runtime helper tests covering active-chat latency updates, viewport-preserving chip mutation wrapping, and placeholder restore behavior when no active chat is selected. Validation: `node --check static/runtime_helpers.js && node --check static/app.js && node --test tests/frontend_runtime.test.mjs tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k "runtime_helpers or composer_viewport_helpers or app_js"`.
- Progress note (2026-03-29, follow-up 9): Extracted stream/activity chip status transitions from `static/app.js` into `runtime_helpers.createStreamActivityController(...)`, delegated `syncActivePendingStatus` and send/resume status transitions (`markStreamActive`, `markStreamError`, `markNetworkFailure`, `markStreamReconnecting`, `markResumeAlreadyComplete`, `markReconnectFailed`) through `streamActivityController`, and preserved existing active-chat gating + latency sync behavior by injecting dynamic getters and existing chip/status mutators. Added direct runtime-helper tests for pending/idle transitions, active stream chip updates, reconnect/complete/failure behavior, and active-chat guards. Validation: `node --check static/runtime_helpers.js && node --check static/app.js && node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/composer_viewport_helpers.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py -k "runtime_helpers or app_js"`.
- Progress note (2026-03-29, follow-up 10): Extracted chat-list + pinned-collapse orchestration from `static/app.js` into `static/chat_tabs_helpers.js` (`normalizeChat`, `upsertChat`, `syncPinnedChats`, `syncChats`, pinned-collapse persistence/UI sync/toggle/auto-collapse, pin-button label sync), rewired `app.js` through `chatTabsController` delegates, and wired helper asset ordering/version/no-store coverage in route/template tests.
- Progress note (2026-03-29, follow-up 11): Extended `static/composer_state_helpers.js` with `createDraftController(...)` and delegated draft-storage lifecycle (`loadDraftsFromStorage`, `persistDraftsToStorage`, `setDraft`, `getDraft`) from `app.js` to the controller. Added direct Node coverage in `tests/composer_state_helpers.test.mjs` and reran helper + route meta/public route validations.
- Progress note (2026-03-29, follow-up 12): Extended `static/stream_controller.js` with `createToolTraceController(...)` and delegated inline tool-trace lifecycle (`resetToolStream`, `findPendingToolTraceMessage`, `ensurePendingToolTraceMessage`, `appendInlineToolTrace`, `finalizeInlineToolTrace`) from `app.js` through helper-managed wrappers. Added direct Node coverage in `tests/stream_controller.test.mjs` and refreshed hardening guard assertions to tolerate helper ownership moves.
- Progress note (2026-03-29, follow-up 13): Extended `static/runtime_helpers.js` with `createHapticUnreadController(...)` + `latestCompletedAssistantHapticKey(...)` and delegated haptic/unread lifecycle (`latestCompletedAssistantHapticKey`, `triggerIncomingMessageHaptic`, `incrementUnread`) from `app.js` to controller wrappers. Added runtime-helper Node coverage for haptic dedupe + unread increments (active/hidden gating) and updated streaming hardening guard coverage for helper export ownership.
- Progress note (2026-03-29, follow-up 14): Extended `static/interaction_helpers.js` with quote text utility ownership (`unwrapLegacyQuoteBlock`, `normalizeQuoteSelection`, `splitGraphemes`, `wrapQuoteLine`, `formatQuoteBlock`) and rewired matching `app.js` wrappers to thin delegates. Added direct helper tests in `tests/interaction_helpers.test.mjs` plus route-meta guard updates in `tests/test_routes_meta.py` so quote-format internals can continue moving out of `app.js` without brittle false failures. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 15): Extended `static/interaction_helpers.js` with selection-quote/input utility ownership (`getQuoteWrapWidth`, `isCoarsePointer`, `applyQuoteIntoPrompt`, `activeSelectionQuote`, `quotePlacementKey`, `showSelectionQuoteAction`, `syncSelectionQuoteAction`) and rewired the corresponding `app.js` functions into thin delegates. Added direct helper tests for quote-width estimation bounds, coarse-pointer detection, and prompt quote insertion/caret sync, and updated route-meta ownership assertions for selection quote positioning internals. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 16): Extended `static/interaction_helpers.js` with selection-quote timer/reset helper ownership (`clearSelectionQuoteState`, `cancelSelectionQuoteTimer`, `scheduleSelectionQuoteClear`, `scheduleSelectionQuoteSync`) and rewired matching `app.js` wrappers to delegate through helper APIs with injected callbacks/state. Added direct helper tests for state reset/hide behavior, timer cancellation delegation, clear-on-empty-selection scheduling, and sync reschedule semantics. Updated route-meta coverage to assert helper-side ownership for the extracted timer/reset functions while preserving existing app-side delegate/wiring guards. Validation: `node --check static/interaction_helpers.js && node --check static/app.js && node --test tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 17): Extended `static/render_trace_helpers.js` with rendering ownership (`renderBody`, `renderToolTraceBody`, `roleLabelForMessage`) and rewired matching `app.js` functions to thin delegates with injected formatting/DOM dependencies (`cleanDisplayText`, `escapeHtml`, `document`, `operatorDisplayName`). Added direct helper tests in `tests/render_trace_helpers.test.mjs` for fenced/non-fenced body rendering, tool-trace DOM toggle behavior, and role-label mapping; updated `tests/test_routes_meta.py` assertions to track helper ownership and app delegation for the extracted render functions. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 18): Extended `static/render_trace_helpers.js` with message card render ownership (`messageVariantForRole`, `shouldSkipMessageRender`, `applyMessageMeta`, `renderMessageContent`) and rewired matching `app.js` wrappers into thin delegates with injected dependencies/callbacks (`formatMessageTime`, `roleLabelForMessage`, `renderBody`, `renderToolTraceBody`). Added direct helper tests in `tests/render_trace_helpers.test.mjs` for role/body-skip mapping, metadata application (role/pending class + timestamp), and tool-vs-body renderer dispatch; updated `tests/test_routes_meta.py` assertions to track helper ownership and app-side delegation for these extracted message-node helpers. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, combined follow-ups 19-21): Extended `static/render_trace_helpers.js` with message-node orchestration ownership (`messageStableKey`, `messageStableKeyForPendingState`, `upsertMessageNode`, `createMessageNode`, `appendMessages`, `findMessageNodeByKey`) and rewired matching `app.js` wrappers into thin delegates with injected rendering/template dependencies. Added direct helper tests in `tests/render_trace_helpers.test.mjs` for stable key generation, upsert pipeline composition, node creation/dataset behavior, append-skip semantics, and selector-based key lookup fallback; updated `tests/test_routes_meta.py` ownership/delegation assertions for all newly extracted functions. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, combined follow-ups 22-24): Extended `static/render_trace_helpers.js` with history-target lookup + visible stream patch ownership (`findLatestHistoryMessageByRole`, `findLatestAssistantHistoryMessage`, `patchVisiblePendingAssistant`, `patchVisibleToolTrace`) and rewired matching `app.js` wrappers into thin delegates with injected phase guards, key lookup, logging, and render/viewport dependencies. Added direct helper tests in `tests/render_trace_helpers.test.mjs` for latest target derivation contracts, assistant visible pending-body patch behavior, and tool trace body/time patch behavior (including no-target success path); updated `tests/test_routes_meta.py` ownership/delegation assertions for all newly extracted helpers. Validation: `node --check static/render_trace_helpers.js && node --check static/app.js && node --test tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 25): Extended `static/chat_history_helpers.js` with chat-history mutation and active-view scheduling ownership (`addLocalMessage`, `updatePendingAssistant`, `syncActiveMessageView`, `scheduleActiveMessageView`) and rewired matching `static/app.js` wrappers into thin delegates through `chatHistoryController`. Injected `runAfterUiMutation` into chat-history controller creation (with timeout fallback inside helper) to preserve deferred active-chat rerender semantics while removing `activeRenderScheduled`/`activeRenderChatId` bookkeeping from `app.js`. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for pending assistant mutation/finalize behavior and active-chat-only sync/scheduled rerenders, and updated `tests/test_routes_meta.py` assertions for helper ownership + app-side delegation. Validation: `node --check static/chat_history_helpers.js && node --check static/app.js && node --test tests/chat_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-03-29, follow-up 26): Extended `static/chat_history_helpers.js` with read-sync gating ownership (`maybeMarkRead`) and rewired `static/app.js` `maybeMarkRead(...)` into a thin controller delegate. Injected read-gating dependencies into chat-history controller creation (`getIsAuthenticated`, `isNearBottomFn`, `messagesContainer`, `unseenStreamChats`, `markReadInFlight`, `markReadFn`) so active-chat/auth/near-bottom/unread checks and in-flight dedupe live in the helper while preserving behavior. Added direct helper tests in `tests/chat_history_helpers.test.mjs` for auth/active/visibility/unread gating and forced in-flight dedupe behavior, and updated `tests/test_routes_meta.py` assertions for helper + app delegate wiring. Validation: `node --check static/chat_history_helpers.js && node --check static/app.js && node --test tests/chat_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/interaction_helpers.test.mjs tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-04-02, follow-up 28): Extended `static/stream_state_helpers.js` with `createPersistenceController(...)` and delegated stream resume-cursor + pending snapshot persistence (`read/write/get/set/clearStoredStreamCursor`, `read/write/clearPendingStreamSnapshot`, `normalizeSnapshotLines`, `mergeSnapshotToolJournalLines`, `persistPendingStreamSnapshot`, `restorePendingStreamSnapshot`) from `static/app.js` through a thin `streamPersistenceController`. Added direct Node coverage in `tests/stream_state_helpers.test.mjs` plus `tests/stream_state_app_delegation.test.mjs`, and updated `tests/test_streaming_hardening_guards.py` to follow helper ownership while preserving app-side rehydrate/resume wiring. Validation: `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/shared_utils.test.mjs tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs` and `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py`.
- Progress note (2026-04-08, follow-up 35): Extended `static/chat_tabs_helpers.js` with reconnect resume-cooldown/block suppression ownership so `upsertChat(...)` and `syncChats(...)` in `static/app.js` are thin delegates again. Injected `resumeCooldownUntilByChat`, `reconnectResumeBlockedChats`, `suppressBlockedChatPending`, and `nowFn` into `chatTabsController`, added direct helper regressions in `tests/chat_tabs_helpers.test.mjs`, and added app-wrapper contract coverage in `tests/chat_tabs_app_delegation.test.mjs`. Validation: `node --check static/chat_tabs_helpers.js static/app.js tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs` and `node --test tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs`.
- Progress note (2026-04-08, follow-up 38): Extended `static/bootstrap_auth_helpers.js` with `apiPost(...)`, auth-bootstrap retry, and bootstrap-version refresh ownership, and extended `static/startup_bindings_helpers.js` with startup binding validation/bootstrap/watchdog ownership so `static/app.js` now routes startup entrypoints through thin `bootstrapAuthController` / `startupBindingsController` delegates. Updated `tests/test_routes_meta.py` and `tests/test_streaming_hardening_guards.py` to assert against the new helper boundaries, then revalidated the focused Node helper/delegation suites plus Python route/guard coverage (`75 passed`).

- [x] R26: Finish stream lifecycle extraction and contract-test the send/resume/hydration state machine
 - Status: done
 - Severity: High
 - Scope/files: `static/app.js`, `static/stream_controller.js`, `static/stream_state_helpers.js`, `tests/stream_controller.test.mjs`, `tests/frontend_runtime.test.mjs`, possibly new dedicated stream-lifecycle test module.
 - Why it matters: The live tool-stream bug was ultimately a protocol/framing issue, but debugging also showed lifecycle complexity spread across `app.js` and `stream_controller.js`: initial send, early-close suppression, resume recursion, 409 terminalization races, latency lifecycle, and history reconciliation all span multiple helpers. This is still the highest-blast-radius UI subsystem.
 - Proposed change: Move remaining stream send/resume/finalize orchestration behind one explicit controller/state-machine API with direct contract tests for live tool events, early close, resume-409 graceful completion, hydration merge, and latency preservation during reconnect.
 - Validation steps:
   - `node --test tests/stream_controller.test.mjs tests/frontend_runtime.test.mjs`
   - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_streaming_hardening_guards.py`
   - Live browser smoke against the deployed Mini App with dev auth and a tool-heavy prompt.
 - Notes/dependencies: Pair naturally with R25 but can start earlier as a targeted hardening pass.
 - Execution result: Completed 2026-03-28. Moved `hydrateChatAfterGracefulResumeCompletion(...)` and `consumeStreamWithReconnect(...)` into `static/stream_controller.js`, wired `app.js` through controller delegates, and added direct Node contract tests (`tests/stream_controller.test.mjs`) plus guard coverage in `tests/test_streaming_hardening_guards.py` / `tests/test_routes_meta.py`. Browser smoke confirmed the desktop entry path still works after the consolidation.

- [x] R27: Reduce production stream/debug logging noise and sensitive payload exposure
 - Status: done
 - Severity: Medium
 - Scope/files: `.env`, `miniapp_config.py`, `job_runtime.py`, `routes_chat_stream.py`, frontend debug wiring/tests/docs.
 - Why it matters: The live environment currently runs with `MINIAPP_REQUEST_DEBUG=1` and `MINI_APP_STREAM_TIMING_DEBUG=1`, and browser/server traces include chunk previews, timing payloads, and request metadata. That was useful for current forensics, but it increases log volume and can expose prompt/tool details longer than necessary.
 - Proposed change: Introduce a safer operator-debug mode with explicit temporary enablement, bounded preview lengths/redaction rules, and a documented default-off production posture while preserving lightweight non-sensitive health telemetry.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py tests/test_server_startup.py`
   - Manual log inspection before/after toggling debug envs.
 - Notes/dependencies: Coordinate with R24 so secret/log redaction and debug policy land together.
 - Execution result: Completed 2026-04-02. Added `operator_debug` + `stream_timing_debug` config wiring, required operator-debug gating before publishing runtime timing payloads, and trimmed frontend stream debug payload previews to non-sensitive telemetry while preserving operator diagnostics.

- [x] R28: Decompose oversized test monoliths and add browser-regression fixtures for live-risk paths
 - Status: done
 - Severity: Medium
 - Scope/files: `tests/test_routes_meta.py` (933 LOC), `tests/test_routes_chat.py` (1134 LOC), `tests/test_hermes_client.py` (3046 LOC), shared fixtures/util helpers under `tests/`.
 - Why it matters: Core regression suites are themselves becoming hard to reason about. The browser-derived regressions we just fixed (SSE literal newline framing, reconnect latency behavior, dev-auth desktop path) are mixed into already-large files, which raises maintenance cost and makes missing coverage harder to spot.
 - Proposed change: Split test monoliths by concern (auth/meta, stream contracts, browser-bootstrap guards, Hermes client direct vs persistent transport) and add reusable fixtures/utilities for dev-auth/browser-flow assertions.
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_auth.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_server_public_routes.py`
   - `node --test tests/*.test.mjs` (or targeted helper suites after split)
 - Notes/dependencies: Lower risk than product-code refactors and will make future regression work faster.
 - Execution result: Completed 2026-04-08. Split the old render-trace/browser regression monolith into focused suites (`tests/render_trace_debug_helpers.test.mjs`, `tests/render_trace_text_helpers.test.mjs`, `tests/render_trace_message_helpers.test.mjs`, `tests/render_trace_history_helpers.test.mjs`) plus a slim facade contract test, and moved the anchored viewport regression out of `tests/frontend_runtime.test.mjs` into the history-focused suite.

- [x] R29: Split `static/render_trace_helpers.js` monolith into focused view/render slices
 - Status: done
 - Severity: High
 - Scope/files: `static/render_trace_helpers.js` (206 LOC facade after split), `static/render_trace_text_helpers.js` (297 LOC), `static/render_trace_debug_helpers.js` (136 LOC), `static/render_trace_message_helpers.js` (562 LOC), `static/render_trace_history_helpers.js` (407 LOC), split helper tests, `tests/render_trace_app_delegation.test.mjs`, `static/app.js` delegates.
 - Why it matters: The app.js decomposition shifted a large amount of rendering logic into `render_trace_helpers.js`, which is now the largest frontend helper and carries mixed responsibilities (trace debug state, markdown rendering, message node creation, viewport virtualization). This creates a new blast-radius hotspot and slows regression diagnosis.
 - Proposed change: Split into focused modules/controllers (for example: `render_trace_debug_helpers`, `render_trace_message_helpers`, `render_trace_history_helpers`) with explicit dependency injection boundaries and delegation contract tests.
 - Validation steps:
   - `node --check static/render_trace_helpers.js static/render_trace_text_helpers.js static/render_trace_debug_helpers.js static/render_trace_message_helpers.js static/render_trace_history_helpers.js`
   - `node --test tests/render_trace_text_helpers.test.mjs tests/render_trace_debug_helpers.test.mjs tests/render_trace_message_helpers.test.mjs tests/render_trace_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/render_trace_app_delegation.test.mjs tests/frontend_runtime.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py`
 - Notes/dependencies: Execute in thin slices to preserve existing controller/delegation test contracts.
 - Execution result: Completed 2026-04-08. Split the old `static/render_trace_helpers.js` monolith into focused text, debug, message, and history helper modules while retaining a facade export layer for compatibility. Added matching focused Node test files, updated template/static version wiring and route-meta assertions for the new assets, and kept `static/app.js` delegation contracts stable through the facade.
 - Validation result: `node --check static/render_trace_helpers.js static/render_trace_text_helpers.js static/render_trace_debug_helpers.js static/render_trace_message_helpers.js static/render_trace_history_helpers.js`; `node --test tests/render_trace_text_helpers.test.mjs tests/render_trace_debug_helpers.test.mjs tests/render_trace_message_helpers.test.mjs tests/render_trace_history_helpers.test.mjs tests/render_trace_helpers.test.mjs tests/render_trace_app_delegation.test.mjs tests/frontend_runtime.test.mjs` → `61 passed`; `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_streaming_hardening_guards.py` → `71 passed in 1.57s`.

- [x] R30: Decompose `routes_chat_management.py` into route registration + service-layer operations
 - Status: done
 - Severity: Medium
 - Scope/files: `routes_chat_management.py` (613 LOC → 290 LOC), new `routes_chat_management_service.py` (418 LOC), and related route contract tests.
 - Why it matters: `routes_chat_management.py` was the largest backend route module and mixed request parsing/validation, store orchestration, and response shaping for multiple endpoints. That concentration raised blast radius for chat UX + pin/fork/admin changes.
 - Proposed change: Extract a service-layer module for chat management operations and keep route registration as thin adapters (mirroring existing dynamic getter conventions used by other route modules).
 - Validation steps:
   - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_meta.py`
   - `.venv/bin/python -m pytest -q tests/test_server_public_routes.py`
 - Notes/dependencies: Preserve monkeypatch-friendly dynamic getter injection and endpoint response contracts.
 - Execution result: Completed 2026-04-09. Added `routes_chat_management_service.py` with getter-injected `ChatManagementService` methods for history serialization/payload shaping, file-preview resolution/building, runtime eviction, pin/reopen/branch/clear/remove flows, and chats-status response shaping. Rewrote `routes_chat_management.py` so it now focuses on request parsing, route guards, boolean/title validation, and delegation into the service layer while preserving current guard wiring and monkeypatch-friendly getter behavior.
 - Validation result: `source .venv/bin/activate && python -m py_compile routes_chat_management.py routes_chat_management_service.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_server_public_routes.py`; `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat.py tests/test_routes_meta.py` → `124 passed`; `source .venv/bin/activate && python -m pytest -q tests/test_server_public_routes.py` → `4 passed`. During validation, fixed a missing `_authed_client(...)` setup line in `tests/test_routes_chat.py` and refreshed a stale file-preview ownership assertion in `tests/test_routes_meta.py` to match the current app-side click binding contract.

- [x] R31: Harden warm-runtime ownership/reuse env semantics and fallback telemetry
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client.py` (warm-runtime ownership resolution + reuse policy path), `hermes_client_types.py`, `miniapp_config.py`, `tests/test_hermes_client.py`, `tests/test_config.py`
 - Why it matters: The current warm-runtime batch is surfacing nondeterministic behavior from env-driven ownership/reuse semantics. Targeted validation now fails in five places: default reuse-policy checks are unexpectedly allowed, policy-blocked decisions still attempt reuse, fallback telemetry reports `agent-worker-isolated` instead of the requested logical path, and runtime-status reports `persistent_runtime_ownership_requested=shared` instead of preserving the expected `auto` request semantics. That makes rollout/switch debugging harder and means ambient env state can silently change policy behavior.
 - Proposed change: Separate requested-vs-resolved ownership fields cleanly, make warm-reuse defaults deterministic under test and production config, normalize fallback telemetry to the requested logical transport, and add focused config/contract tests so ambient host env cannot silently flip reuse attempts.
 - Validation steps:
   - `source .venv/bin/activate && python -m pytest -q tests/test_config.py tests/test_hermes_client.py -k "warm_reuse or runtime_status_reports_recall_health or persistent_runtime_ownership"`
   - `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py`
   - Re-check miniapp runtime diagnostics / warm-session strategy payload after the fix.
 - Notes/dependencies: Do before deeper warm-worker reuse rollout work so the contract is stable; keep requested-vs-resolved fields explicit in operator diagnostics.
 - Execution result: Completed 2026-04-08. Preserved requested ownership separately from resolved ownership by keeping `MiniAppConfig.persistent_runtime_ownership` as the requested value, using `resolved_persistent_runtime_ownership()` only for the enforced runtime choice, and making `HermesClient` diagnostics read `MINI_APP_PERSISTENT_RUNTIME_OWNERSHIP_REQUESTED` (default `auto`) instead of collapsing to the resolved env. Updated `server.py` to inject both requested and resolved ownership env values when composing the shared client, added autouse env-clearing fixtures in the targeted test files so ambient miniapp env no longer flips warm reuse/persistent session behavior under pytest, and refreshed config assertions to expect requested `shared` + resolved `checkpoint_only` in subprocess mode.
 - Validation result: `source .venv/bin/activate && python -m py_compile hermes_client.py miniapp_config.py server.py tests/test_hermes_client.py tests/test_config.py`; `source .venv/bin/activate && python -m pytest -q tests/test_config.py tests/test_hermes_client.py -k "warm_reuse or runtime_status_reports_recall_health or persistent_runtime_ownership"` → `17 passed`; `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py tests/test_server_startup.py` → `11 passed`; direct runtime-status spot-check confirmed startup routing now reports requested `auto` and resolved `shared` for the shared-backend path.

- [x] R32: Split `routes_chat_stream.py` into thin route adapters plus stream service/generator helpers
 - Status: done
 - Severity: High
 - Scope/files: `routes_chat_stream.py`, new helper/service module(s) such as `routes_chat_stream_service.py`, and targeted stream-route tests.
 - Why it matters: `routes_chat_stream.py` is still a 422 LOC route monolith with a 403-line `register_stream_routes(...)` and a 186-line `_stream_job_response(...)`/generator path. It mixes request/auth resolution, SSE framing, queue heartbeat/polling policy, stale-open-job recovery, warm-attach recovery, and segment-rollover behavior in one place, which keeps stream changes high-blast-radius.
 - Proposed change: Keep `register_stream_routes(...)` as a thin registrar, move the generator/heartbeat/recovery policy into helper/service-level functions/classes with injected getters/callbacks, and preserve current SSE event/status contracts (`400/404/409`, heartbeat cadence, terminal recovery, segment rollover, timing debug gating).
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile routes_chat_stream.py tests/test_routes_chat.py tests/test_streaming_hardening_guards.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat.py tests/test_streaming_hardening_guards.py -k "stream_chat or stream_chat_resume or stream recovered"`
 - Notes/dependencies: Preserve monkeypatch-friendly dynamic getter injection and SSE parity with sync-route helpers; likely pairs naturally with R33 direct tests.
 - Execution result: Completed 2026-04-09. Extracted the stream route monolith into a thin `routes_chat_stream.py` registrar plus `routes_chat_stream_service.py` for auth/chat resolution, stale-open-job handling, header/payload parsing, and job-event logging, alongside `routes_chat_stream_generator.py` for SSE response construction, heartbeat/meta polling, synthetic terminal recovery, warm-handoff recovery, and segment-rollover behavior. Updated the existing route tests to target the new generator seam directly where monotonic timing/queue silence behavior is asserted, while preserving the existing route contracts and dynamic getter injection pattern. Follow-up hardening in the same resumed pass fixed the segment-rollover boundary so a stream that becomes terminal in DB exactly as the segment deadline hits now emits a synthetic terminal SSE event instead of closing silently.
 - Validation result: `source .venv/bin/activate && python -m py_compile routes_chat_stream.py routes_chat_stream_service.py routes_chat_stream_generator.py tests/test_routes_chat.py tests/test_streaming_hardening_guards.py tests/test_routes_chat_stream_service.py` → success; `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat.py tests/test_streaming_hardening_guards.py tests/test_routes_chat_stream_service.py` → `82 passed` in the resumed 2026-04-09 session after hardening the segment-rollover terminal-recovery boundary.

- [x] R33: Add direct contract tests for extracted chat-management and stream service layers
 - Status: done
 - Severity: Medium
 - Scope/files: new direct test modules for `routes_chat_management_service.py` and the extracted stream helper/service layer from R32.
 - Why it matters: the direct extracted-module coverage scan found no test references to `routes_chat_management_service.py`, and stream route behavior is still mostly validated indirectly through larger route/hardening suites. That leaves service-layer contracts vulnerable to drift even though the route splits are now central architecture.
 - Proposed change: add focused direct contract tests for service-layer payload shaping, file-preview/path resolution, remove/reopen/branch flows, and stream-helper edge cases (heartbeat throttling, synthetic terminal recovery, stale-open-job handling, segment rollover) so extracted ownership is covered directly instead of only through integration suites.
 - Validation steps:
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat_management_service.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat_stream_service.py`
 - Notes/dependencies: Execute after or alongside R32 so the stream-side direct tests target the final helper boundary instead of the current monolith registrar.
 - Execution result: Completed 2026-04-09. Added `tests/test_routes_chat_management_service.py` with direct service-layer coverage for create/branch/remove contracts and runtime eviction, plus `tests/test_routes_chat_stream_service.py` with direct coverage for stream-route service activation/recovery behavior, mobile segment selection, event-id parsing, silent-queue terminal synthesis, segment-rollover logging, and the terminal-at-rollover boundary where the stream must emit a synthetic terminal event instead of closing silently.
 - Validation result: `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat_management_service.py tests/test_routes_chat_stream_service.py` → `10 passed`; combined route/service validation `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat_management_service.py tests/test_routes_chat_stream_service.py tests/test_routes_chat.py tests/test_streaming_hardening_guards.py` → `82 passed`; `node --check static/runtime_helpers.js static/stream_controller.js tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs && node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs` → `94 passed`.

- [x] R34: Finish the last meaningful `static/app.js` runtime/shell orchestration extractions
 - Status: done
 - Severity: Medium
 - Scope/files: `static/app.js`, `static/runtime_helpers.js`, `static/shell_ui_helpers.js`, `static/chat_tabs_helpers.js`, `static/stream_controller.js`, and focused Node delegation tests.
 - Why it matters: despite the large R25 decomposition, `static/app.js` is still 2382 LOC and retains a few non-trivial ownership seams: composer refocus retry/orchestration (`focusComposerForNewChat`), Telegram confirm/popup fallback (`confirmAction`), unread increment trace logging, reconnect resume-budget bookkeeping, and pending-finalize trace logging. Those are now the highest-value remaining pieces of bespoke app-level behavior.
 - Proposed change: move the remaining runtime/shell orchestration into the most adjacent helper owners (for example `shell_ui_helpers`, `runtime_helpers`, `chat_tabs_helpers`, `stream_controller`) using getter-based deps so `app.js` is left with composition/delegation only.
 - Validation steps:
   - `node --check static/app.js static/runtime_helpers.js static/shell_ui_helpers.js static/chat_tabs_helpers.js static/stream_controller.js`
   - `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/shell_ui_helpers.test.mjs tests/chat_tabs_helpers.test.mjs`
 - Notes/dependencies: The working tree is already dirty in `static/stream_controller.js`/`tests/stream_controller.test.mjs` with a queue-latency behavior tweak, so start by reconciling or preserving that in-flight change before broad extraction edits.

- [x] R35: Break `static/stream_controller.js` `createController(...)` into focused stream-lifecycle subcontrollers
 - Status: done
 - Severity: High
 - Scope/files: `static/stream_controller.js`, potential new helper module(s) or internal subcontroller factories, and `tests/stream_controller.test.mjs` / app delegation guards.
 - Why it matters: `static/stream_controller.js` is now 1415 LOC and its main `createController(...)` spans 1171 lines, making it the next frontend monolith after `app.js`. It currently mixes SSE parsing, event dispatch, pending assistant/tool patching, latency/status updates, reconnect handling, finalization, and UI synchronization in one constructor-sized closure.
 - Proposed change: split `createController(...)` into focused subcontrollers/factories (for example stream event dispatch, terminal/finalize flow, queue/meta handling, reconnect lifecycle) while preserving the existing public facade expected by `app.js` and current tests.
 - Validation steps:
   - `node --check static/stream_controller.js tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs`
   - `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs`
 - Notes/dependencies: Sequence after R34 if that pass removes the last app-owned stream wrappers first; preserve the current latency behavior tweak under validation while refactoring.
 - Execution result: Completed 2026-04-09. Split `static/stream_controller.js` into helper-owned internal subcontrollers: `createStreamSessionController(...)` now owns replay cursor / abort-controller / first-assistant notification state, `createStreamTranscriptController(...)` owns transcript patching + terminal application + hydration + reconnect consumption, and `createStreamLifecycleController(...)` owns finalize/send/resume orchestration. Rewired `createController(...)` into composition-only glue, exported the new subcontroller factories for direct contract coverage, added direct regressions in `tests/stream_controller.test.mjs`, and refreshed `tests/test_streaming_hardening_guards.py` to assert the new ownership boundary. Validation: `node --check static/stream_controller.js tests/stream_controller.test.mjs`, `node --test tests/stream_controller.test.mjs tests/stream_controller_app_delegation.test.mjs` (46 passed), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "test_stream_controller_module_exports_core_api or test_app_resume_handles_no_active_job_reconnect_gracefully or test_app_persists_pending_stream_snapshot_for_pre_resume_rehydrate"` (3 passed, 6 deselected).

- [x] R44: Split `static/stream_controller.js` transcript/lifecycle subcontrollers into focused helper slices
 - Status: done
 - Severity: High
 - Scope/files: `static/stream_controller.js`, focused stream controller tests (`tests/stream_controller_*.test.mjs`), and app delegation / hardening guards that assert helper ownership
 - Why it matters: the first-pass split landed, but the file has regrown to 1645 LOC and still carries two oversized internal seams: `createStreamTranscriptController(...)` is ~651 LOC and `createStreamLifecycleController(...)` is ~510 LOC. The module is still the largest live frontend behavior surface after `app.js`, so stream fixes remain high-blast-radius even though the outer facade is cleaner.
 - Proposed change: keep the current public `createController(...)` API stable, but split transcript consumption, terminal/finalize flow, resume/reconnect policy, and request/send orchestration into narrower helper factories or adjacent modules. Preserve replay-cursor, tool-trace, and pending-history contracts already covered by the focused Node suites.
 - Validation steps:
   - `node --check static/stream_controller.js tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`
   - `node --test tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs`
   - `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "stream_controller or resume or pending_stream_snapshot"`
 - Notes/dependencies: sequence before any fresh stream UX tweaks so the current controller/test seams stay stable while ownership narrows further.
 - Execution result: Completed 2026-04-10. `static/stream_controller.js` now decomposes transcript ownership across `createTranscriptSignatureHelpers(...)`, `createTranscriptHydrationController(...)`, `createTranscriptEventController(...)`, and `createTranscriptConsumeController(...)`, leaving `createStreamTranscriptController(...)` as a thin 14-line composition wrapper. Lifecycle ownership is now split across `createStreamFocusController(...)`, `createStreamFinalizeController(...)`, `createStreamSendController(...)`, and `createStreamResumeController(...)`, leaving `createStreamLifecycleController(...)` as a 27-line composition/wiring shim. Validation: `node --check static/stream_controller.js tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs` (passed), `node --test tests/stream_controller_policy_session.test.mjs tests/stream_controller_response.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/stream_controller_tool_trace.test.mjs tests/stream_controller_app_delegation.test.mjs` (`51 passed`), and `source .venv/bin/activate && python -m pytest -q tests/test_streaming_hardening_guards.py -k "stream_controller or resume or pending_stream_snapshot"` (`1 failed, 2 passed, 6 deselected`) with the remaining failure isolated to an unrelated existing `runtime_latency_helpers.js` guard assertion in the dirty tree.

- [x] R45: Split `job_runtime_worker_launcher.py` subprocess stream loop into focused launcher/transport helpers
 - Status: done
 - Severity: Medium
 - Scope/files: `job_runtime_worker_launcher.py`, `tests/test_job_runtime_worker_launcher.py`, and any helper module(s) introduced for subprocess transport/error classification
 - Why it matters: `job_runtime_worker_launcher.py` is now 641 LOC and `_stream_events_via_subprocess(...)` alone spans ~357 lines, mixing child launch, environment shaping, event parsing, stderr handling, idle/limit breach recovery, and process-tree teardown. That makes the worker-launcher path hard to reason about during miniapp isolation incidents.
 - Proposed change: extract focused helpers for subprocess spawn/config, stream/event decode loop, and terminal failure/limit-breach classification while preserving the current public launcher contract and diagnostics fields.
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile job_runtime_worker_launcher.py tests/test_job_runtime_worker_launcher.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_worker_launcher.py`
 - Notes/dependencies: coordinate with the existing isolated-worker launcher behavior and avoid changing timeout/ownership policy semantics in the same pass.
 - Execution result: Completed 2026-04-10. `SubprocessJobWorkerLauncher` now routes the subprocess stream path through focused helper bands for spawn/config (`_child_env`, `_assert_spawn_allowed`, `_spawn_subprocess`, `_register_spawn`, `_write_payload`), transport/decode (`_stdout_line_queue`, `_decode_subprocess_event`, `_iter_subprocess_events`, `_record_stream_event`), attach/terminal event handling (`_handle_child_spawn_event`, `_handle_attach_ready_event`, `_handle_worker_terminal_event`), and teardown/error mapping (`_emit_timeout_error`, `_wait_for_subprocess_exit`, `_emit_nonzero_exit_error`, `_finalize_subprocess_stream`), leaving `_stream_events_via_subprocess(...)` as a 78-line orchestration method. Validation: `source .venv/bin/activate && python -m py_compile job_runtime_worker_launcher.py tests/test_job_runtime_worker_launcher.py tests/test_routes_jobs_runtime.py` (passed); `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_worker_launcher.py` (`1 failed, 24 passed`) with the remaining failure isolated to the pre-existing dirty-tree/open-job-claim test `test_runtime_run_chat_job_uses_configured_worker_launcher`; and `source .venv/bin/activate && python -m pytest -q tests/test_job_runtime_worker_launcher.py -k "not test_runtime_run_chat_job_uses_configured_worker_launcher"` (`23 passed, 1 deselected`).

- [x] R46: Decompose `hermes_client.py` warm-reuse / attach-resume transport monolith into focused miniapp transport helpers
 - Status: done
 - Severity: High
 - Scope/files: `hermes_client.py` (and helper module(s) if introduced), `tests/test_hermes_client.py`, and targeted warm-reuse/runtime-status regressions
 - Why it matters: `hermes_client.py` has regrown to 2677 LOC, with the miniapp-critical transport paths concentrated in a few oversized methods: `stream_events(...)` (~235 LOC), `_attempt_live_worker_attach_resume(...)` (~223 LOC), `_attempt_live_worker_attach_handshake(...)` (~127 LOC), and `attempt_warm_reuse(...)` (~131 LOC). This is now the largest backend blast-radius hotspot tied directly to miniapp attach/resume continuity and operator diagnostics.
 - Proposed change: split warm-reuse policy evaluation, live-worker attach handshake/resume orchestration, and transport fallback/reporting into focused helpers/dataclasses while preserving the current `HermesClient` public API and requested-vs-resolved ownership telemetry.
 - Validation steps:
   - `source .venv/bin/activate && python -m py_compile hermes_client.py tests/test_hermes_client.py tests/test_config.py`
   - `source .venv/bin/activate && python -m pytest -q tests/test_hermes_client.py -k "warm_reuse or attach or runtime_status_reports_recall_health or stream_events"`
   - `source .venv/bin/activate && python -m pytest -q tests/test_config.py -k "persistent_runtime_ownership or warm_reuse"`
 - Notes/dependencies: keep fallback labels/requested-vs-resolved ownership fields stable; this should precede deeper warm-worker reuse rollout work so the transport contract stops drifting inside one giant class.
 - Execution result: Completed 2026-04-10. `hermes_client.py` now routes the attach/resume seam through helper-owned stages: `_build_attach_handshake_result(...)` + `_probe_live_worker_attach_handshake(...)` narrow `_attempt_live_worker_attach_handshake(...)` to a small retry loop, `_build_attach_resume_request_payload(...)` + `_connect_attach_resume_transport(...)` narrow `_attempt_live_worker_attach_resume(...)` to transport resolution + error mapping, `_attempt_validated_warm_reuse(...)` separates validation gating from attempt recording, and `_stream_events_via_configured_remote_transport(...)` + `_stream_events_after_remote_transport(...)` isolate configured HTTP fallback/reporting from local launch/warm-reuse flow. Validation: `source .venv/bin/activate && python -m py_compile hermes_client.py tests/test_hermes_client.py tests/test_config.py` (passed), `source .venv/bin/activate && python -m pytest -q tests/test_hermes_client.py -k "warm_reuse or attach or runtime_status_reports_recall_health or stream_events"` (`42 passed, 64 deselected`), and `source .venv/bin/activate && python -m pytest -q tests/test_config.py -k "persistent_runtime_ownership or warm_reuse"` (`1 passed, 18 deselected`).

## Archived execution and refresh evidence
- Refactor-pass refresh evidence (2026-04-10):
  - `git status --short --branch` → `## main...origin/main` with only untracked `var/`; this confirms the earlier tracked miniapp refactor diffs have landed and the canonical plan metrics needed to be recomputed against current repo reality.
  - Backend hotspot scan: `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` → largest current production files include `hermes_client.py` (2677), `server.py` (663), `job_runtime_worker_launcher.py` (641), `job_runtime.py` (373), `routes_auth.py` (290), and `routes_chat_stream_generator.py` (283).
  - Frontend hotspot scan: `find static tests -type f \( -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' \) -print0 | xargs -0 wc -l | sort -nr | head -n 30` → `static/app.js` (2520), `tests/chat_history_helpers.test.mjs` (1937), `static/stream_controller.js` (1645), `tests/frontend_runtime.test.mjs` (1616), `static/chat_history_helpers.js` (1045), and `static/bootstrap_auth_helpers.js` (589).
  - Current function/closure audit shows remaining oversized seams, not just large files: `build_auth_success_response(...)` in `routes_auth.py` (~92 LOC), `createController(...)` in `static/bootstrap_auth_helpers.js` (~588 LOC), `createController(...)` in `static/chat_history_helpers.js` (~907 LOC), `_stream_events_via_subprocess(...)` in `job_runtime_worker_launcher.py` (~357 LOC), and miniapp transport methods in `hermes_client.py` led by `stream_events(...)` (~235 LOC) and `_attempt_live_worker_attach_resume(...)` (~223 LOC).
  - Direct extracted-module coverage scan confirms the earlier helper splits are still covered directly (`tests/bootstrap_auth_helpers.test.mjs`, `tests/chat_history_helpers.test.mjs`, `tests/stream_controller_*`, template-order smoke tests), but the worker-launcher and regrown `hermes_client.py` transport seams remain concentrated in larger Python suites rather than smaller helper-owned contracts.
  - Targeted validation: `source .venv/bin/activate && python -m pytest -q tests/test_routes_auth.py` → `17 passed in 4.70s`; `node --test tests/bootstrap_auth_helpers.test.mjs` → `15 passed`; `node --test tests/chat_history_helpers.test.mjs` → `64 passed`.
  - Placeholder debt scan: `search_files("TODO|FIXME|HACK", file_glob="*.py")` and equivalent JS scan both returned no marker debt, so the current backlog is structural/complexity-driven rather than obvious unfinished stubs.
- Refactor-pass refresh evidence (2026-04-09):
  - `git status --short --branch` → `## main...origin/main` with tracked modifications in `docs/maintainers/history/refactor-plan.hermes-miniapp-v4.md`, `routes_chat_stream.py`, `static/runtime_helpers.js`, `static/stream_controller.js`, `tests/frontend_runtime.test.mjs`, `tests/stream_controller.test.mjs`, `tests/stream_state_helpers.test.mjs`, `tests/test_routes_chat.py`, and `tests/test_streaming_hardening_guards.py`, plus untracked `routes_chat_stream_service.py`, `routes_chat_stream_generator.py`, `tests/test_routes_chat_management_service.py`, and `tests/test_routes_chat_stream_service.py`.
  - `source .venv/bin/activate && python -m py_compile routes_chat_stream.py routes_chat_stream_service.py routes_chat_stream_generator.py tests/test_routes_chat_stream_service.py tests/test_routes_chat_management_service.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_streaming_hardening_guards.py` → success.
  - `source .venv/bin/activate && python -m pytest -q tests/test_routes_chat_stream_service.py tests/test_routes_chat_management_service.py tests/test_routes_chat.py tests/test_routes_meta.py tests/test_streaming_hardening_guards.py -k "routes_chat_stream_service or routes_chat_management_service or stream_resume_emits_synthetic_terminal_when_queue_silent or stream_resume_emits_done_for_dead_job_with_live_warm_handoff or stream_efficiency_mode_throttles_job_state_db_reads or resume_stream_recovers_done_terminal_state_when_queue_is_empty or resume_stream_emits_segment_rollover_meta_before_expected_mobile_reconnect or routes_chat_stream_supports_mobile_segment_rollover_controls"` → `16 passed, 127 deselected in 1.47s`.
  - Fresh verification closed the earlier backlog assumptions for the stream route split and direct service coverage: `routes_chat_stream.py` is already a thin registrar, and direct extracted-module tests now exist for both `routes_chat_management_service.py` and the stream service/generator boundary.
- Refactor-pass refresh evidence (2026-04-08):
  - `git status --short --branch` → dirty tree centered in `hermes_client.py`, `hermes_client_types.py`, `miniapp_config.py`, frontend helper/controller modules, and matching tests; untracked planning notes under `docs/maintainers/plans/`.
  - `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` now shows top production monoliths led by `hermes_client.py` (2453), `job_runtime.py` (851), `job_runtime_worker_launcher.py` (621), `routes_chat_management.py` (613), and `server.py` (569).
  - `find static tests -type f \( -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' \) -print0 | xargs -0 wc -l | sort -nr | head -n 30` still shows frontend hotspots in `static/app.js` (2981), `static/render_trace_helpers.js` (1328), `static/stream_controller.js` (975), `tests/stream_controller.test.mjs` (1158), and `tests/render_trace_helpers.test.mjs` (934).
  - `source .venv/bin/activate && python -m pytest -q tests/test_config.py tests/test_hermes_client.py tests/test_streaming_hardening_guards.py` → `115 passed, 5 failed`; failures cluster in warm-reuse policy defaults, warm-reuse decision gating, fallback label contract (`agent` vs `agent-worker-isolated`), and startup ownership reporting (`auto` requested vs `shared` effective).
  - `node --test tests/frontend_runtime.test.mjs tests/stream_controller.test.mjs tests/render_trace_helpers.test.mjs tests/chat_history_helpers.test.mjs tests/stream_state_helpers.test.mjs` → `134 passed, 0 failed`.
- `git status --short --branch`:
  - `## main...origin/main`
  - tracked modifications in runtime/store/routes/tests and untracked docs/new modules.
- Initial baseline signal before execution:
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py tests/test_broad_exception_policy.py`
  - `42 passed, 1 failed`
  - failure: `tests/test_broad_exception_policy.py::test_broad_exception_handlers_require_policy_and_observability`
  - violations: `job_runtime.py` lines 221 and 229 missing `broad-except-policy:` justification.
- Execution validations (controller session, 2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py` → `1 passed in 0.03s`
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py` → `26 passed in 1.52s`
  - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py` → `18 passed in 1.62s`
  - `.venv/bin/python -m pytest -q tests/test_store.py` → `27 passed in 0.17s`
  - `.venv/bin/python -m pytest -q tests/test_config.py` → `7 passed in 0.02s`
  - `.venv/bin/python -m pytest -q tests/test_routes_jobs_runtime.py tests/test_store.py tests/test_config.py` → `52 passed in 1.74s`
- Code audit signals addressed:
  - `hermes_client_agent.py` subprocess PIPE lifecycle now has explicit idempotent stdio close handling in teardown paths.
- Planning refresh evidence (2026-03-27 post-R7):
  - `git status --short --branch` → dirty working tree with split transport modules and runtime/store/route updates still in-flight.
  - `search_files("except Exception")` highlights persistent broad handlers and embedded child-runner broad catch in `_agent_runner_script()` (string code path not AST-checked by broad-exception policy gate).
  - `search_files("hermes_client_agent_(direct|persistent)", tests/)` indicates transport modules are mostly covered via top-level client tests, with limited direct module contract coverage.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `27 passed in 1.56s`.
- Execution validations for R8/R9 (2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "persistent and (timeout or watchdog or stream_events)"` → `3 passed, 26 deselected in 0.15s`.
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py -k "tool_progress or stream_via_agent"` → `5 passed, 24 deselected in 1.70s`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `30 passed in 2.80s`.
- Execution validations for R10 (2026-03-27):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py` → `32 passed in 3.23s`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.18s`.
- Refactor-pass refresh evidence (monolith audit, 2026-03-27):
  - `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` shows top monoliths: `job_runtime.py` (746), `server.py` (545), `hermes_client.py` (526), `store_jobs.py` (512), `store_chats.py` (470).
  - `read_file` audit confirmed multi-concern mixing in `server.py` (bootstrap/hooks/routes), `hermes_client.py` (config/diagnostics + routing), and `job_runtime.py` (worker loop + event bus + policy).
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_hermes_client.py` → `81 passed in 5.40s`.
- Follow-up cleanup evidence (2026-03-28, hermes_client monolith reduction):
  - `wc -l hermes_client.py` → `354`.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.18s`.
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_jobs_runtime.py tests/test_routes_auth.py` → `55 passed in 2.50s`.
  - `.venv/bin/python -m pytest -q tests/test_config.py tests/test_store.py` → `34 passed in 0.19s`.
- Follow-up cleanup evidence (2026-03-28, job_runtime monolith reduction):
  - `wc -l job_runtime.py job_runtime_chat_job.py` → `509 job_runtime.py`, `194 job_runtime_chat_job.py`.
  - `python -m py_compile job_runtime.py job_runtime_chat_job.py` (with `.venv` active) → success.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_routes_jobs_runtime.py` → `19 passed in 1.57s`.
  - `.venv/bin/python -m pytest -q tests/test_routes_chat.py tests/test_routes_auth.py` → `37 passed in 0.98s`.
  - `.venv/bin/python -m pytest -q tests/test_config.py tests/test_store.py` → `34 passed in 0.16s`.
- Full-project refactor/hardening assessment refresh (2026-03-28):
  - `git status --short --branch` confirms broad dirty tree still centered in server/runtime/store/routes/tests with newly extracted helper modules untracked.
  - `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` current top monoliths: `store_jobs.py` (512), `job_runtime.py` (509), `store_chats.py` (470), `server.py` (460), `hermes_client_agent_direct.py` (372).
  - `search_files("TODO|FIXME|XXX|HACK", "*.py")` → no placeholder debt markers in code.
  - `search_files("job_runtime_chat_job|job_runtime_events|server_startup|server_public_routes|hermes_client_bootstrap", path=tests/)` → no direct test references for newly extracted modules; added R17 to close this gap.
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py tests/test_hermes_client.py` → `33 passed in 3.22s`.
- Execution validations for R17 (2026-03-28):
  - `.venv/bin/python -m pytest -q tests/test_hermes_client_bootstrap.py tests/test_job_runtime_events.py tests/test_job_runtime_chat_job.py tests/test_server_startup.py tests/test_server_public_routes.py` → `16 passed in 0.12s`.
  - `.venv/bin/python -m pytest -q tests/test_hermes_client.py tests/test_routes_jobs_runtime.py tests/test_routes_chat.py` → `81 passed in 5.40s`.
- Execution validations for R14 (2026-03-28):
  - `python -m py_compile store_jobs.py store_jobs_claim.py store_jobs_retry.py store_jobs_queries.py` (with `.venv` active) → success.
  - `.venv/bin/python -m pytest -q tests/test_store.py tests/test_routes_jobs_runtime.py` → `46 passed in 1.81s`.
  - Added targeted stale-cleanup reason regression: `test_cleanup_stale_jobs_thread_missing_reason_mapping`.
- Post-batch policy sanity check (2026-03-28):
  - `.venv/bin/python -m pytest -q tests/test_broad_exception_policy.py` → `1 passed in 0.03s`.
- Live browser refactor audit (2026-03-28 late):
  - `git status --short --branch` → dirty working tree centered in stream/auth/server/frontend files with untracked local scratch demos.
  - Python hotspot scan: `job_runtime.py` (627), `server.py` (436), `routes_chat_stream.py` (308), `routes_auth.py` (180), `miniapp_config.py` (157).
  - Frontend hotspot scan: `static/app.js` (3562), `static/stream_controller.js` (410), helper modules all far smaller (`<= 299`).
  - Live browser QA against the deployed `/app` route now works via dev auth; console showed that after SSE framing fix, live `meta`/`tool`/`chunk`/`done` events parse correctly and render during pending state.
  - Service logs during that QA captured `GET /app?dev_secret=*** plus TelegramBot preview traffic to the same path, proving current query-string desktop bootstrap leaks secrets into logs and previews.
  - Production `.env` currently keeps `MINIAPP_REQUEST_DEBUG=1` and `MINI_APP_STREAM_TIMING_DEBUG=1`, which was useful for forensics but increases log volume and sensitive preview exposure.
- Refactor refresh evidence (2026-04-02):
  - `git status --short --branch` → dirty working tree on `main` with active helper extraction edits in `static/*_helpers.js`, `static/app.js`, `job_runtime.py`, `routes_chat_stream.py`, `miniapp_config.py`, and paired Python/Node test updates plus untracked app-delegation test files.
  - Backend hotspot scan: `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` → largest modules now `job_runtime.py` (629), `routes_chat_management.py` (600), `server.py` (491), `store_schema.py` (435), `hermes_client.py` (430).
  - Frontend hotspot scan: `find static tests -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' | xargs wc -l | sort -nr | head -n 30` → `static/app.js` still largest (2603) and `static/render_trace_helpers.js` is now 1324 LOC, becoming a new helper monolith.
  - Placeholder debt scan: `search_files("TODO|FIXME|HACK|XXX", file_glob="*.py")` and equivalent static JS scan → no marker debt found.
  - Python validation sweep: `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py tests/test_server_public_routes.py tests/test_streaming_hardening_guards.py tests/test_server_startup.py` → `66 passed in 1.30s`.
  - Frontend validation sweep: `node --test tests/bootstrap_auth_helpers.test.mjs tests/bootstrap_auth_app_delegation.test.mjs tests/chat_admin_helpers.test.mjs tests/chat_admin_app_delegation.test.mjs tests/chat_history_helpers.test.mjs tests/chat_history_meta_app_delegation.test.mjs tests/file_preview_helpers.test.mjs tests/file_preview_app_delegation.test.mjs tests/keyboard_shortcuts_helpers.test.mjs tests/keyboard_shortcuts_app_delegation.test.mjs tests/render_trace_helpers.test.mjs tests/render_trace_app_delegation.test.mjs tests/stream_state_helpers.test.mjs tests/stream_state_app_delegation.test.mjs` → `104 passed, 0 failed`.
- Refactor refresh evidence (2026-04-08):
  - `git status --short --branch` → dirty working tree on `main` with tracked edits in `hermes_client.py`, `hermes_client_types.py`, `miniapp_config.py`, `server_startup.py`, `static/app.css`, `static/app.js`, `static/chat_history_helpers.js`, `static/composer_viewport_helpers.js`, `static/render_trace_helpers.js`, `static/runtime_helpers.js`, `static/stream_controller.js`, `static/stream_state_helpers.js`, `templates/app.html`, paired Node/Python tests, plus untracked docs under `docs/maintainers/plans/`.
  - Backend hotspot scan: `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` → largest current modules are `tests/test_hermes_client.py` (3046), `hermes_client.py` (2453), `tests/test_routes_chat.py` (1134), `tests/test_job_runtime_worker_launcher.py` (1004), `tests/test_store.py` (982), `tests/test_routes_meta.py` (933), `job_runtime.py` (851), `tests/test_routes_jobs_runtime.py` (823), `hermes_client_types.py` (736), and `routes_chat_management.py` (613).
  - Frontend hotspot scan: `find static tests -type f \( -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' \) -print0 | xargs -0 wc -l | sort -nr | head -n 30` → `static/app.js` (2981) remains the main frontend monolith; `static/render_trace_helpers.js` (1328), `tests/stream_controller.test.mjs` (1158), `tests/chat_history_helpers.test.mjs` (1152), `static/stream_controller.js` (975), and `tests/render_trace_helpers.test.mjs` (934) are the next largest frontend surfaces.
  - Placeholder debt scan: `search_files("TODO|FIXME|XXX|HACK", file_glob="*.py")` and equivalent static JS scan → no placeholder debt markers in current source.
  - Targeted Python validation: `source .venv/bin/activate && python -m pytest -q tests/test_config.py tests/test_hermes_client.py tests/test_streaming_hardening_guards.py` → `5 failed, 115 passed in 5.26s`; failures are all in `tests/test_hermes_client.py` warm-runtime semantics (`test_evaluate_warm_reuse_policy_defaults_disabled`, `test_record_warm_reuse_decision_available_policy_blocked`, `test_stream_events_attempts_warm_reuse_when_policy_allows`, `test_stream_events_skips_warm_reuse_attempt_when_policy_blocks`, `test_runtime_status_reports_recall_health`).
  - Targeted frontend validation: `node --test tests/chat_history_helpers.test.mjs tests/composer_viewport_helpers.test.mjs tests/frontend_runtime.test.mjs tests/render_trace_helpers.test.mjs tests/stream_controller.test.mjs tests/stream_state_helpers.test.mjs` → `141 passed, 0 failed`.
- Execution validations for R58/R63 (2026-04-12):
  - `node --check static/app.js tests/bootstrap_auth_app_delegation.test.mjs tests/startup_bindings_app_delegation.test.mjs tests/chat_tabs_app_delegation.test.mjs tests/interaction_app_delegation.test.mjs tests/render_trace_history_app_delegation.test.mjs tests/template_startup_script_order.test.mjs` → success.
  - `node --test tests/bootstrap_auth_app_delegation.test.mjs tests/startup_bindings_app_delegation.test.mjs tests/chat_tabs_app_delegation.test.mjs tests/interaction_app_delegation.test.mjs tests/render_trace_history_app_delegation.test.mjs tests/template_startup_script_order.test.mjs` → `11 passed`.
  - `source .venv/bin/activate && python -m pytest -q tests/test_routes_meta.py -k "render_trace or startup or helper"` → `26 passed, 42 deselected`.
  - `static/app.js` now centralizes late-bound helper/bootstrap lookup in `createDeferredHelperRegistry(...)` and routes controller dependency assembly through named composition helpers (`createHistoryRenderControllerDeps(...)`, `createStartupBindingsControllerDeps(...)`, `createInteractionControllerDeps(...)`) instead of large inline object literals.
- Refactor-pass refresh evidence (2026-04-12 late):
  - `git status --short --branch` → `## main...origin/main` with modified plan/log docs, backend runtime/config/route files (`hermes_client_types.py`, `job_runtime_chat_job.py`, `job_runtime_worker_launcher.py`, `miniapp_config.py`, `routes_auth_service.py`, `routes_chat_management_service.py`, `server.py`, `store_chat_queries.py`, `store_models.py`, `store_schema.py`), frontend app/helper files (`static/app.js`, `static/chat_history_helpers.js`, `static/chat_tabs_helpers.js`, `static/composer_viewport_helpers.js`, `static/interaction_helpers.js`, `static/render_trace_history_helpers.js`, `static/stream_controller.js`, `templates/app.html`), paired JS/Python tests, plus untracked split runtime suites and `tests/render_trace_history_app_delegation.test.mjs`.
  - Backend hotspot scan: `find . -name '*.py' -not -path './.venv/*' -print0 | xargs -0 wc -l | sort -nr | head -n 30` → current production hotspots led by `hermes_client.py` (2964), `hermes_client_types.py` (940), `job_runtime_worker_launcher.py` (835), `server.py` (682), `miniapp_config.py` (483), `store_chats.py` (468), `routes_chat_management_service.py` (463), `store_schema.py` (458), and `job_runtime_chat_job.py` (380).
  - Frontend hotspot scan: `find static tests -type f \( -name '*.js' -o -name '*.mjs' -o -name '*.ts' -o -name '*.tsx' \) -print0 | xargs -0 wc -l | sort -nr | head -n 30` → `static/app.js` (3061), `static/stream_controller.js` (1966), `static/chat_history_helpers.js` (1717), `static/chat_tabs_helpers.js` (1022), `static/bootstrap_auth_helpers.js` (846), `static/chat_admin_helpers.js` (807), `static/interaction_helpers.js` (795), and `static/render_trace_message_helpers.js` (744).
  - Function-size audit on the remaining monoliths shows the current regrowth seams: `createStartupBindingsControllerDeps(...)` (~122 LOC), `createBootstrapAuthControllerDeps(...)` (~108 LOC), `createChatTabsControllerDeps(...)` (~96 LOC), and `createDeferredControllerHelper(...)` (~75 LOC) in `static/app.js`; `createToolTraceController(...)` (~199 LOC), `createStreamSessionController(...)` (~196 LOC), `createTranscriptConsumeController(...)` (~193 LOC), and `createStreamResumeController(...)` (~161 LOC) in `static/stream_controller.js`; `createHistoryHydrationController(...)` (~257 LOC), `createHistoryOpenController(...)` (~174 LOC), and `createReadThresholdController(...)` (~173 LOC) in `static/chat_history_helpers.js`; `createController(...)` (~339 LOC) and `createTabPresentationController(...)` (~200 LOC) in `static/chat_tabs_helpers.js`; `_build_owner_state_summary(...)` and reusable-candidate helpers in `hermes_client_types.py` still concentrate warm-session shaping logic.
  - Direct coverage linkage scan indicates the recent split frontend runtime suites are present and the app-shell delegation contract test for history render now exists (`tests/render_trace_history_app_delegation.test.mjs`), so the current debt is primarily structural regrowth rather than missing direct coverage for the newest helper seams.
  - Targeted JS validation: `node --check static/app.js static/chat_tabs_helpers.js static/chat_history_helpers.js static/stream_controller.js static/interaction_helpers.js static/render_trace_history_helpers.js static/composer_viewport_helpers.js tests/chat_tabs_helpers.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/interaction_helpers.test.mjs tests/stream_controller_policy_session.test.mjs tests/frontend_runtime_stream_activity.test.mjs tests/frontend_runtime_history_render.test.mjs tests/render_trace_history_app_delegation.test.mjs tests/template_startup_script_order.test.mjs` → success.
  - Targeted JS validation: `node --test tests/chat_tabs_helpers.test.mjs tests/chat_tabs_app_delegation.test.mjs tests/chat_history_open_hydrate.test.mjs tests/chat_history_read_mutation.test.mjs tests/chat_history_visibility_sync.test.mjs tests/interaction_helpers.test.mjs tests/interaction_app_delegation.test.mjs tests/stream_controller_policy_session.test.mjs tests/stream_controller_resume_finalize.test.mjs tests/render_trace_history_app_delegation.test.mjs tests/frontend_runtime_stream_activity.test.mjs tests/frontend_runtime_history_render.test.mjs tests/frontend_runtime_chat_tab_cycle.test.mjs tests/template_startup_script_order.test.mjs` → `181 passed`.
  - Targeted Python validation: `source .venv/bin/activate && python -m py_compile hermes_client_types.py job_runtime_chat_job.py job_runtime_worker_launcher.py miniapp_config.py routes_auth_service.py routes_chat_management_service.py server.py store_chat_queries.py store_models.py store_schema.py tests/test_hermes_client.py tests/test_job_runtime_chat_job.py tests/test_job_runtime_worker_launcher.py tests/test_routes_auth_service.py tests/test_routes_chat.py tests/test_routes_chat_management_service.py tests/test_routes_meta.py` → success.
  - Targeted Python validation: `source .venv/bin/activate && python -m pytest -q tests/test_hermes_client.py -k "warm_reuse or attach or recall_health or child_spawn or registry_owner_state_payload_and_detail_helpers" tests/test_job_runtime_chat_job.py tests/test_job_runtime_worker_launcher.py -k "configured_worker_launcher or subprocess or timeout or warm" tests/test_routes_auth_service.py tests/test_routes_chat_management_service.py tests/test_routes_meta.py -k "render_trace or startup or helper or chat_management"` → `35 passed, 194 deselected`.
