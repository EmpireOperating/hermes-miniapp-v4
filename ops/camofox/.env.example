# Copy to ops/camofox/.env and fill with strong random values.
CAMOFOX_API_KEY=replace_with_hex_key
CAMOFOX_ADMIN_KEY=replace_with_hex_key

# Optional absolute host path for read-only cookie imports.
# CAMOFOX_COOKIES_DIR=/path/to/camofox/cookies
